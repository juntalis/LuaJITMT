# M1 Notes

Initial slice:

- Copied `plan/auxiliary/lj_atomic.h` to `src/lj_atomic.h` unchanged, per 02 §2.6 and 12 §M1.
- Seeded `src/lj_mtfields.md` from 02 §2.5.
- No runtime code includes `lj_atomic.h` yet in this slice; behavior should remain unchanged.
- User explicitly scoped verification to x86-64 Linux and said to ignore AArch64.

Verification:

- `cmp -s plan/auxiliary/lj_atomic.h src/lj_atomic.h`: identical.
- `gcc -std=gnu99 -Wall -Wextra -Werror -mcx16 -Isrc -include src/lj_atomic.h -x c -c /dev/null -o /tmp/lj_atomic_gcc.o`: passed.
- `clang -std=gnu99 -Wall -Wextra -Werror -mcx16 -Isrc -include src/lj_atomic.h -x c -c /dev/null -o /tmp/lj_atomic_clang.o`: passed.
- `tools/ci/m0_matrix.sh`: passed under the then-current invariant check matrix.
  This result is superseded by the default lockless JIT on/off matrix below.

Follow-up slice after removing the `LJ_MT` compatibility wall:

- Updated ignored `plan/*.md` instructions so the lockless x86-64 Linux runtime is the primary path, not hidden behind `LUAJIT_THREADSAFE`/`LJ_MT`; removed active ARM64/aarch64 work from the milestone instructions.
- Made `LJ_MT` always-on internally in `lj_arch.h` and changed the platform guard to x86-64 Linux + GC64.
- Follow-up legacy cleanup removed the residual `LJ_MT` macro entirely, keeping
  only the x86-64 Linux + GC64 platform guard, and flattened internal
  root/metatable/thread helper aliases onto their acquire/release primitives.
- Added always-on `tv_rawload`/`tv_rawstore` routing through `lj_atomic.h` for the main TValue setters/copy path.
- Added the initial `TGState` scaffold, embedded `GG_State.main_tg`, `global_State.main_tg`, `global_State.jitp`, and `lua_State.tg_hint`.
- Initialized/freed the main TG from `lj_state.c`, copied dispatch/hotcount templates into it, and added `lj_tg.o` to normal and amalgamated builds.
- Updated `tools/ci/m0_matrix.sh` to run the default lockless runtime with JIT on/off only.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN) XCFLAGS="-DLUAJIT_DISABLE_JIT"`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `src/luajit -e "require'ffi'; assert(2^31 == 2147483648); local t={}; for i=1,1000 do t[i]=i end; assert(t[999]==999)"`: passed.

TG accessor migration slice:

- Added `G2TG`/`L2TG` in `lj_obj.h` so non-dispatch C code can reach the current TG without relying on `GG_State`.
- Added transitional helpers in `lj_tg.h` for `cur_L` and `jit_base`:
  - `lj_tg_setcur_L`, `lj_tg_clearcur_L`, `lj_tg_cur_L`.
  - `lj_tg_setjit_base`, `lj_tg_jit_base`.
  - These update/read TG state while mirroring through `g->cur_L`/`g->jit_base` until vm_x64.dasc stops addressing those global fields directly.
- Migrated C-side scratch buffer use to `TG.tmpbuf`:
  - `lj_buf`, `lib_os`, `lj_serialize`, `lj_gc`, recorder BUFHDR constants.
  - Kept `g->tmpbuf` initialized/freed for now as a transitional compatibility field.
- Migrated C-side temporary TValue communication to `TG.tmptv/tmptv2`:
  - API pseudo-index scratch, FFI/cdata comparison result handoff, recorder post-processing save/restore.
  - Left architecture backends that embed `g->tmptv` offsets for the later x86-64 VM/asm migration.
- Migrated JIT-side PRNG use to `TG.prng`:
  - trace assembly random RA state, mcode hint probing, hotcount retry jitter, trace penalty jitter.
  - Left string interning uses of `g->prng` for the later concurrent string table work.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN) XCFLAGS="-DLUAJIT_DISABLE_JIT"`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `src/luajit -e "local ffi=require'ffi'; local x=ffi.new('int[1]', 7); assert(x[0]==7); local t={}; for i=1,1000 do t[i]=('%d'):format(i) end; assert(t[999]=='999')"`: passed.
- The per-TG accessor invariant is documented beside the TG helper layer and
  covered through the build, stock suite, FFI smoke, and later runtime behavior
  fixtures. Keep the rationale in the helper-layer comments: per-TG fields are
  mutator-local publication state, while universe-owned fields remain in
  `global_State`.

Dispatch/hotcount mirror prep for x64 TG dispatch migration:

- `TG.dispatch` and `TG.hotcount` were previously copied only once during `lj_tg_init`.
- Added `lj_tg_sync_dispatch(g)` and call it after dispatch mode transitions so the main TG dispatch table mirrors the GG template.
- Updated `lj_dispatch_init_hotcount(g)` to initialize both `GG.hotcount` and `TG.hotcount`.
- Added `hotcount_setg(g, pc, val)` and migrated JIT hotcount writes in `lj_trace.c`/`lj_record.c` to update both mirrors.
- Added `LJ_STATIC_ASSERT(GG_DISP2HOT == TG_DISP2HOT)` under `LJ_HASJIT`; this preserves the current VM hotcount addressing invariant and prepares for `DISPATCH` pointing at `TG.dispatch`.
- This is a bridge slice: VM asm still uses `GG.dispatch`, but the TG mirror is now live instead of stale.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN) XCFLAGS="-DLUAJIT_DISABLE_JIT"`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `src/luajit -e "jit.on(); local s=0; for i=1,1e6 do s=s+i end; assert(s==500000500000)"`: passed.

x64 VM/JIT TG-owned field bridge:

- Added `GG_DISP2TG(field)` as a transitional x64 addressing helper while
  `DISPATCH` still points at `GG.dispatch`.
- Migrated x64 VM references for TG-owned `cur_L`, `jit_base`, `tmpbuf`, and
  `tmptv` to `GG_DISP2TG(...)`; non-TG globals still use `DISPATCH_GL`.
- Migrated x86-64 JIT generated operands for `tmptv`, `cur_L`, and
  `jit_base` to `J2TG`/`emit_*tg`.
- Important bug found during verification: generated trace stores in RETF and
  snapshot-exit code still wrote `g->jit_base`, while the x64 VM exit path now
  reads `tg->jit_base`. Moving those stores to `emit_settg` fixed a stock
  JIT-suite segfault/hang around `lang/compare.lua --- pcall`.
- Full switch to `TG.dispatch` as the `DISPATCH` base is still pending; this
  slice only bridges TG-owned fields from the current `GG.dispatch` base.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN) XCFLAGS="-DLUAJIT_DISABLE_JIT"`: passed.
- `cd tests/stock/test && ../../../src/luajit test.lua --quiet`: 508 passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `src/luajit -e "jit.on(); local s=0; for i=1,1e6 do s=s+i end; assert(s==500000500000)"`: passed.
