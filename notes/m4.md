# M4 notes

C-only thread substrate:

- Added `lj_thr.{h,c}` as the first pthread-backed OS-thread substrate:
  - `lj_thr_create` / `lj_thr_join` wrap pthread create/join and assign a
    monotonic small runtime id.
  - `lj_thr_set_tg` / `lj_thr_get_tg` provide the planned one-TG-per-OS-thread
    TLS pointer.
  - `lj_thr_cpucount` wraps `_SC_NPROCESSORS_ONLN`.
  - `lj_thr_fence` wraps the existing seq_cst atomic fence helper.
  - `lj_thr_sleep_ns` marks sleep as a native region and polls on leave when a
    Lua state is available.
- Wired `lj_thr.o` into normal builds and included `lj_thr.c` in the amalgam
  translation unit.
- Linked Linux targets with `-pthread`, because the core now references
  pthread entrypoints directly.
- Added `tests/t-thr-substrate.c` and `tools/ci/m4_thr_substrate.sh`.
  The test verifies:
  - main-thread TG TLS set/get;
  - `cpucount` and fence entrypoints;
  - native-state sleep acks a pending safepoint request on leave;
  - pthread create/join around a child TG that owns an anchored Lua thread,
    attaches while in native state, then detaches and clears TLS.

Verification:

- `tools/ci/m4_thr_substrate.sh`: passed.

M4 pure-compute smoke gate:

- Added `tests/t-mt-smoke.lua` and `tools/ci/m4_threading_smoke.sh` for the
  named M4 `t-mt-smoke` requirement.
- The test runs `-joff` thread sets from 1 through 8 workers by default,
  spawns all workers before joining, and checks deterministic arithmetic
  results plus `thread:running()` after join.
- The worker body is intentionally narrow: pure arithmetic loop plus returned
  results, with parent-side table bookkeeping only.

Verification:

- `tools/ci/m4_threading_smoke.sh`: passed.
- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.

M4 VM shutdown of live threads:

- Original M4 slice: added `GCROOT_THREADING` and rooted the threading
  live-thread table from `global_State.gcroot[]` when the first thread was
  spawned. Current implementation keeps that original context but replaces the
  mutable shared table with the native `global_State.threading_live` root list.
- Added `lj_threading_shutdown(L)`, called from `lua_close()` before finalizer
  separation and state teardown. It asserts main-OS-thread close in assert
  builds, issues `LJ_GC2_HS_STOPREQ` while child Lua threads are active,
  waits for rooted secondary `LJThread` handles to reach DONE, joins unjoined
  pthreads, and removes the native live-root entry.
- Added `lj_safepoint_checkstop(L, actions)` and wired it into channel waits,
  timed channel waits, `thread:join()`, mutex waits, and `threading.sleep()`.
  Blocking APIs now turn STOPREQ into the planned
  `thread interrupted: VM shutdown` Lua error.
- Added `tests/t-threading-shutdown.lua` and
  `tools/ci/m4_threading_shutdown.sh`. The test drops an unjoined worker
  blocked in `channel:recv()` under `pcall` and another unjoined worker in a
  CPU-bound Lua loop; process shutdown must interrupt both, let them write
  markers, join them, and then exit.
- Follow-up: live-root list head operations now use explicit helpers too.
  `lj_thread_live_head_*()` owns `global_State.threading_live`
  acquire/CAS/xchg, and `lj_thread_live_node_*()` owns `LJThread.live_node`
  publication/clearing. The shutdown notes document why raw covered access in
  `lib_threading.c`, legacy GC, and GC2.
- Verification: `git diff --check`, `tools/ci/m4_threading_shutdown.sh`,
  `tools/ci/m4_threading_api.sh`, and `tools/ci/m3_gc2_scaffold.sh` passed.
- Added `lj_safepoint_ack_check()` and routed x64 VM safepoint polls through
  it so a mutator that acknowledges `LJ_GC2_HS_STOPREQ` throws immediately
  instead of acknowledging shutdown and continuing bytecode execution.
- Changed the x64 VM poll trigger to check the global pending-handshake count
  (`g->gc2.hs_pending`) before calling the checked ack helper. This keeps
  secondary Lua threads polling even while the current x64 dispatch base still
  uses the embedded main `GG_State` layout.
- Later M6 dispatch-localization work moved the x64 VM poll trigger from the
  original global `DISPATCH_GL(gc2.hs_pending)` check to `TGPOLL`, which reads
  the running `TGState.poll` through the current TG dispatch base. The shutdown
  invariant check now checks the current `TGPOLL` marker plus the checked ack helper
  instead of the original global pending-count marker.

Verification:

- `tools/ci/m4_threading_shutdown.sh`: passed.
- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_threading_smoke.sh`: passed.
- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.
- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

Channel peek API conformance:

- Changed empty `ch:peek()` from `nil, "empty"` to `nil, false`, matching
  `plan/09_threading_api.md` section 9.1.
- Updated `tests/t-threading-api.lua` assertions for initial empty peek and
  parent-held mutex worker peek.

Verification:

- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_chan_stress.sh`: passed.

M4 `lua_State` owner claim scaffold:

- Added `lua_State.thr_owner` and `scan_epoch`, plus `TGState.tid`.
- Main TGs now get a nonzero tid during `lj_tg_init`; `threading.current()`
  reports that tid for the main-thread handle.
- Added `lj_thr_newid`, `lj_thr_current_id`, `lj_state_claim`, and
  `lj_state_release`.
- `threading.spawn` now claims the new child `lua_State` while populating its
  initial stack, releases it before `pthread_create`, preassigns the child tid,
  and the child pthread claims the state before running Lua. The child releases
  ownership after result publication and before DONE.
- `thread:join()` claims a DONE child state while copying cached child-stack
  results, so concurrent joiners serialize the cross-thread read through the
  same owner word.
- Updated `src/lj_mtfields.md` to mark `L->thr_owner` as an active shared
  claim field rather than a future one.
- Remaining owner-protocol work:
  - `coroutine.resume` ownership transfer/error semantics;
  - GC worker `GCSCAN` claims for suspended coroutine stacks;
  - `stack_dirty_epoch` maintenance on resume/yield/state transitions;
  - debug/C API foreign-state checks.

Verification:

- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_threading_smoke.sh`: passed.
- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.
- `tools/ci/m4_threading_shutdown.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.

TG mirror fallback tightening:

- Tightened `lj_tg_cur_L(g)` and `lj_tg_jit_base(g)` so the legacy
  `global_State.cur_L` / `jit_base` mirrors are used only when no raw TLS TG
  is installed. If a secondary OS thread has a TLS TG and that TG's per-thread
  field is null, the accessor now returns null instead of falling back to the
  main/global mirror.
- Extended `tests/t-thr-substrate.c` to poison the global mirrors while a
  worker TLS TG is active, then assert the accessors return the worker TG's
  null fields rather than the global mirrors.

Verification:

- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m4_threading_capi.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.

Attach entrant shutdown accounting:

- Added `global_State.mt_entering` for secondary threads that are inside the
  lifecycle entry protocol but have not yet claimed `mt_live`.
- `luaMT_attach()` now increments this counter before `lj_state_claim()`.
  This covers the external-C-thread window where an attach can block behind
  `mt_gc_exclusive` but is not yet visible to `lua_close()` through `mt_live`
  or the `threading_live` root list.
- `lj_threading_shutdown()` stores `mt_shutdown`, wakes `mt_gc_exclusive`
  waiters, waits for `mt_entering == 0`, and then performs the existing
  STOPREQ/`mt_live` wait. An entrant that observes shutdown drops
  `mt_entering` before returning failure; an entrant that becomes live drops
  `mt_entering` only after `mt_live` is claimed.
- `tests/t-threading-capi.c` forces the edge by setting `mt_gc_exclusive`,
  starting a foreign `luaMT_attach()` on a child state, waiting until
  `mt_entering` is nonzero and `mt_live` is still zero, then calling
  `lua_close()`. The worker must reject attach and `lua_close()` must not
  return before the counted entrant is done.

Channel wait parking stability:

- Contended blocking channel sends/receives, including timed rendezvous send
  completion, use the same CAS ticket and per-slot sequence protocol.
- Waiters now spin for at most 64 CPU pause instructions while watching the
  channel futex generation, then park through `chan_wait()` /
  `chan_wait_timeout()` if no peer made progress. A spin-observed wake on a Lua
  path still checks fresh STOPREQ before retrying.
- This restores the planned short-handoff latency path without reintroducing an
  unbounded pause loop or changing timeout/linearization semantics.

Verification:

- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.
- `tools/ci/m4_threading_shutdown.sh`: passed.

LuaLS threading meta:

- Tightened the tracked `threading.lua` LuaLS meta stub for the full
  Lua-facing `threading.*` API: thread join overloads, mutex methods,
  generic `threading.channel<T>`, typed send/recv/peek contracts, and module
  constructor fields.

- Matched the current C implementation and `tests/t-threading-api.lua`:
  `channel:peek()` reports `value, true` or `nil, false`; only timed
  `send()`/`recv()` report `nil, "timeout"`.
- Follow-up annotation cleanup added named send/thread callback aliases,
  explicit nil-returning method contracts, and a LuaLS error-level workspace
  check against the VS Code server extension.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

Join/GC root-scan progress:

- `lj_state_gcscan_claim()` no longer waits when it observes the anonymous
  `LJ_THREAD_GCSCAN` sentinel. A GC scanner can rediscover the same
  `lua_State` through another grey edge while it owns that sentinel; waiting
  would self-deadlock because the sentinel intentionally carries no owner id.
  Returning "busy" lets GC2 requeue the state or let the owning thread satisfy
  `NEEDSCAN`, preserving nonblocking progress without adding a temporary lock.
- Added `m4_threading_join_gcscan`, a bounded closed-upvalue spawn/join stress
  where workers force full collections while the parent immediately joins.
  This covers the observed worker-in-GC vs parent-in-join liveness cycle.

M4 TSAN runtime instrumentation:

- Strengthened `tools/ci/m4_tsan_drivers.sh` so it rebuilds LuaJIT target
  objects with ThreadSanitizer before linking the C unit drivers.
- Runtime build flags now include `-fsanitize=thread`, `-O1`, debug info, and
  frame pointers through `TARGET_CFLAGS` / `TARGET_LDFLAGS` /
  `TARGET_SHLDFLAGS`.
- Driver builds use matching TSAN/frame-pointer flags. `-Wno-tsan` suppresses
  GCC's known `atomic_thread_fence` instrumentation warning so actual TSAN
  reports stay visible.

Verification:

- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m4_thr_substrate.sh`: passed after the instrumented run.

M4 attach-during-handshake second pass:

- Fixed the §9.3 attach race where a TG could run `tg_attach_catchup()` before
  a handshake starts, then CAS-prepend itself into `g->gc2.tg_list` while the
  leader handshake is still active but after the leader's first list walk.
- `lj_safepoint_handshake()` now:
  - scans the TG list through acquire pointer loads;
  - avoids double-counting TGs already signaled by checking `reqmask`/`poll`;
  - performs the planned second pass for `hs_epoch_ack < epoch` entries;
  - repeats late scans while waiting for pending acknowledgements, so TGs
    inserted while an older TG holds the handshake open are still signaled.
- The shared handshake-updated mirrors `mark_active`, `alloc_black`, and
  `TGF_STOPREQ` are now written through `lj_atomic` helpers.
- `tests/t-thr-substrate.c` now manually inserts a TG without pre-CAS catch-up
  during an active handshake and verifies that the leader catches it up to
  `ALLOC_BLACK` before completion. STOPREQ shutdown coverage lives in the
  dedicated shutdown gate, avoiding sticky interrupt state in this fixture.
- The late-insert probe now waits for the current epoch ack and the remote-ack
  `poll` clear. Action mirrors are published before ack cleanup, so the `poll`
  word is the completion observation; the ack path release-clears it.
- The stronger runtime-TSAN gate caught additional shared-field hygiene issues
  in this path:
  - raw `tg_list` link loads were replaced with acquire pointer loads;
  - concurrent test release of `tg->cur_L` now uses atomic pointer stores, and
    the handshake walk reads `cur_L` atomically.

Verification:

- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m3_safepoint_handshake.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_threading_smoke.sh`: passed.
- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.
- `tools/ci/m4_threading_shutdown.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

Channel close wakeup API coverage:

- Extended `tests/t-threading-api.lua` to cover:
  - a worker blocked in `ch:recv()` waking after parent-side `ch:close()` and
    returning `nil, false`;
  - a worker blocked in `ch:send()` on a full buffered channel waking after
    close and surfacing the existing "closed channel" error;
  - close-drain behavior for the buffered value that existed before close.

Verification:

- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_chan_stress.sh`: passed.

M4 L1-L4 litmus coverage:

- Extended `tests/t-mt-litmus.lua` to mirror the plan's M4 memory-model
  litmus set more directly:
  - L1 message passing: parent writes a shared table field before channel
    send, child checks it after recv;
  - L2 join happens-before: child fills a table and parent validates it after
    join;
  - L3 Dekker-style fence ordering: two threads use `threading.fence()` and
    the test rejects the forbidden `ra == 0 && rb == 0` outcome;
  - L4 per-producer FIFO: two producers feed one channel and the parent checks
    monotonic order per producer.
- Kept the existing explicit-GC-while-child-active and mutex exclusion cases.
- Deliberately did not port later `plan/auxiliary/tests/t-litmus.lua` table-race
  cases yet; concurrent table/object semantics are M5 work.

Verification:

- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- Targeted clean amalgam build: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

Resolved M4 accessor follow-up:

- The remaining runtime-accessor risk was to avoid silently falling back to
  main-TG mirrors while a secondary OS thread already has a real TG. The
  TLS-backed `G2TG` slice and the later TG mirror-fallback tightening below
  close that item for the M4 gate.

TLS-backed `G2TG` access:

- Changed the central `G2TG(g)` path from `g->main_tg` to
  `lj_thr_get_tg_fallback(g)`, which returns the current OS thread's TLS TG
  when set and falls back to `g->main_tg` during bootstrap or code paths that
  have not attached a secondary TG.
- `lj_tg_init` now seeds main-thread TLS as soon as the embedded main TG is
  available.
- `lj_thr_get_tg` remains the raw TLS getter for tests and future callers,
  while `lj_thr_get_tg_fallback` is the runtime-safe accessor used by
  `G2TG`.
- Extended `tests/t-thr-substrate.c` so the main thread and pthread worker
  both assert that `G2TG` resolves to their current TG.

Verification:

- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

Mid-handshake attach catch-up:

- Reworked the safepoint leader from a pre-counted `hs_pending = live`
  snapshot to a sentinel-pending protocol:
  - publish action bits;
  - publish `hs_pending = 1` as the leader sentinel;
  - publish the next epoch;
  - traverse the TG list and increment `hs_pending` only for live TGs whose
    `hs_epoch_ack` is older than the new epoch;
  - drop the sentinel after deterministic/native acks have been attempted.
- This prevents a TG that CAS-prepends after the old live-count snapshot from
  being signaled without also being counted.
- Exposed `lj_safepoint_apply_tg(g, tg, actions)` so attach can self-apply
  the active in-flight action set before making the TG visible in
  `gc2.tg_list`.
- `lj_tg_attach` now samples the current epoch and, when `hs_pending` is
  nonzero, applies the current actions locally and release-stores
  `hs_epoch_ack = epoch`. The leader skips TGs that already acknowledged the
  epoch, so a mid-handshake attach is either counted/signaled or self-caught
  before first bytecode.
- Extended `tests/t-thr-substrate.c` with a controlled race:
  - hold the leader handshake open by clearing a remote TG's `cur_L`;
  - attach a pthread TG while the epoch is pending;
  - assert the child TG self-applies `ALLOC_BLACK` and acks the current epoch
    without incrementing the leader's signaled count;
  - restore the held `cur_L`, ack the counted remote TG, and join both
    threads.

Verification:

- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m3_safepoint_handshake.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

C-only channel substrate:

- Added `lj_chan.{h,c}` as the first channel substrate:
  - bounded power-of-two MPMC ring;
  - per-slot sequence numbers using the Vyukov-style enqueue/dequeue claim
    CAS pattern;
  - `TValue` payload slots;
  - nonblocking `try_send` / `try_recv`;
  - blocking `send` / `recv` wrappers that spin briefly, then park on a
    futex word and mark channel parks as native regions when a Lua state is
    available;
  - graceful `close` wakeups for blocked senders/receivers;
  - capacity zero is represented as a one-slot rendezvous channel whose
    blocking sender waits until the receiver advances `deq`.
- Wired `lj_chan.o` into normal builds and included `lj_chan.c` in the
  amalgam translation unit.
- Added `tests/t-chan-stress.c` and `tools/ci/m4_chan_stress.sh`.
  The test covers:
  - basic try-send/try-recv/close behavior;
  - capacity rounding;
  - rendezvous send blocking until a receiver consumes;
  - 4-producer/4-consumer stress over 20,000 integer `TValue` transfers,
    checking every value is received exactly once.

Verification:

- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

Lua-visible `threading` module base:

- Added `lib_threading.c` with the first Lua-visible threading APIs:
  - `threading.cpucount()`
  - `threading.fence()`
  - `threading.sleep(seconds)`
- The functions are backed by the existing `lj_thr` substrate. `sleep`
  enters native state and polls on leave through `lj_thr_sleep_ns`.
- Registered `threading` through package preload rather than default
  `luaL_openlibs`. This keeps upstream stock global/package contents stable
  while making `require"threading"` work.
- Added `tests/t-threading-api.lua` and `tools/ci/m4_threading_api.sh`.

Verification:

- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m4_chan_stress.sh`: passed.
- Targeted clean amalgam build: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

Lua-visible channel userdata:

- Added `UDTYPE_CHANNEL` userdata wrapping the existing `LJChan` payload.
- Registered channel methods through the `threading.channel` constructor's
  closure environment:
  - `ch:send(value)` blocks until the value is accepted and returns `true`;
    sending to a closed channel raises `"closed channel"`.
  - `ch:recv()` blocks until a value or close and returns `value, true` or
    `nil, false`.
  - `ch:peek()` is nonblocking and returns `value, true`, `nil, "empty"`, or
    `nil, false` when closed and drained.
  - `ch:close()` wakes blocked senders/receivers.
  - `__gc` closes the channel and `__tostring` reports `threading.channel`.
- `threading.channel(capacity)` accepts non-negative capacities. Capacity
  zero remains the rendezvous-channel form from the substrate.
- Added a userdata store barrier before publishing Lua values into channel
  storage so black channels can safely receive white objects.
- Added legacy-GC and GC2 userdata traversal for in-flight channel slots.
- Extended `tests/t-threading-api.lua` to cover constructor/method exposure,
  peek/send/recv/close behavior, closed-send errors, range errors, and channel
  slot GC keepalive for a table payload.
- Updated the ignored implementation plan wording in `plan/09_threading_api.md`
  so the C API additions are not described as guarded by a compatibility flag.

Verification:

- `tools/ci/m4_threading_api.sh`: passed.
- Targeted clean amalgam build: passed.
- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

Lua-visible spawn/join/current slice:

- Added `UDTYPE_THREAD` userdata for `threading.thread` handles.
- Added a `LJThread` payload in `lj_thr.h` with:
  - substrate `LJThr`;
  - child `lua_State *`;
  - separately allocated `TGState *`;
  - state/join/futex/status/result-count fields;
  - a main-thread marker.
- Added `TGState.thread_ud` as a non-root backpointer used by
  `threading.current()`.
- Important lifecycle choice: spawned TGs are allocated separately from the
  userdata and are not freed on join. `lj_tg_detach` leaves dead TG nodes in
  `g->gc2.tg_list`, so tombstone memory must remain address-valid until a real
  reclamation scheme exists.
- `threading.spawn(f, ...)` now:
  - creates a child Lua thread in the same VM;
  - copies `f` and arguments to the child stack;
  - originally rooted a `threading.thread` handle in the thread-method private
    live table; the current code allocates a native live-root node while the
    handle is stack-rooted, claims `mt_live`, publishes the node, then calls
    `pthread_create`;
  - initializes a child TG and lets the pthread attach it before executing Lua.
- The pthread entry runs the child with `lua_pcall(..., LUA_MULTRET, 0)`,
  records either success results or an error object on the child stack, detaches
  the TG, publishes `DONE`, and wakes joiners.
- `thread:join([timeout_s])` now:
  - rejects joining the current/main thread;
  - waits in native state and polls on leave;
  - supports timeout as `nil, "timeout"`;
  - is idempotent and copies cached child-stack results on every join;
  - keeps the live-root node in place until after caller stack growth and
    result copies, so `luaMT_join()` callers holding only a `lua_State *` do
    not lose the child root during an allocating stack check;
  - returns `true, ...results` or `false, errobj`.
- The final winner-side `pthread_join()` native leave now preserves its action
  mask and checks STOPREQ only after result copying, live-root removal, and TG
  reclamation. The previous path discarded actions from that native leave, so a
  STOPREQ acknowledged while joining an already-DONE child could be recorded on
  the joining TG without surfacing at the join boundary; checking after cleanup
  keeps caught interrupts from stranding the joined child in the live list.
- DONE-child result-claim waits now also check STOPREQ after each native sleep
  slice while waiting for a temporarily busy child `lua_State` owner. A
  `luaMT_join()` caller interrupted in that window now throws before claiming
  or releasing the child state; a later join can still copy the cached results
  once the owner clears. `t-threading-capi` pins the wedged-owner path.
- `thread:id()` and `thread:running()` are registered.
- `threading.current()` lazily creates a main-thread handle and returns the
  current TG's handle for spawned threads.
- Legacy GC and GC2 userdata traversal now mark `UDTYPE_THREAD` child
  `lua_State` stacks.
- Extended `tests/t-threading-api.lua` for spawn result tuples with nil holes,
  idempotent joins, child errors, join timeout, `current():id()` from a child,
  and concurrent joiners.
- `tools/ci/m4_threading_api.sh` now runs the Lua smoke test with `-joff`, as
  required by the M4 gate, without adding any build-time `LJ_MT` wall.

Verification:

- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

Lua-visible mutex convenience API:

- Added `UDTYPE_MUTEX` userdata and `threading.mutex()`.
- Registered:
  - `m:lock()` using a futex-backed state word and native-state waits;
  - `m:trylock()` returning a boolean;
  - `m:unlock()` waking waiters and erroring on unlocked mutexes;
  - `__tostring` returning `threading.mutex`.
- Extended `tests/t-threading-api.lua` to cover trylock/lock/unlock behavior,
  an unlock error, and a spawned worker blocked on a parent-held mutex.
- Added the first M4 invariant for automatic legacy GC while secondary Lua
  threads are active. Repeated spawn/join exposed an automatic-GC hang around
  the sixth child; forcing GC only between joins passed. The current guard
  raises `g->gc.threshold` to `LJ_MAX_MEM` for the active-child interval and
  restores the prior threshold after the last child detaches. A later slice
  split this from the one-way `mt_active` latch into `global_State.mt_live`.
- Added a matching `lua_gc` guard for explicit collection while secondary Lua
  threads are active:
  - current behavior routes `collect` and `step` through explicit GC2 request
    assistance during the active-child interval;
  - `stop`/`restart` update the saved logical threshold and keep the temporary
    M4 pause in force;
  - `isrunning` reports the saved logical state, not the temporary M4 pause.
  A child blocked on a channel plus parent-side `collectgarbage()` reproduced
  the hang before this guard.
- Added `global_State.mt_gc_exclusive` so explicit legacy `collect` / `step`
  cannot race a new secondary entrant between the `mt_live` check and the GC
  work. Spawn and `luaMT_attach()` wait out this slow-path word before
  incrementing `mt_live`; `collect` / `step` release it if a live entrant wins
  the race.
- Added `tests/t-mt-litmus.lua` and `tools/ci/m4_threading_litmus.sh`.
  The litmus suite runs 100 `-joff` repetitions covering:
  - join happens-before for child return values;
  - channel handoff visibility after `threading.fence()`;
  - explicit parent `collectgarbage("collect")` / `step` while a child is
    active and blocked;
  - FIFO channel order;
  - mutex exclusion across a blocked spawned worker.
- Added `tests/t-threading-stress.lua` and `tools/ci/m4_threading_stress.sh`.
  The stress suite runs 1000 sequential `-joff` spawn/join cycles and forces
  explicit collections between batches. Each child returns an arithmetic
  result and its `threading.current():id()`, checked against the parent handle.

Verification:

- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

M4 TSAN C-driver gate:

- Added `tools/ci/m4_tsan_drivers.sh`.
- The script builds normal LuaJIT, then compiles and runs:
  - `tests/t-thr-substrate.c`;
  - `tests/t-chan-stress.c`;
  with `-fsanitize=thread`, `-O1`, debug info, warnings, `-mcx16`, and
  `TSAN_OPTIONS=halt_on_error=1 second_deadlock_stack=1`.

Verification:

- `tools/ci/m4_tsan_drivers.sh`: passed.

Channel timeout API:

- Added `LJ_CHAN_TIMEOUT`, timed send/recv C entry points, and Lua
  `ch:send(value, timeout_s)` / `ch:recv(timeout_s)` wrappers.
- Timeout returns are Lua-visible as `nil, "timeout"`; closed send still
  raises "closed channel", and closed empty recv remains `nil, false`.
- Fixed the bounded-ring capacity-1 edge: the per-slot sequence value alone
  aliases full/free at cap 1, so send reservation now checks `enq - deq`
  before claiming a ticket. Added C and Lua regressions so a full cap-1
  channel cannot overwrite the buffered value while a timed sender waits.
- Timed rendezvous sends now cancel their own unconsumed ticket on timeout;
  if a receiver wins the race, send returns success. Blocking rendezvous send
  behavior is unchanged.
- Audited `plan/12_implementation_plan.md` and the targeted plan search for
  `LJ_MT`/`LUAJIT_THREADSAFE` gating language. The remaining hit is the
  binding `plan/00_README_AGENT.md` rule saying not to hide behind those
  flags; no stale implementation-plan lock-behind-`LJ_MT` instruction remains.

Verification:

- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.

Legacy-upvalue MT latch:

- Split the previous active-child counter from the bytecode-compat latch:
  `global_State.mt_active` is now the one-way "secondary Lua threads have
  existed" latch, while `global_State.mt_live` drives temporary M4 GC pause,
  `lua_gc()` behavior, and shutdown STOPREQ decisions.
- `threading.spawn()` flips `mt_active` before the pthread can run. The first
  successful flip closes the spawning Lua state's open upvalues with
  `lj_func_closeuv(L, tvref(L->stack))`, giving pre-latch closures a closed
  cell before publication to another OS thread.
- `lj_func_newL_gc()` now snapshots local-slot legacy upvalues into fresh
  closed cells once `mt_active` is set. Upper-level captures still inherit the
  parent closure's upvalue pointer, matching the M4 legacy-bytecode deviation
  in `plan/10_bytecode_compat.md` section 10.4.
- Added `tests/t-threading-upvalue.lua` and
  `tools/ci/m4_threading_upvalue.sh`. The test covers single-thread
  pre-latch sharing, first-spawn close of an active parent-frame upvalue,
  post-latch sibling capture-by-value, and upper-level capture inheritance.
- Updated `tests/t-mt-litmus.lua` to avoid parent-local upvalue writes as a
  cross-thread result channel after the latch; it now observes worker results
  through `join()` return values and channels.
- Follow-up lifecycle helper slice: `mt_active`, `mt_live`,
  `mt_gc_exclusive`, and `mt_shutdown` now route through `mt_*()` helpers in
  the threading, GC, API, GC2, legacy-upvalue, and finalizer-spawn tests. The
  M4 shutdown notes document why raw production/test fixture access to these shared
  lifecycle and explicit-GC exclusion words.

Verification:

- `tools/ci/m4_threading_upvalue.sh`: passed.
- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.
- `tools/ci/m4_threading_shutdown.sh`: passed.
- `tools/ci/m4_threading_smoke.sh`: passed.
- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.

Thread self-join:

- Changed `threading.current():join(timeout_s)` from a caller error into an
  immediate `nil, "timeout"` for explicit timeouts. Untimed self-join now
  fails fast with `self-join would deadlock` instead of parking forever. The
  self check uses the current TG's thread handle, so coroutines on the same OS
  thread are covered too.
- Joining the main thread from a different Lua thread still errors; the main
  thread has no DONE transition in the M4 runtime model.
- Extended `tests/t-threading-api.lua` to assert timed and untimed self-join
  behavior from the main thread and a worker.

Verification:

- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_thr_substrate.sh`: passed.

Public C threading API:

- Added the planned public declarations to `lua.h`:
  `luaMT_spawn`, `luaMT_join`, `luaMT_fence`, `luaMT_attach`, and
  `luaMT_detach`.
- Refactored Lua-visible spawn/join through shared helpers so the C API and
  `threading.*` use the same child setup, result copying, timeout, idempotent
  join, and self-join handling.
- Added `lua_State.mt_thread`, a GC-scanned reference from a child
  `lua_State *` back to its `threading.thread` userdata. This lets
  `luaMT_join(L, child, timeout)` resolve the durable join state while keeping
  the child state rooted through either the userdata or the native live-thread
  root list.
- Added `GCROOT_THREADING_ENV` so `luaMT_spawn()` can bootstrap the private
  threading metatable environment before `require"threading"` and later
  `require"threading"`/Lua calls reuse the same private environment.
- Added conservative `luaMT_attach()`/`luaMT_detach()` support for a foreign
  OS thread to claim an already-created non-main `lua_State *`, install a TG,
  attach it to the handshake list, run Lua C API work, and detach/release the
  state. Dead TG nodes remain tombstoned in `gc2.tg_list`, matching the
  current M4 reclamation limitation.
- `threading.current()` now uses the cached `__main` handle only for the real
  main Lua thread; an externally attached non-spawned state without a joinable
  `threading.thread` userdata errors instead of borrowing the main handle.
- Added a shutdown latch and `mt_live` futex wait/wake so `lua_close()` rejects
  new secondary entrants and waits for external `luaMT_attach()` threads to
  detach even when no spawned `threading.thread` handle exists in the live
  table.
- Made `lj_tg_detach()` self-ack any pending handshake request before clearing
  the TG signal words, so an attached C thread that detaches during shutdown
  does not strand `gc2.hs_pending`.
- Added `tests/t-threading-capi.c` and
  `tools/ci/m4_threading_capi.sh`. The test covers spawn/join before
  `require"threading"`, join
  timeout then success, error object preservation, `luaMT_fence`, a pthread
  attach/detach smoke, and close-time waiting for an attach-only secondary
  thread; it now also includes an internal owner-wait regression hook.
- Follow-up join owner-wait slice: `thread:join()`/`luaMT_join()` still claim
  a DONE child state before copying cached results, preserving the owner rule
  and concurrent join semantics, but a temporarily busy owner now waits through
  native sleep slices instead of spinning on `la_cpu_pause()`. Stop actions
  observed during that wait are folded into the existing post-copy cleanup
  check so a winner-side join cannot strand the child live root.

Verification:

- `tools/ci/m4_threading_capi.sh`: passed.
- `tools/ci/m4_threading_api.sh`: passed.
- `tools/ci/m4_threading_upvalue.sh`: passed.
- `tools/ci/m4_threading_litmus.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.
- `tools/ci/m4_threading_shutdown.sh`: passed.
- `tools/ci/m4_threading_smoke.sh`: passed.
- `tools/ci/m4_chan_stress.sh`: passed.
- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
  - Includes phase/markbits/traverse, C handshake, VM safepoint, paranoia,
    M2 arena, GC-header invariant check, normal build, amalg build, and stock
    matrix:
    - lockless JIT on: 508 stock tests passed.
    - lockless JIT off: 386 stock tests passed.

TG tombstone list reclamation:

- Added `lj_tg_reclaim_dead(g)`, a conservative leader-side reclamation pass
  for `gc2.tg_list`. It physically unlinks eligible `TGF_DEAD` nodes only when
  the main TG is the sole live registered TG (`n_threads == 1`) and no
  handshake is pending, so active handshake/list walkers cannot hold a
  reclaimed link. M5 later tightened this for arena-internal TGs: owner-local
  allocator state is transferred to the main TG before physical unlink, and a
  dead TG stays linked only if that transfer cannot complete.
- Follow-up helper routing keeps that same attach/detach and reclaim protocol
  but accesses the global registry root/live count only through
  `gc2_tg_*`/`gc2_n_threads_*` helpers. The helper boundary documents the
  required acquire/release ordering, and the M3 safepoint fixture exercises the
  observable attach/detach behavior.
- Wired the pass into regular lifecycle consolidation points:
  `thread:join()` winner cleanup, `lua_close()` threading shutdown cleanup,
  public `luaMT_detach()`, GC2 scaffold cycle/drain boundaries, and
  `lj_gc2_fini()`.
- Updated C substrate and safepoint tests so detached TGs are first proven to
  be skipped while dead, then physically removed from `gc2.tg_list` by the
  reclamation pass.
- This removes dead non-arena TGs from active list walks. M5 adds allocator
  ownership transfer for arena-internal dead TGs before unlink.

Verification:

- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m3_safepoint_handshake.sh`: passed.
- `tools/ci/m4_threading_capi.sh`: passed.
- `tools/ci/m4_threading_stress.sh`: passed.
- `tools/ci/m4_threading_shutdown.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.

TG mirror fallback tightening:

- Declared `lj_thr_get_tg()` for low-level TG accessors and changed
  `lj_tg_cur_L(g)` / `lj_tg_jit_base(g)` so a thread with a raw TLS TG uses
  that TG's fields even when they are NULL.
- The global `g->cur_L` / `g->jit_base` mirrors remain only bootstrap and
  no-TLS fallbacks. A secondary TG no longer accidentally observes the main
  thread's mirrored state.
- Extended `tests/t-thr-substrate.c` with a worker that poisons the global
  mirrors, clears its own TG fields, and asserts the accessors return NULL.

Verification:

- `tools/ci/m4_thr_substrate.sh`: passed.
- `tools/ci/m4_threading_capi.sh`: passed.
- `tools/ci/m4_tsan_drivers.sh`: passed.
- `tools/ci/m3_gc2_scaffold.sh`: passed.
