# M5 notes

M5 legacy GC pacing-control atomics:

- Added `lj_gc_pause_*()` and `lj_gc_stepmul_*()` helpers for the legacy
  `GCState.pause` and `GCState.stepmul` public GC controls.
- `lua_gc(LUA_GCSETPAUSE)` and `lua_gc(LUA_GCSETSTEPMUL)` now use atomic
  exchange helpers, preserving the stock API return value while avoiding raw
  shared-word load/store races.
- Restart-threshold calculation, incremental GC step-limit calculation, GC2
  assist-shift initialization, and state initialization now use the same helper
  accessors.
- Extended `m5_gc2_pacing_atomic` to exercise the helpers and document raw runtime
  access to these control fields outside `lj_gc.h`.

M5 root publication idle barrier:

- `lj_gc_pubroot()` now routes its GC2 side through `lj_gc2_barrier_tv_g()`
  instead of calling `lj_gc2_markobj()` directly. This keeps MARK/WEAK
  publication behavior and idle generational remembered-root behavior, while
  preventing idle stack/root publication from setting arena mark bits.
- This fixes `m2_arena_gcsweep`, where `lua_pushcclosure()` allocated a white C
  closure and then stack publication marked its arena cell before any active
  GC2 mark cycle.

M5 C table value publication helpers:

- Added `lj_tab_store*()` wrappers for common C-side table slot writes. The
  helpers build a stack `TValue` and publish it through `lj_tab_storetv()`,
  which already uses `copyTVrel()` for release-store table-slot publication.
- Converted runtime/library/parser/serializer call sites that wrote directly
  through `lj_tab_set*()` results to use the wrappers or `copyTVrel()`. This
  covers nil/bool/int/intptr/string/table/thread/proto/function/udata values
  in the touched C paths, plus the serializer dictionary preparation that used
  to write raw low bits into a table slot.
- Tightened the follow-up API/table-library cases that hid the table
  destination behind a temporary pointer: direct `lua_settable()`/`lua_setfield()`
  slots initially moved to release stores and now participate in the table CAS
  retry bridge described below; `table.insert()` position shifts plus
  `table.pack()` array initialization use `lj_tab_storetv()`/`lj_tab_storenil()`.
  The Lua 5.2 `table.pack()` `"n"` field now uses a retrying CAS helper too, and
  the completed result table is republished before it is returned.
- Recorder template-table growth markers now use a nil-only CAS helper for the
  temporary self-table sentinel and keep `lj_tab_storenil()` for cleanup, so
  nil-preservation markers are routed through a fresh lookup before resizing.
- `jit.util` result builders now CAS-publish integer, pointer, proto, and
  snapshot-map fields, publish the proto edge explicitly, and republish their
  completed result tables before returning them to Lua.
- `debug.getinfo(..., "L")` now roots the activelines table before filling it,
  CAS-publishes each line marker, and republishes the completed table before the
  debug wrapper attaches it to the result object.
- FFI/clib alias setters that hide slots behind local `TValue *` variables now
  release-publish too: FFI `__newindex` uses `copyTVrel()`, callback slot
  clearing uses `lj_tab_storenil()`, and C library cache constants use
  `lj_tab_storetv()`/`lj_tab_storeint()`.
- Added `lj_tab_storenilraw()` for state-free nil release stores and routed
  `lj_tab_clear()` plus GC weak/finalizer table slot clearing through it.
- Parser constant-table slot markers keep the original compact `hi == 0`
  encoding, but now publish that marker with `const_slot_store()` instead of
  raw `o->u64` stores.
- Parser string/cdata anchors now use a nil-only CAS helper so temporary `true`
  anchors never overwrite an existing constant slot index, while forwarded table
  slots are retried through a fresh lookup.
- Bytecode template-table loading decodes key/value constants into local
  temporaries first and release-publishes array/hash table slots with
  `lj_tab_storetv()`.
- Serializer decode outputs now release-publish through `copyTVrel()`,
  including the early nested-table root store before recursive array/hash fill.
- Serializer dictionary preparation now assigns string/metatable dictionary
  indexes through a nil-only CAS helper, preserving duplicate-entry semantics
  while retrying forwarded slots and republishing the prepared dictionary.
- Snapshot unsinking now restores table-slot values into local temporaries and
  publishes them with `lj_tab_storetv()` instead of letting `snap_restoreval()`
  write directly through a `lj_tab_set()` result.
- This is still a scoped legacy-`GCtab` bridge. It does not implement the
  original 06 section 6.3 RETIRING/FORWARD/CAS write protocol, and hash-slot
  migration correctness remains on the original plan.
- Added `tools/ci/m5_tab_value_publish.sh`, wired into the M5 aggregate, to
  keep raw `lj_tab_set*()` value publication patterns from returning.

M5 VM event cache mask publication:

- Added `vmevmask_*()` helpers for the shared VM-event handler-cache byte in
  `global_State` and routed C-side reads, writes, clears, and restore checks
  through acquire/release/CAS operations. This follows the existing `hookmask`
  helper pattern for another VM-control byte and keeps the stock `jit.attach`
  API and VM-event behavior intact.
- The VM-event send macros now acquire the cache mask before deciding whether
  to prepare a handler call. Missing-handler caching still clears the event bit,
  `jit.attach()` still invalidates the cache with `VMEVENT_NOCACHE`, and nested
  VM-event calls still restore the old mask unless an invalidation was observed.
- The requirement is documented here because the remaining value is the
  memory-ordering invariant.

M5 x64 TSETS hash-store slow path:

- Demoted x86-64 `BC_TSETS_Z` string-key table stores to `vmeta_tsets` instead
  of walking hash chains and writing `Node.val` directly in the VM fast path.
  This covers `TSETS`, `GSET`, and the string-key fallback from `TSETV`.
- Updated the remaining x86-64 interpreter array table stores (`TSETV`,
  `TSETB`, and `TSETR`) so the fast lookup/nil-decision path still runs in the
  VM, but the final slot publication calls `lj_tab_storetv()` instead of
  emitting a raw `mov [RC], ITYPE`. The GC2 follow-up remains the table barrier
  path, preserving SSB traversal semantics for black parent tables.
- `BC_TGETS_Z` keeps the node-header hmask read path for fast string-key loads.
  Array `TSETV`/`TSETB` paths keep their previous-value snapshots.
- This is an interim step: it removes the x64 interpreter's direct legacy
  string-key hash-chain store. The generic x64 `vmeta_tset` continuation now
  calls `lj_meta_tsettv_pair()` so the returned table slot is release-stored
  from C with resolved parent context, but the C/meta table setter is still
  legacy `GCtab` code until the original 06 section 6.3 `NHdr` write protocol
  lands.
- Updated the x64 guards so TGETS still requires node-header hmask loads while
  TSETS requires the hash-store slow path and rejects the old `mov [TMPR],
  ITYPE` store; the TSET guard also rejects the old raw `mov [RC], RB`
  `vmeta_tset` slot write.

M5 x64 TSETM array-store publication:

- Added `lj_tab_storetvn()` as a batch table-slot publication helper using
  `copyTVrel()` for each copied `TValue`.
- Updated x86-64 `BC_TSETM` so constructor vararg result copies keep the
  existing fit check, `lj_tab_reasize()` retry, and table barrier behavior, but
  publish the resulting array slots through `lj_tab_storetvn()` instead of a
  raw `mov [TMPR], ITYPE` loop.
- Follow-up: the x64 `TSETM` GC2 range barrier now calls
  `lj_gc2_barrier_tvn_pair_g()` with the destination table parent, so
  generational remembered-set filtering can queue the table instead of treating
  the copied values as parentless roots.
- This covers the resize/retry window where `lj_tab_reasize()` can publish a
  replacement legacy array before `BC_TSETM` retries and fills the slots. The
  original 06 section 6.3 `AHdr` RETIRING/FORWARD/CAS migration protocol and
  the original 07 store-barrier macro still remain the final target.

M5 x64 TSET black-table repair helper:

- Added `lj_gc_barrierback_tab_g()` as a VM-callable wrapper around the legacy
  table black-to-gray repair and routed x86-64 `TSETV`, `TSETB`, `TSETR`, and
  `TSETM` black-table branches through it.
- The x64 fast paths now repair the table before publishing, reload bytecode
  operands from `PC_*`, and retry the normal nil/metamethod/FORWARD/header
  checks instead of preserving transient slot registers across a C call.
- Removed the x64 VM's inline `barrierback` macro/list surgery and extended the
  TSET guard to require helper calls plus retry labels while rejecting the old
  macro use.
- Follow-up: x86-64 `TSETV`, `TSETB`, `TSETR`, and `TSETM` now publish through
  `lj_tab_storetv()`/`lj_tab_storetvn()` first and run post-store VM publication
  helpers only when `TGMARK` is active or the table is still black. Single-slot
  stores call `lj_gc_pubtabtv_vm()`; range stores call `lj_gc_pubtabtvn_vm()`,
  which preserves the previous `TSETM` table barrier.
- The helper path drives both the parent-aware GC2 barrier and the legacy
  incremental black-table repair from the stored slot snapshot. The x64 TSET
  tests now catch reintroducing pre-store `lj_gc_barrierback_tab_g()` repairs.
- Follow-up cleanup: retired the now-dead x64 `vm_gc2_barriertab` label after
  `TSETM` stopped falling through to it; the TSET notes document why we avoid reintroducing the
  stale VM-only table-barrier subroutine.

M5 JIT table-store NYI guard:

- Updated indexed-store recording so hash-based table stores (`HSTORE` paths)
  and array-based table stores (`ASTORE` paths) stop recording with the normal
  NYI-bytecode trace error before legacy direct slot-store IR is emitted.
- This is a conservative interim step for the current legacy `GCtab` runtime.
  It prevents generated traces from performing unsynchronized table-slot writes
  while the original 06 section 6.3 `NHdr`/`AHdr` RETIRING/FORWARD/CAS write
  protocol and the original 08 section 8.8 traced-store barrier/write protocol
  are still pending. The original report intended traced array stores to keep
  barrier coverage; the current M5 bridge deliberately demotes them until the
  trace codegen side can publish through the final protocol instead of raw
  stores.
- Added `tools/ci/m5_jit_hash_store_nyi.sh`, wired into the M5 aggregate, to
  assert that hot string-key and array table stores both remain interpreted.
- Updated the GC2 traverse scaffold's table-store JIT barrier check to assert
  the same interim no-trace behavior while still validating that the
  interpreter fallback enqueues the table barrier during marking. Traced
  upvalue barriers remain covered separately.
- A later M6 slice retargeted `tools/ci/m5_jit_hash_store_nyi.sh` to the current
  boundary: existing non-nil hash/array overwrites may trace through the helper
  bridge, while new-key and nil-slot stores still stay NYI. The GC2 traversal
  scaffold's non-weak JIT table-store case was retargeted the same way, while
  a later M8/M6 helper split retargeted weak table stores to trace through the
  P_WEAK-aware hash/array helpers too.

M5 table resize off-table hash rebuild:

- Updated `lj_tab_resize()` so a non-empty replacement hash vector is allocated
  and rebuilt off-table before `GCtab.node` is release-published. Old hash
  entries are copied from acquired key/value snapshots; entries that belong in
  the final array size are routed to the array, and the remaining pairs are
  inserted into the unpublished replacement hash.
- Array-tail values that leave the array during shrink are inserted into the
  unpublished replacement hash before the smaller `asize` is published. If an
  explicit resize asks for too small a hash part, `hbits` is raised from the
  counted live hash payload needed after array routing.
- Old hash-vector retirement is armed only after the rebuilt vector is
  published. This removes the previous window where readers could acquire a new
  empty or partially rebuilt hash vector while old pairs or array-tail values
  were still being reinserted.
- This is still an intermediate legacy-`GCtab` bridge, not the original 06
  section 6.3.5 cooperative `AHdr`/`NHdr` resize protocol. The original target
  remains RETIRING/FORWARD/CAS migration with helper copying; the current slice
  still uses legacy `GCtab` fields and non-cooperative one-thread copying.
- Extended the table publication/retirement gates to require rebuild-before-
  publish ordering, to cover a hash-resident integer key moving into the array
  part during resize, and to cover array-tail keys moving into the hash part on
  shrink.

M5 table rehash/new-key release publication:

- Added `tab_storekeyrel()` so hash keys are canonicalized for `-0` before the
  key slot is release-published, instead of release-storing the key and then
  patching `key.u64` directly.
- Converted resize/rehash value moves from old hash and array-tail storage to
  `copyTVrel()`. Some destinations are unpublished rebuild storage, but this
  keeps the current legacy-`GCtab` bridge uniform for slots that can become
  visible through array-size or node publication after the rebuild.
- This does not replace the original 06 section 6.3 RETIRING/FORWARD/CAS
  helper-copy resize protocol; it narrows the remaining raw-store surface while
  that protocol is still pending.

M5 x64 JIT HREFK node-header bounds:

- Added a runtime HREFK slot bounds guard in x86/x64 lowering: generated code
  now checks the recorded constant slot index against `TabNodeHdr.hmask` before
  loading `node[slot].key`. This prevents a trace from indexing past a newer,
  smaller node vector if the legacy `GCtab.hmask` mirror is stale around a
  resize publication.
- The existing HREFK key guard still verifies slot identity after the bounds
  check. Hash stores and the final generation-aware HREFK guard model remain
  part of the original 06/08 `NHdr` write/trace plan.
- Documented the codegen bounds guard requirement alongside recorder-time
  node/header snapshots; the HREFK behavior is covered by the JIT node-header
  cases.

M5 recorder template hmask snapshots:

- Updated the table-bump recorder path for template tables so both the grown
  table and template table derive hash sizes from the acquired node header
  instead of `GCtab.hmask`. Template hash scans now walk the same node/mask
  generation they acquired.
- Extended the HREFK recorder guard to reject stale template-table hash-mask
  snapshots in addition to the constant-slot HREFK shape snapshot checks.

M5 x64 JIT HREF node-header hmask:

- Updated x86/x64 regular `IR_HREF` lowering so dynamic hash lookups load the
  mask from `TabNodeHdr` at `node-sizeof(TabNodeHdr)` after the generated trace
  has loaded `GCtab.node` into the destination register. The backend no longer
  emits `offsetof(GCtab, hmask)` for regular HREF hash indexing.
- This closes the regular dynamic-HREF mixed `GCtab.node`/`GCtab.hmask` reader
  for the current headered-node representation. Constant-slot HREFK guards and
  hash stores still need the original 08 section 8.8 `NHdr`/generation-aware
  trace work and the 06 section 6.3 write protocol.
- Documented the `TabNodeHdr` displacement requirement and the obsolete
  `offsetof(GCtab, hmask)` x86 backend form; dynamic HREF behavior is covered
  by the JIT node-header cases.

M5 x64 table node-header hmask reads:

- Updated x86-64 read-only hash fast paths to load `GCtab.node` first and then
  load the hash mask from `TabNodeHdr` at `[node-8]`, rather than reading the
  legacy `GCtab.hmask` mirror. Covered paths are `getmetatable`'s
  `__metatable` probe, `ipairs_aux` empty-hash fallback check, `lj_vm_next`
  hash traversal, `BC_TGETS_Z`, and the hash traversal part of `BC_ITERN`.
- `BC_TSETS_Z` originally gained the same node-header mask for coherent
  string-key slot addressing, but a later M5 slice demoted it to `vmeta_tsets`
  so the x64 VM no longer performs direct legacy hash-slot stores.
- Tightened the existing x64 guards to require the `[node-8]` header loads and
  reject the old `TAB->hmask`/`NEXT_TAB->hmask` pairs.

M5 table node-header hmask slice:

- Added a small `TabNodeHdr` immediately before every table hash `Node[]`
  allocation, while keeping `GCtab.node` pointing at `Node[0]` for existing
  VM/JIT addressing. The header carries the mask for that exact node vector, so
  C readers can derive `(node, hmask)` from one acquired publication instead of
  pairing `GCtab.node` with a separately observed `GCtab.hmask`.
- Gave the shared empty hash fallback a real header by embedding
  `global_State.nilnodehdr` immediately before `global_State.nilnode`; empty
  tables still point at `&g->nilnode`, but `lj_tab_node_hmask_acq()` is now
  valid for every published table node pointer, including the nilnode.
- Updated table allocation, resize retirement, close-time free, GC/GC2 raw
  memory marking, bytecode/serializer/parser scans, table library scans,
  `lj_tab_next()`/key-index logic, and recorder-time HREFK shape snapshots to
  use the node-bound mask when walking a hash vector. `GCtab.hmask` remains as
  a compatibility mirror for current VM/JIT offsets.
- This is a deliberate step toward the original 06 section 6.2/6.3 `NHdr`
  design, not a replacement for it. The full header-generation helper-copy
  protocol, RETIRING/FORWARD/CAS writes, and x64 VM/JIT header-mask loads still
  remain pending.
- Added `tests/t-tab-nodehdr.c` and `tools/ci/m5_tab_nodehdr.sh`; tightened the
  empty-hash, node-publish, and retire tests to assert header-mask invariants.

M5 x64 TSET nil-decision snapshots:

- Updated `src/vm_x64.dasc` `BC_TSETV`, `BC_TSETS`, and `BC_TSETB` so previous
  slot values are loaded into `r8` before nil/metamethod decisions instead of
  comparing table slots directly in memory.
- This is a narrow x86-64 snapshot step for the current legacy `GCtab` paths.
  It does not implement the final 06 section 6.3 RETIRING/FORWARD/CAS write
  protocol for `AHdr`/`NHdr`; that original target remains pending.
- Added `tools/ci/m5_x64_tset_nil_snapshot.sh`, wired into the M5 aggregate,
  with interpreter array/string set smoke coverage. The required snapshot
  ordering is documented beside the x64 VM path.

M5 x64 getmetatable node/hmask ordering:

- Updated `src/vm_x64.dasc` `getmetatable` fast-function probing of
  `__metatable` so the metatable hash `GCtab.node` base is loaded into `r8`
  before `hmask` is loaded and the string-key index is computed.
- This gives the base-library metatable probe the same node-before-hmask order
  as the current C-side legacy `GCtab` readers. The final `NHdr` table-header
  protocol remains the original target.
- Added `tools/ci/m5_x64_getmetatable_node_order.sh`, wired into the M5
  aggregate, with runtime `getmetatable()` coverage. The node-before-hmask
  ordering rationale is documented beside the x64 VM path.

M5 x64 TGETS/TSETS node/hmask ordering:

- Updated `src/vm_x64.dasc` `BC_TGETS`/`BC_TSETS` string-key hash fast paths so
  they load the legacy `GCtab.node` base into `r8` before loading `hmask` and
  computing the hash index in `TMPR`.
- This mirrors the C-side node-acquire-then-hmask order for the current
  publish/retire `GCtab` table layout. It does not replace the final
  `NHdr`/cached-hmask trace and interpreter design in the original plan.
- Added `tools/ci/m5_x64_tgets_node_order.sh`, wired into the M5 aggregate,
  with interpreter string get/set smoke coverage for both fast paths. The
  node-before-hmask ordering rationale is documented beside the x64 VM path.

M5 x64 ipairs_aux slot snapshot:

- Updated `src/vm_x64.dasc` `ipairs_aux` so the array value is loaded into
  `r8` before the nil decision and the same snapshot is copied to the returned
  value slot.
- This matches the `BC_ITERN` array snapshot discipline for the current legacy
  `GCtab` array path. It is x86-64-only and leaves the final `AHdr`/`NHdr`
  array/iterator protocol unchanged in the original plan.
- Added `tools/ci/m5_x64_ipairs_snapshot.sh`, wired into the M5 aggregate, with
  an interpreter `ipairs()` smoke test. The single-snapshot requirement is
  documented beside the x64 VM helper.

M5 JIT HREFK recorder shape snapshot:

- Updated `src/lj_record.c` constant-key table lookup recording so the HREFK
  fast path snapshots the legacy `GCtab.node` pointer and `hmask` around
  `lj_tab_get()` and only derives a slot offset if the shape is unchanged.
  If a concurrent resize publication changes the shape while recording, the
  recorder falls back to regular `HREF`.
- The runtime HREFK key guard still protects the recorded slot; this change
  prevents the recorder from mixing an `oldv` returned from one node vector
  with a later node/hmask pair. This is interim legacy `GCtab` protection before
  the final `AHdr`/`NHdr` table-header recording path.
- Added `tools/ci/m5_jit_hrefk_record_snapshot.sh`, wired into the M5
  aggregate, with a constant-key JIT lookup smoke test. The recorder comment
  owns the node/hmask snapshot requirement.

M5 x64 JIT HREF node/hmask ordering:

- Updated `src/lj_asm_x86.h` regular `IR_HREF` lowering so nonzero hash lookups
  load `GCtab.node` into the destination register first, then compute the
  masked hash index in a separate scratch register before adding it to the node
  base.
- This prevents generated x64 trace code from pairing a newly published
  `hmask` with an older `node` pointer during the current legacy `GCtab`
  publish/retire phase. It is still an interim ordering fix before the final
  08 section 8.8 `AHdr`/`NHdr` table-header FLOAD path.
- Added `tools/ci/m5_jit_href_node_order.sh`, wired into the M5 aggregate, with
  a dynamic-key JIT hash lookup smoke test. The x64 lowering comment owns the
  node-before-hmask requirement.

M5 x64 BC_ITERN slot snapshots:

- Updated `src/vm_x64.dasc` `BC_ITERN`/`vm_IITERN` so array and hash candidate
  values are loaded into `r8` before the nil decision and the same snapshot is
  copied to the iterator result. Hash keys are copied through `r9` after the
  value snapshot passes the nil test.
- This mirrors the C-side `lj_tab_next()` snapshot discipline for the current
  legacy `GCtab` iterator path. It relies on x86-64 plain-load acquire behavior
  and does not touch aarch64/ARM64. The final 06 section 6.3.6 `AHdr`/`NHdr`
  iterator generation protocol remains pending.
- Added `tools/ci/m5_x64_itern_snapshot.sh`, wired into the M5 aggregate, with
  an interpreter `pairs()` smoke test. The array/hash snapshot requirement is
  documented beside the x64 VM iterator path.

M5 JIT table FLOAD mutability:

- Split the optimizer's legacy `HREF`/table-field fold rule so `HREF` keeps its
  existing CSE path, but `FLOAD TAB_ARRAY`, `TAB_NODE`, `TAB_ASIZE`, and
  `TAB_HMASK` now emit fresh loads instead of being CSE'd under the old
  "no corresponding stores" assumption.
- This matches the current intermediate legacy `GCtab` publication work:
  array/node vectors are release-published and retired, and asize/hmask can
  change on resize. It is not the final 08 section 8.8 `AHdr`/`NHdr`
  FLOAD-indirection design; that original target remains pending.
- Added `tools/ci/m5_jit_table_fload_mutable.sh`, wired into the M5 aggregate,
  with a JIT table-access smoke test and documented invariants rejecting the old
  `fload_tab_ah` rule for mutable table fields.
- Follow-up: TDUP template-table `TAB_ASIZE`/`TAB_HMASK` folds now snapshot the
  template array/node headers instead of reading `GCtab.asize`/`GCtab.hmask`
  mirrors directly; the FLOAD mutability notes document why we avoid the old direct reads.

M5 x64 vm_next hash-slot snapshot:

- Updated `src/vm_x64.dasc` `lj_vm_next` hash traversal so hash-slot values are
  loaded into a register before the nil decision instead of comparing the shared
  node field directly.
- This relies on x86-64's plain-load acquire behavior and does not touch
  aarch64/ARM64. It is still an interim legacy `GCtab` step before the final
  `AHdr`/`NHdr` iterator protocol.
- Added `tools/ci/m5_x64_table_next_snapshot.sh`, wired into the M5 aggregate,
  with a runtime `next`/`pairs` smoke test. The `lj_vm_next` snapshot
  requirement is documented beside the x64 VM helper.

M5 table array publication/retirement:

- Added `lj_tab_array_acq()` / `lj_tab_array_rel()` and
  `lj_tab_asize_acq()` / `lj_tab_asize_rel()` for C-side legacy `GCtab.array`
  access while keeping the original 06 `AHdr` target intact.
- Added `GCtab.acap` as the allocation-size truth for legacy arrays. Resize
  grows by allocating and initializing a fresh array, release-publishing the
  pointer, then release-publishing `asize`; shrink lowers `asize` without
  physically shrinking the current allocation, so stale larger-size readers do
  not pair with a too-small allocation.
- Added `TabArrayRetire` records under `g->tab.retired_arrays`; separated old
  arrays are armed with the safepoint epoch after unpublication and reclaimed by
  `lj_tab_reclaim_retired()` only after a later completed epoch. Colocated
  arrays are embedded in `GCtab` and are not retired separately.
- Converted C-side table array scanners/callers to acquire-load the array/size
  and snapshot `TValue` slots before nil/type/copy decisions. VM x64 fast paths
  still read the legacy fields directly; that remains a later x86-64 slice.
- Added `tests/t-tab-array-publish.c` and
  `tools/ci/m5_tab_array_publish.sh`, wired into the M5 aggregate, to pin the
  ordered publication and array-retirement behavior.

M5 table hash-node TValue snapshots:

- Added reusable `lj_tv_load_acq()` / `lj_tv_isnil_acq()` helpers for
  C-side hash-node key/value decisions.
- Converted table duplication, resize reinsertion, hash rehash counting,
  lookup/set key comparisons, insertion occupancy checks, key-index lookup,
  and `next()` hash traversal to use acquired `TValue` snapshots.
- Extended the same snapshot discipline to `table.maxn`, GC/GC2 table
  traversal, weak clearing, FFI finalizer-table scans, serialization,
  bytecode writing including deterministic key sorting, parser template-table
  fixup, and recorder traversal typing. Slot writes and returned value-slot
  addresses remain legacy `GCtab` behavior.
- Follow-up `table.maxn` cleanup now acquire-snapshots slots relooked up after
  a FORWARD value is observed, so both the original slot and the retry slot are
  tested from caller-owned `TValue` snapshots.
- Added `tests/t-tab-slot-snapshot.c` and
  `tools/ci/m5_tab_slot_snapshot.sh`, wired into the M5 aggregate, to pin the
  snapshot helpers and reject the old direct key/value decision patterns.

M5 table node-vector publication:

- Added `lj_tab_node_acq()`, `lj_tab_node_set()`, and `lj_tab_node_rel()`
  beside the existing ordered hash-chain helpers.
- `newhpart()` now initializes every hash node and establishes freetop/hmask
  before release-publishing `GCtab.node`; resize-to-empty likewise publishes
  `g->nilnode` only after the empty-hash metadata is in place.
- C-side table, GC/GC2, serialization, bytecode-writer, parser, and recorder
  readers now enter table hash vectors through `lj_tab_node_acq()` instead of
  raw `noderef(t->node)`. This is still the legacy `GCtab` layout, not the
  final NHdr generation protocol from the implementation plan.
- Added `tests/t-tab-node-publish.c` and `tools/ci/m5_tab_node_publish.sh`,
  wired into `tools/ci/m5_concurrent_objects.sh`, to exercise initialized
  publication and document raw runtime `GCtab.node` helper bypasses.

M5 table model gate:

- Copied the normative standalone concurrent table protocol model from
  ignored `plan/auxiliary/nbtab_model.c` into tracked `tests/t-nbtab-model.c`.
  Keeping a tracked copy matters because `plan/` is intentionally ignored in
  this workspace, while the CI wrapper must be reproducible from the committed
  tree.
- Added `tools/ci/m5_nbtab_model.sh` to build the model with a strict C-driver
  warning profile in `gnu11` mode. The C11 mode matters because the model uses
  `_Alignas(64)` for cache-line separated atomics.
- This is deliberately a pre-port gate. The next table slices can reshape
  `GCtab` / `Node` against a known-good model instead of mixing test import
  with runtime table changes.
- The tracked copy replaces the ignored model's raw `volatile` spin with
  repeated atomic loads so the repository invariant check remains green.

Verification:

- `tools/ci/m5_nbtab_model.sh`: passed.

M5 internal tag audit:

- Tried the straightforward section 6.1 approach of moving `LJ_TNUMX` /
  `LJ_TISNUM` down to make room for `LJ_TKEYLOCK` and `LJ_TFORWARD`.
- Reverted it after stock tests showed NaN comparisons failing. On this x64
  dual-number representation, the canonical hardware NaN pattern occupies the
  `~15u` itype range, so making `LJ_TISNUM == ~15u` causes `tvisnum(0/0)` to
  fail and comparison operators raise `"attempt to compare two number values"`.
- The table sentinel representation needs a design that preserves canonical
  NaN and integer tagging. Do not blindly lower `LJ_TISNUM` for M5 tables.

Verification after revert:

- `tools/ci/m5_nbtab_model.sh`: passed after the invariant check spin fix.
- `tools/ci/m0_invariant_audit.sh`: passed.
- `tools/ci/m0_matrix.sh`: passed after reverting the unsafe tag shift.

M5 NaN tag regression guard:

- Added `tests/t-itype-nan.c` to pin the hazard found during the internal tag
  audit:
  - `setnanV()` and `setnumV(0/0)` must remain `tvisnumber()` /
    `tvisnum()` and must not look like integers or GC references;
  - `lua_pushnumber(0/0)` / `lua_tonumber()` must preserve NaN as a Lua
    number;
  - Lua `0/0` and `tonumber("nan")` must report `type(...) == "number"` and
    compare unequal to themselves.
- Added `tools/ci/m5_itype_nan.sh` for the focused C/API guard and
  `tools/ci/m5_concurrent_objects.sh` as the current aggregate M5 entry point
  for the nbtab model, NaN tag guard, and M0 invariant checks.

Verification:

- `tools/ci/m5_concurrent_objects.sh`: passed.

Per-TG `math.random`:

- Switched interpreter `math.random()` and `math.randomseed()` from the shared
  math-library PRNG userdata to `L2TG(L)->prng`, so seeding is local to the
  calling OS-thread TG.
- Updated the JIT recorder for `math.random()` to call `lj_prng_u64d()` with
  `&J2TG(J)->prng`, keeping current single-thread JIT traces consistent with
  the interpreter path. Multi-threaded JIT execution remains M6 work.
- Kept the legacy hidden PRNG userdata as library-registration scaffolding for
  this slice; it is no longer the runtime state for math random.
- Added `tests/t-math-random-tg.lua` and `tools/ci/m5_math_random_tg.sh`.
  The regression seeds a parent and spawned child with the same seed and proves
  the child seed/advance does not perturb the parent's sequence.
- Wired the new gate into `tools/ci/m5_concurrent_objects.sh`.
- Regenerated `src/Makefile.dep` for the new `lib_math.c -> lj_tg.h`
  dependency. `make -C src depend` still prints the existing `lj_err.c`
  preprocessor warning, but exits 0 and updates the dependency file.

Verification:

- `tools/ci/m5_math_random_tg.sh`: passed.
- `tools/ci/m5_concurrent_objects.sh`: passed.
- `tools/ci/m0_matrix.sh`: passed.

Native-state no-argument PRNG reseed:

- Added `lj_prng_seed_secure_l(lua_State *, PRNGState *)` for Lua-facing
  secure reseeds and later removed the old no-`lua_State`
  `lj_prng_seed_secure(PRNGState *)` compatibility entry point. Bootstrap now
  calls the native-aware entry point with `NULL` before a `lua_State` exists.
- Wrapped Linux `getrandom(2)` and the POSIX `/dev/urandom` fallback in
  native-state regions. The fallback closes its file descriptor before
  delivering any pending STOPREQ.
- `math.randomseed()` without arguments now uses the L-aware reseed path; an
  explicit numeric seed still stays on the deterministic in-VM seed path.
- Added `tests/t-prng-seed-native.lua` and
  `tools/ci/m5_prng_seed_native.sh`, and wired the coverage into the M5 aggregate.

Verification:

- `tools/ci/m5_prng_seed_native.sh`: passed.

Release-published GC roots:

- Added `setgcrefroot()` for `GCRef` root slots. It accepts `GCobj *` values,
  stores the raw pointer with release ordering, and accepts null roots without
  relying on `setgcref()`'s `&(gc)->gch` form.
- Converted current `gcroot[]` and base-metatable publications to
  `setgcrefroot()` in meta init, C/API metatable assignment, string/FFI/io
  library setup, FFI finalizer table setup, and threading library roots.
- Converted C-side `gcroot[]` readers in legacy GC, GC2, FFI finalizer setup,
  and standard I/O root access to `gcref_acq()` snapshots, matching the
  release-published root contract.
- Added `tools/ci/m5_gcroot_publish.sh` and wired it into
  `tools/ci/m5_concurrent_objects.sh` as a invariant for reintroducing direct
  `setgcref()` publication or plain C-side reads for these root families.

Verification:

- `tools/ci/m5_gcroot_publish.sh`: passed.
- Normal `make -C src`: passed.

Release-published metatable pointers:

- Generalized the root publication primitive into `setgcrefrel()` and kept
  `setgcrefroot()` as the root-slot alias.
- Added `setgcrefmt()` for runtime `GCtab.metatable` / `GCudata.metatable`
  publication. The C API and base-library `setmetatable` paths now use release
  stores while preserving the existing write barriers.
- Left construction-time metatable initialization on plain `setgcref()`;
  those objects are still new and unpublished at the write point.
- Follow-up: serializer table decode and JIT snapshot restore no longer use
  that construction-time exception. Both can materialize tables while GC2
  allocation-black is active, so they now release-publish `GCtab.metatable` and
  run `lj_gc_pubtabobj()` for non-null metatables before the edge can be missed.
- Follow-up: userdata env/metatable constructors also no longer rely on the
  construction-time exception after allocation. `lj_udata_new()` release-publishes
  `GCudata.env`, library constructors release-publish `GCudata.metatable`, and
  both use object-pair barriers because GC2 allocation-black can make the new
  userdata marked before those edges are visible.
- Follow-up: `lj_state_new()` now applies the same rule to inherited thread
  environments, release-publishing `L1->env` and barrier-marking the environment
  before the new linked thread can be missed during GC2 allocation-black.
- Follow-up: `buffer.new()` now release-publishes `SBufExt` string/metatable
  dictionary refs and barriers those table edges after creating the buffer
  userdata, so dictionary tables cannot be missed if the buffer allocates black.
- Follow-up: weak self-metatable constructors used by `newproxy()` and FFI
  finalizer tables now release-publish `GCtab.metatable`, CAS-publish their
  `__mode` fields, and call `lj_gc_pubtab()` after installing `__mode`, so
  allocation-black GC2 cycles capture the weak mode instead of relying on the
  old new-white exception.
- Follow-up: `ffi.pin()` now release-publishes the hidden payload `TValue` and
  barriers the userdata-to-value edge before publishing `UDTYPE_FFI_PIN`, so GC2
  traversal cannot skip the payload after seeing an already-linked pin handle.
- Documented the x64 fast-function metatable store as a release store on TSO.
- The x64 base-library `setmetatable` fast path now calls
  `lj_gc2_barrier_obj_pair()` for the table -> metatable edge before its
  legacy black-table repair, matching the C API path's GC2 parent/child
  publication coverage for this non-nil metatable fast path.
- Follow-up: `lj_gc2_barrier_obj_pair()` is declared with `LJ_FUNCA` so
  amalgamated no-JIT builds still export the helper for the x64 VM call.
- Converted GC, GC2, metamethod dispatch, userdata/cdata finalizer setup,
  serializer dictionary/metatable handling, and threading-library environment
  readers to acquire-snapshot release-published metatable/env/SBuf dictionary
  refs before following them.
- Extended the same acquire-snapshot discipline to public C API, base, debug,
  buffer, and jit library setup, loader/library registration, closure
  inheritance, and JIT recorder metatable/env readers. Direct current-thread
  environment replacement through the API/base `setfenv(0)` paths now
  release-stores the new env ref.
- Added `tabref_acq()` and extended `tools/ci/m5_metatable_publish.sh` to catch
  raw `setgcref()` stores in the runtime C `setmetatable` paths and plain C
  reader loads for this shared metatable/env reader set. The guard now also
  requires the x64 fast path to publish the GC2 object-pair barrier before
  falling through to the legacy repair.

Verification:

- `tools/ci/m5_metatable_publish.sh`: passed.
- Normal `make -C src`: passed.
- `tools/ci/m5_concurrent_objects.sh`: passed.
- `tools/ci/m0_matrix.sh`: passed.

M5 userdata type publication:

- Added `lj_udata_udtype_acq()` / `lj_udata_udtype_rel()` for the shared
  `GCudata.udtype` discriminator.
- Regular userdata now release-publishes the default `UDTYPE_USERDATA` before
  the userdata-list link is made visible. Specialized userdata publishes its
  final type only after the payload/metatable state needed by GC and runtime type
  checks is initialized: buffer `SBufExt` and dictionaries, IO file state, FFI
  CLIB cache/metatable, threading thread/mutex/channel payloads, and
  `lj_chan_init()`.
- Converted C-side GC, GC2, API/library, FFI conversion/recording, and
  threading readers to acquire-snapshot the type before branching. Existing IR
  `UDATA_UDTYPE` loads remain trace guards, not C-side shared-field reads.
- Added `m5_udtype_publish` coverage. Raw `udtype` reads/writes outside the
  helpers and the constructor ordering rule are documented here and covered by
  the behavior case.

Verification:

- `tools/ci/m5_udtype_publish.sh`: passed.
- `tools/ci/m5_concurrent_objects.sh`: passed.

M5 CType name publication:

- Added `ctype_name_acq()` for acquire-snapshotting `CType.name` and changed
  `ctype_setname()` to release-publish fixed name strings.
- Converted FFI name hash/lookup, field lookup, type representation, parser
  diagnostics/assertions, C library redirection, conversion, recording, and
  `ffi.typeinfo()` readers to snapshot `CType.name` through the helper before
  comparing or following the string.
- This is deliberately narrower than the original FFI concurrency plan: it
  covers only the published `CType.name` reference. `CTState.hash`,
  `CTState.top`, `CType.next`, and ctype table growth are still legacy shared
  state and remain on the later CTState concurrency plan.
- Added `tools/ci/m5_ctype_name_publish.sh`, wired into the M5 aggregate, to
  document raw `CType.name` `gcref()` reads/writes in the converted FFI files and
  run a typedef/tag/metatype smoke test.

Verification:

- `tools/ci/m5_ctype_name_publish.sh`: passed.
- `tools/ci/m5_concurrent_objects.sh`: passed.

M5 JIT trace-slot publication:

- Added C-side trace registry helpers so `traceref()` acquire-loads the trace
  slot and treats the in-flight sentinel as `NULL`. Trace-slot stores now use
  release helpers for pending, final publication, and clearing.
- Added `TraceVec`, an RCU-published trace-vector header that pairs `sizetrace`
  with the slot array consumed by `traceref()`. Growth now allocate-copies a new
  vector, release-publishes it through `J->tracev`, keeps `J->trace/sizetrace`
  as token-held mirrors, retires the old vector with `g->gc2.hs_epoch`, and
  drains retired vectors from the existing safepoint SMR hook.
- In-flight traces no longer publish `&J->cur` through `J->trace[]`; the slot
  carries `LJ_TRACE_PENDING` until `trace_save()` publishes the compact final
  `GCtrace`.
- Reordered `trace_stop()` so mcode is committed and the final trace slot is
  release-published before root bytecode, side-exit patching, or stitching links
  are enabled. Root bytecode patches in this path use a full-word release store.
- Moved bytecode publication helpers into `lj_bc.h`: `bc_publish()`,
  `bc_publish_op()`, and `bc_publish_d()` compose full instruction words before
  release-storing them. `lj_trace_reenableproto()`, `trace_unpatch()`,
  blacklist/lazy-NOJIT patching, pending patch restore/unpatch, recorder TNEW
  operand retuning, recorder JFUNC temporary unpatching, and `bc_cfunc_ext`
  wrapping now use those helpers instead of narrow bytecode stores.
- The x64 `BC_ISNEXT` despecialization path now calls VM-visible
  `lj_bc_publish_*_vm()` wrappers for the current `ISNEXT` word, the target
  `ITERN` word, and traced `JLOOP` unpatches. The M5 trace-publication guard
  rejects direct `PC` bytecode stores in `vm_x64.dasc`.
- Adjusted x86/x64 tail assembly so self-linked traces use the current
  assembler trace object directly instead of reading the pending trace-table
  sentinel.
- Added acquire/release helpers for the shared 16-bit trace-number links:
  `pt->trace`, `T->link`, `T->nextroot`, and `T->nextside`. GC/GC2 traversal,
  trace flush, bytecode writing, `jit.util.traceinfo()`, side-chain publish,
  and stitch/blacklist updates now snapshot or publish these fields through the
  helpers.
- Added x64 per-trace exit target tables. Guard exits now branch to trace-local
  rows that preserve registers, load `T->exittab[exitno]`, and transfer through
  that target. Slots initially point at the legacy interpreter exit stubs; side
  trace attachment release-stores the child `mcode` into the parent slot after
  the child trace has been published. `trace_stop()` no longer calls
  `lj_asm_patchexit()` on the supported x64 path.
- Flush paths reset x64 exittab slots back to the interpreter exit stubs before
  clearing trace slots or unlinking a root trace. This matches the original
  flush protocol's data-retargeting step.
- `lj_mcode_free()` now detaches active mcode areas and records each area in a
  heap-side `MCodeRetire` node tagged with the current safepoint epoch.
  Original bridge state: the completed-handshake hook called
  `lj_mcode_reclaim_retired()` directly after the two-epoch `LJ_FLUSH_EPOCHS`
  margin. The current M3 SMR bridge routes that same call through
  `lj_gc2_reclaim_retired()`, so `jit.flush()` still does not immediately
  unmap executable pages and GC2 accounts the reclaimed work.
- `J->retiredmcode` head operations now go through shared
  `mcode_retired_head_*()` helpers. Mcode retirement push uses helper CAS,
  reclaim/shutdown uses helper xchg, and mark plus GC2 paranoia scans use
  helper acquire loads. The M5/M6 JIT publication guards document raw retired
  mcode head access in the implementation, scanner, and focused fixture.
- Dead `GCtrace` bodies now carry `retire_epoch`/`retired_next` in the body and
  move to `J->retiredtraces` instead of freeing `exittab` storage immediately.
  Retirement marks the body/exittab as raw memory right away, future GC/GC2 raw
  root scans keep them marked, and `lj_trace_reclaim_retired()` frees them after
  the same two-epoch flush margin. Shutdown sweeps still free immediately because
  no mutator can run traces during `lua_close()`.
- `J->retiredtracev` and `J->retiredtraces` head operations now go through
  shared `tracevec_retired_head_*()` and `trace_retired_head_*()` helpers.
  Retirement push uses helper CAS, reclaim/shutdown use helper xchg, and mark
  and GC2 paranoia scans use helper acquire loads. `m5_jit_trace_publish.sh`
  documents why raw retired trace/vector head access in the runtime, scanner, and
  focused fixtures.
- The assembler retry path now frees unpublished `J->curfinal` scratch bodies
  immediately through `lj_trace_free_unpublished()`. These bodies are allocated
  before `trace_save()` publishes a valid `startpt`, so retiring them could put
  uninitialized prototype roots on `J->retiredtraces` for later GC marking.
- Public full-flush entrypoints now call `lj_trace_flushall_hs()`, which routes
  through `HS_FLUSHJ|HS_EXIT_TRACES` before the leader clears trace slots.
  Acknowledgers only apply per-thread exit/abort actions; the leader calls
  `lj_trace_flushall(mainthread(g))` after `hs_pending` drains.
- Public function/proto/trace flush modes still run their existing scoped
  retarget/unpatch logic. Previous bridge state: they always published an
  `HS_EXIT_TRACES` boundary after the request, including already-flushed or
  otherwise missing scopes. Current bridge: `trace_flushroot()`,
  `lj_trace_flush()`, and `lj_trace_flushproto()` report scoped trace-exit
  publication work, and proto disabling counts as boundary work even without
  existing traces. `luaJIT_setmode()` publishes one `HS_EXIT_TRACES` boundary
  only when that work count is nonzero. The remaining original target is to give
  scoped flushes full per-root retirement/token structure, not to expand them
  into a whole-JIT `HS_FLUSHJ`.
- Scoped flush now also performs the first per-root publication retirement step.
  Previous bridge state: after the scoped `HS_EXIT_TRACES` boundary, the
  unlinked root could remain visible through `J->trace` until a later GC sweep
  called `lj_trace_free()`. Current bridge: scoped roots are tagged with
  `LJ_TRACE_SCOPE_FLUSHING`; after the handshake completes,
  `lj_trace_flushscope_retire()` clears side-trace slots rooted at tagged roots
  first, then clears the tagged root slots. It sets `T->traceno = 0` before
  `traceslot_clear()` and updates `J->freetrace`, so a later GC sweep cannot
  clear a reused slot and stale sides cannot point at a reused root number. This
  makes trace numbers reusable immediately while preserving the existing GC/SMR
  body retirement path. `jit_scoped_slots_retired` exposes the slot-retirement
  count.
- This is narrower than the original §8.3/§8.4 target: `J->trace` vector growth
  now uses the `TraceVec` RCU bridge, but non-x64 backends still carry their old
  patcher implementations, and parser-local bytecode fixups remain
  pre-publication writes rather than shared runtime patching.
- Added `tools/ci/m5_jit_trace_publish.sh`, wired into the M5 aggregate, to
  document raw trace-slot GCRef stores and shared trace-number field accesses,
  document raw live bytecode patching in the trace/record/dispatch paths, and to
  smoke root trace publish/flush, recursive self-link traces, side-exit trace
  attachment through the x64 exit table, side-trace flush reset, and trace-vector
  growth/epoch retirement. The guard also builds `tests/t-jit-mcode-retire.c`
  and `tests/t-jit-trace-retire.c` to assert same/next-epoch retention and
  two-epoch mcode/trace-body reclamation. It now also rejects unconditional
  scoped `HS_EXIT_TRACES` publication from `lj_dispatch.c`, requires both the
  count-aware scoped flush helper and `lj_gc_pubtrace()` for trace publication,
  and preserves the disable-proto boundary and the post-handshake scoped
  slot-retirement bridge.

Verification:

- `tools/ci/m5_jit_trace_publish.sh`: passed.
- `tools/ci/m5_concurrent_objects.sh`: passed.

Release-published direct registry root:

- Added `copyTVrel()` for release-ordered `TValue` publication.
- Added `lj_gc_pubroot()` for stores to global root slots without a heap object
  owner. It routes through the GC2 TV barrier and preserves the legacy
  incremental mark invariant for propagate/atomic phases. This helper was
  renamed from the earlier legacy-named `lj_gc_barrierroot()` surface so
  root publication follows the same `lj_gc_pub*` convention as heap-owner
  publication wrappers.
- Updated `copy_slot()` so direct `LUA_REGISTRYINDEX` replacement uses
  `lj_gc_pubroot()` plus `copyTVrel()`. Ordinary registry table entries
  still go through table stores; they remain part of the table-port work.
- Added `tests/t-registry-root.c` and `tools/ci/m5_registry_root.sh`. The test
  replaces the registry via both `lua_replace()` and `lua_copy()`, drops the
  source stack value, runs a full GC, and verifies the replacement table is
  still rooted.

Verification:

- `tools/ci/m5_registry_root.sh`: passed.
- Normal `make -C src`: passed.
- `tools/ci/m5_concurrent_objects.sh`: passed.
- `tools/ci/m0_matrix.sh`: passed.

Metatable `nomm` policy:

- Removed runtime miss-cache writes from `lj_meta_cache()` and the table
  `__len` miss path. Metamethod misses no longer set new `nomm` bits.
- C API and base-library `setmetatable` now clear the installed metatable's
  `nomm` field, so metatable fast paths degrade to explicit method lookup
  after installation.
- The x64 `setmetatable` fast path and JIT recorder now clear the installed
  metatable's `nomm` before publishing/storing the metatable edge.
- Kept construction/fixed-setup `nomm` initialization and explicit table-store
  clears. The full table-port work still owns table cell atomicity.
- Added `tests/t-nomm-cache.c` and `tools/ci/m5_nomm_cache.sh`.
- C-source `nomm` readers/writers now route through `lj_tab_nomm_acq()` /
  `lj_tab_nomm_rel()`, and the notes document why we avoid direct `->nomm` accesses outside
  the x64 VM fast path.

Verification:

- `tools/ci/m5_nomm_cache.sh`: passed.
- `tools/ci/m5_concurrent_objects.sh`: passed.
- `tools/ci/m0_matrix.sh`: passed.

Lua metamethod snapshot bridge:

- Added `lj_meta_cachetv()` / `lj_meta_lookuptv()` and `lj_ctype_metatv()`
  helpers so Lua and ctype metamethod lookup callers can acquire-snapshot
  metatable slots into caller-owned `TValue` storage before tag checks, calls,
  copies, weak-mode decisions, finalizer registration, or recorder metadata
  caching.
- Converted core `lj_meta.c` VM helpers, base/buffer/string `__tostring` and
  `__pairs` paths, userdata/ctype finalizer lookup, weak-table `__mode`
  traversal, and recorder metamethod table peeks to use snapshots.
- C-side metatable pointer readers, including the public API and recorder
  boundary paths, now acquire-snapshot the release-published metatable ref before
  those slot snapshots are taken; the original report allowed relaxed metatable
  loads, but the current bridge keeps the release/read contract explicit while
  the full table-generation protocol is still pending.
- Added `tools/ci/m5_meta_snapshot.sh` and wired it into the M5 aggregate to
  keep Lua metamethod users off raw `lj_meta_lookup()` / `lj_meta_fast()`
  result pointers and keep ctype users off raw `lj_ctype_meta()` result
  pointers.
- The guard now explicitly treats `lj_meta_cache()` / `lj_meta_lookup()` /
  `lj_meta_fast*()` and `lj_ctype_meta()` as declaration/definition-only raw
  helpers for production source; callers must use the snapshot-returning
  wrapper APIs.

OS reentrant path guard:

- Added `tools/ci/m5_os_reentrant.sh` to pin the POSIX `os.date` path to
  `gmtime_r` / `localtime_r` and the POSIX `os.tmpname` path to `mkstemp`.
- Added `tests/t-os-reentrant.lua` to exercise `os.date` and `os.tmpname`
  from overlapped child TGs now that the later string-table and per-TG
  allocator slices make concurrent string creation viable.
- Blocked mutating `os.setlocale()` calls after threading activation while
  preserving locale queries, avoiding process-global locale changes once
  secondary TGs may run.
- Mutating `os.setlocale()` now claims `mt_gc_exclusive`, rechecks the
  one-way activation/live-thread state, and wakes waiting spawn/attach entrants
  after the C library mutation. This preserves stock single-threaded mutation
  while closing the pre-activation attach window for process-global locale
  state.
- Wired the coverage into `tools/ci/m5_concurrent_objects.sh`.

Verification:

- `tools/ci/m5_os_reentrant.sh`: passed.

M5 libc error-string reentrancy:

- Added `lj_err_strerrno()` as the shared runtime helper for OS error strings.
  POSIX builds use `strerror_r()` into caller-owned storage; non-POSIX builds
  keep the legacy fallback outside the current x86-64 Linux target.
- Routed `luaL_fileresult()`, `luaL_loadfilex()`, `io.open()` error formatting,
  and pthread creation errors through the helper, snapshotting errno/status
  before safepoint checks or Lua stack work.
- Added `tools/ci/m5_libc_error_reentrant.sh` and
  `tests/t-libc-error-reentrant.lua`, wired into the M5 aggregate, to document raw
  production `strerror()` and stress missing-file error paths from child TGs.

M5 lua_State ownership guard slice:
- Added lj_state_tryclaim/dropclaim so callers can distinguish already-owned states from temporary claims and avoid releasing ownership they did not acquire.
- Guarded lua_xmove with claims on both source and target states; stack growth errors release temporary claims before raising.
- Guarded coroutine.status when inspecting a foreign coroutine, returning thread busy if another OS thread owns it.
- Added tests/t-state-owner.c and tools/ci/m5_state_owner.sh; wired it into the M5 aggregate.
- Remaining §6.7 work per parallel audit: debug coroutine APIs, getfenv/setfenv on thread objects, resume/wrap/lua_resume ownership transfer, lua_status C API, and profiler dump/callback paths.
- Verification: tools/ci/m5_state_owner.sh and tools/ci/m5_concurrent_objects.sh passed.

M5 lua_State C API owner follow-up:
- Extended the owner guard to lua_status and thread-object lua_getfenv/lua_setfenv.
- Thread env set keeps the GC barrier before releasing a temporary owner claim while reusing the existing barrier call site, so the M0 legacy-barrier count stays flat.
- Extended t-state-owner and m5_state_owner static checks for busy and unowned status/env cases.
- Verification: tools/ci/m5_state_owner.sh && tools/ci/m5_concurrent_objects.sh passed sequentially. A parallel attempt was invalid because both scripts run make clean and raced.

M5 lua_resume owner guard:
- lua_resume now claims the resumed state for the duration of lj_vm_resume and releases temporary ownership before returning.
- Extended t-state-owner to resume an unowned coroutine through the VM and verify the owner word returns to 0, plus a busy-state lua_resume error case.
- m5_state_owner has a documented invariant for the claim/drop path around lj_vm_resume.
- At this point the x64 coroutine.resume/wrap fast path still needed VM-side claim/release around argument/result moves.
- Verification: tools/ci/m5_state_owner.sh and tools/ci/m5_concurrent_objects.sh passed.

M5 x64 coroutine fast-path owner guard:
- Added an assembler-facing lj_ffh_coroutine_claim helper that claims the target coroutine before the x64 coroutine.resume/wrap fast path reads co->cframe/status/top/base/maxstack.
- The x64 fast path carries the coroutine pointer with a low-bit temporary-claim tag, strips it before calling lj_vm_resume or touching coroutine fields, and releases the claim before normal returns or fallback.
- The helper call preserves both fast-function inputs: BASE in the callee-saved PC register and NARGS in TMP1 until the tagged coroutine result is known. This matters for Lua bytecode callers because L->top is not a valid source of truth for recomputing NARGS.
- coroutine.wrap error propagation passes the tagged coroutine to lj_ffh_coroutine_wrap_err so the copied error remains valid and temporary ownership is released before raising on the caller state.
- The C fallback ffh_resume also claims the target coroutine and reports "thread busy" for foreign-owned states. On coroutine stack-growth errors, it roots the coroutine's error string on the caller stack before dropping a temporary claim.
- Extended t-state-owner with Lua-level coroutine.resume/coroutine.wrap success cases from unowned coroutines and busy-owner error cases.
- m5_state_owner now documents the fallback claim, the claim-tag helper, x64 fast-path claim/release sequence, and NARGS save/restore. ARM64/aarch64 remains intentionally out of scope for this pass.
- Verification: tools/ci/m5_state_owner.sh, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 debug coroutine owner guard:
- Added owner claims to the public debug C APIs that inspect or mutate a lua_State stack: lua_getstack, lua_getinfo, lua_getlocal, and lua_setlocal.
- Added debug-library claims around composite coroutine operations in debug.getinfo, debug.getlocal, and debug.setlocal so the frame lookup and value transfer happen against one owned suspended state.
- Reworked luaL_traceback to claim the inspected coroutine only while reading stack frames and the pushed function slot, then release before allocating traceback text on the caller stack. debug.traceback now relies on this C API guard instead of holding a broad claim across traceback construction.
- Extended t-state-owner with suspended-coroutine debug.getinfo/getlocal/setlocal/traceback smoke coverage plus busy-owner C API and Lua debug-library errors.
- Regenerated Makefile.dep for the new lj_thr.h / lj_tg.h debug dependencies; make depend still prints the existing lj_err.c build-system warning but exits 0.
- Verification: tools/ci/m5_state_owner.sh, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 profiler lua_State owner guard:
- luaJIT_profile_dumpstack now claims the inspected state before compact debug-stack dumping, so jit.profile.dumpstack(thread, ...) reports "thread busy" instead of walking a foreign-owned coroutine.
- jit.profile's hidden callback coroutine is claimed while the callback frame is assembled and run through lua_pcall.
- Extended t-state-owner with busy-owner coverage for both the public C profile dumpstack API and Lua jit.profile.dumpstack.
- m5_state_owner now statically checks the profile dumpstack claim and the callback coroutine claim.
- Regenerated Makefile.dep for the new profiler/JIT lj_thr.h and error-state dependencies; make depend still prints the existing lj_err.c build-system warning but exits 0.
- Verification: tools/ci/m5_state_owner.sh, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.
- Follow-up owner-wait slice: `lj_state_claim()`, `lj_state_tryclaim()`, and
  `lj_state_gcscan_claim()` now yield through a no-L native sleep helper when
  they observe the temporary `LJ_THREAD_GCSCAN` owner sentinel instead of
  spinning on `la_cpu_pause()`. This preserves the owner-claim semantics while
  preventing a preempted GC stack scanner from making peer owner operations burn
  CPU. The M5 state-owner gate documents why raw pauses in `lj_thr.c`.
- Follow-up: the pthread profiler timer shutdown flag now uses
  `la_store32_rel()`/`la_load32_acq()` instead of raw `ps->abort` accesses. The
  state-owner gate documents why raw profile abort tests or assignments so the timer
  thread boundary stays in the shared-memory model.
- Follow-up: pthread-backed `jit.profile.stop()` now marks the
  `pthread_join()` timer shutdown wait as native and defers STOPREQ delivery
  until after profiler state and the Lua callback registry anchors are cleared.
  The `m5_profile_stop_native` guard forces STOPREQ during the join and verifies
  a second profile start/stop still works after clearing the shutdown flag.
- Follow-up: `jit_profile_callback()` now drops a profiler sample when the
  hidden callback coroutine is owned by another thread instead of exiting the
  process. `t-profile-stop-native` marks that hidden coroutine with a synthetic
  peer owner while samples are generated, and `m5_profile_stop_native` rejects
  reintroducing `exit()` as the failed-claim action. This preserves profiler
  safety for racy callback-state ownership.
- Follow-up: actual Lua errors raised by the profiler callback are now
  contained too. The callback error path clears the hidden callback coroutine
  stack, drops the temporary owner claim, stops profiling, clears the registry
  anchors, and returns instead of calling panic/exit. The same
  `m5_profile_stop_native` gate rejects reintroducing an `exit()` inside
  `jit_profile_callback()`.
- Follow-up read-side cleanup acquire-snapshots the hidden profiler callback
  function registry slot before checking or calling it. The profiler
  STOPREQ/callback behavior fixture owns the observable contract.

M5 per-TG tmpbuf guard:
- Added tools/ci/m5_tmpbuf_tg.sh to pin the M5 buffer invariant: lj_buf_tmp_ routes through L2TG(L)->tmpbuf, concat and formatted-push helpers use that route, and ordinary runtime code does not reach for g->tmpbuf directly.
- Switched the POSIX os.date formatting buffer path from direct &L2TG(L)->tmpbuf setup to lj_buf_tmp_(L), matching the rest of the runtime string-format helpers.
- Wired the coverage into tools/ci/m5_concurrent_objects.sh.
- Verification: tools/ci/m5_tmpbuf_tg.sh, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 string.buffer publication:

- Added acquire/release SBuf pointer helpers for the shared `string.buffer`
  surface. Public buffer methods, string formatting of buffers, concatenation,
  `load(buffer)`, serializer encode/decode/peek, FFI buffer-to-pointer
  conversion, and GC/GC2 buffer-memory marking now snapshot published buffer
  pointers through the helpers instead of following raw `SBufExt` fields.
- Buffer write/advance paths publish `b/w/e/r` updates through release stores
  so another OS thread that receives the buffer object can safely acquire a
  coherent pointer/length snapshot. This keeps the documented single-owner
  semantics for unsynchronized mutation while making accidental sharing
  memory-safe per plan §6.6.
- Fixed an accessor-conversion regression in `lj_bufx_set_borrow()`: borrowed
  extended buffers must start with `w == b`, not the borrowed tmp buffer's
  previous write pointer. The new cross-thread buffer test caught this as stale
  bytes prepended to `buffer.encode()` after earlier temporary-buffer use.
- Follow-up option-table cleanup acquire-snapshots the `dict` and `metatable`
  slots consumed by `buffer.new()`, so dictionary roots are prepared and
  published from a coherent table-slot value after `lj_tab_getstr()` returns.
- Added `tests/t-buffer-thread-safety.lua` and `tools/ci/m5_buffer_publish.sh`.
  The notes describe accessor usage on shared buffer entry points and runs
  cross-thread buffer read/write, `load(buffer)`, format/concat, and
  encode/decode coverage with JIT off and on. It is wired into the M5
  aggregate.

Verification:

- `tools/ci/m5_buffer_publish.sh`: passed.
- `tools/ci/m5_concurrent_objects.sh`: passed.
- `tools/ci/m5_tmpbuf_tg.sh`: passed.
- `tools/ci/m0_invariant_audit.sh`: passed.
- Stock cleanup suite through `tests/stock/test/test.lua`: 508 passed.

M5 threading publication wrapper slice:
- Added M5 publication wrappers in lj_gc.h that preserve the current GC2 +
  legacy incremental behavior while naming the publication boundary at each
  cross-thread/GC-visible store.
- Converted lib_threading publications to the new wrappers: threading
  environment stores, the then-current live-table stores,
  lua_State->mt_thread publication, channel payload publication, and
  main-thread userdata publication.
- `threading.current()` now publishes the lazily-created main-thread userdata
  into the threading module environment through a CAS/retry string-key helper,
  so a forwarded `__main` slot is re-resolved before the existing parent-table
  publish runs.
- lua_State->mt_thread now uses setgcrefrel before the publication barrier.
- Added tools/ci/m5_threading_publish.sh and wired it into the M5 aggregate. The legacy barrier detector count dropped from 39 to 33.
- Verification: tools/ci/m5_threading_publish.sh, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 channel payload atomic slots:
- Replaced `lj_chan.c` plain `slot->tv` struct assignment/load/clear paths
  with `chan_storetv_rel()`, `chan_loadtv_acq()`, and
  `chan_cleartv_rel()`. This keeps channel payload slots within the
  shared-`TValue` atomic access rule while preserving the existing per-slot
  sequence release/acquire edge.
- Added GC-aware receive helpers for Lua-facing channel receives:
  `lj_chan_recv_timeout_gc()` applies a root barrier to the received value
  before clearing the shared channel slot, so the previous channel root is not
  lost before the result is copied onto the Lua stack.
- Updated legacy GC and GC2 channel userdata traversal to acquire-snapshot
  each slot into a local `TValue` before marking it, rather than marking the
  shared slot address directly.
- Documented those helpers and the obsolete plain `slot->tv` payload-copy,
  non-GC-aware Lua receive, and direct shared-channel-slot marking forms.
  Threading publication behavior covers the observable contract.
- Added acquire-side reads for the `lua_State.mt_thread` userdata link in
  join/attach lookup and legacy GC/GC2 thread traversal, matching the
  existing `setgcrefrel()` publication in `threading_state_set_ud()`.
- Added atomic `GCSize` helpers for `g->gc.threshold` and the saved
  `g->mt_gc_threshold`, then routed the M4 active-child GC pause,
  `lua_gc(stop/restart/isrunning/step)`, finalizer threshold suppression, and
  C-side GC checks through those helpers. `lua_gc(stop/restart)` now replays
  the requested logical threshold to the real threshold if the last child exits
  during the saved-threshold update.
- Changed lib_threading reads of the release-published `GCROOT_THREADING_ENV`
  root to `gcref_acq()`, matching the M5 publication contract used for
  `lua_State.mt_thread`.
- Follow-up threading lookup cleanup acquire-snapshots `_LOADED`, module,
  `spawn`, and cached `__main` userdata slots before using their tags or
  object pointers, keeping the environment discovery path on coherent
  `lj_tab_getstr()` results.

M5 native live-thread registry bridge:
- Replaced the original report's shared `GCROOT_THREADING`/`__threads` table
  with `global_State.threading_live`, a CAS-prepended native list of
  `LJThreadLive` nodes.
- Each node release-publishes a `GCRef ud`; join and shutdown tombstone the
  node with a release NULL store instead of mutating a shared `GCtab`.
- Legacy GC and GC2 now acquire-scan the native list and mark non-tombstoned
  `UDTYPE_THREAD` userdata, preserving the child-state/result rooting contract.
- Thread userdata now release-publishes `LJThread->L` and barriers the
  userdata-to-child-state edge before release-publishing `UDTYPE_THREAD`; GC
  traversal acquire-loads the child pointer before marking the child stack.
- Spawn allocates the live node while the handle is still stack-rooted, but
  publishes it only after claiming `mt_live`, avoiding a shutdown wait on a
  pthread that was never created.

M5 jit.profile publication wrapper slice:
- Converted jit.profile registry anchor stores from direct legacy table barriers to
  release-copy stores plus `lj_gc_pubtab`; later moved those registry
  anchor/clear writes onto the table CAS retry bridge.
- Profile start now CAS-publishes the hidden profiler coroutine and callback
  function entries; profile stop CAS-publishes the registry clears through the
  same helper.
- Added tools/ci/m5_jit_profile_publish.sh and wired it into the M5 aggregate. The legacy barrier detector count dropped from 33 to 31.
- Verification: tools/ci/m5_jit_profile_publish.sh, tools/ci/m5_concurrent_objects.sh, tools/ci/m0_invariant_audit.sh, and tools/ci/m0_matrix.sh passed.

M5 runtime publication wrapper slice:
- Added lj_gc_pubtabobj for table metatable/object publications so table-object barriers keep their exact legacy and GC2 behavior while leaving the old macro namespace.
- Converted common runtime publication stores in base/table/metatable/API/buffer/library/FFI/cdata/clib/callback paths to M5 publication wrappers, using setgcrefrel or copyTVrel where the stored reference or TValue is visible through shared runtime objects.
- Left the remaining seven legacy non-lj_gc.c barrier call sites for the dedicated upvalue/cell, parser/proto, and table-internal work: copy_slot C upvalues, lua_upvaluejoin, lua_setupvalue, debug.upvaluejoin, lj_tab_newkey, and parser constant/template-table paths.
- Added tools/ci/m5_runtime_publish.sh and wired it into the M5 aggregate. The legacy barrier detector count dropped from 31 to 7.
- Verification: tools/ci/m5_runtime_publish.sh, make -C src -j after clean, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 publication barrier snapshot bridge:
- Changed GC2 `TValue` barrier entry points and the M5 `lj_gc_pub*tv`
  wrappers to acquire-snapshot the supplied `TValue` before GC2 marking,
  `tviswhite()`, or `gcV()` checks. This covers callers that release-copy into
  a shared destination slot and then pass that destination to the publication
  wrapper.
- Documented that publication wrappers must snapshot the supplied `TValue`
  instead of inspecting the caller's `TValue *` directly.

M5 upvalue publication wrapper slice:
- Converted C closure pseudo-index upvalue stores, lua_upvaluejoin, lua_setupvalue, and debug.upvaluejoin from legacy object barriers to release publication stores plus M5 object wrappers.
- Upvalue pointer joins now publish uvptr refs with setgcrefrel before the matching lj_gc_pubobjobj barrier; upvalue TValue writes use copyTVrel before lj_gc_pubobjtv.
- Added tools/ci/m5_upvalue_publish.sh and wired it into the M5 aggregate. The legacy barrier detector count dropped from 7 to 3.
- Verification: tools/ci/m5_upvalue_publish.sh, make -C src -j after clean, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 upvalue GC snapshot slice:
- Changed legacy GC and GC2 traversal of `GCupval` payloads and C-closure
  upvalue arrays to acquire-snapshot each `TValue` into a local before
  marking, matching the table/channel shared-slot pattern.
- Changed `lj_gc_closeuv()` to release-copy the stack slot into `uv->tv`
  before repointing `uv->v` at the closed storage.
- Documented the close-time release-copy rule near the helper layer. GC marking
  fixtures cover shared upvalue payload snapshots, and the comment explains why
  the helper boundary exists.
- Changed `lj_gc_pubuv()` and close-time gray repair to run GC2/legacy barrier
  decisions from one acquired snapshot of the closed upvalue payload, avoiding
  direct tag/payload inspection of a shared cell after publication. The GC2
  side now derives the owning `GCupval` and uses the parent-aware value barrier
  for remembered-set filtering.
- Changed the `io.lines()` C-closure iterator option copy from a bulk
  `memcpy()` of `fn->c.upvalue[]` to per-slot acquire snapshots before pushing
  the options onto the calling stack.
- C closure construction no longer relies on new-white allocation for hidden
  edges: `lj_func_newC()` release-publishes `fn->c.env`, C API and library
  constructors release-copy each `fn->c.upvalue[]`, and each edge is barriered
  before the closure can lose its source stack root.
- Lua closure construction follows the same rule: closed snapshot upvalues
  release-copy `uv->tv`, `func_newL()` publishes the derived proto and env
  edges, and Lua closure `uvptr[]` entries are release-published and barriered.

M5 table/parser publication wrapper slice:
- Converted lj_tab_newkey to release-publish installed keys before lj_gc_pubtab, preserving the -0 canonicalization path.
- Converted parser proto constant references to setgcrefrel plus lj_gc_pubobjobj.
- Parser and bytecode-reader proto `chunkname`/KGC refs now use release stores
  and proto-to-child barriers, so loaded or parsed prototypes cannot lose
  preexisting names, tables, cdata constants, or child protos during GC2 mark.
- Converted parser template-table stores to release-copy the constant/sentinel value before publishing the table. This keeps key preservation behavior while moving the last parser/table call sites off legacy barrier macros.
- Added tools/ci/m5_table_parser_publish.sh and wired it into the M5 aggregate. The legacy barrier detector count dropped from 3 to 0.
- Verification: tools/ci/m5_table_parser_publish.sh, make -C src -j after clean, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 x64 upvalue publication helper slice:
- Renamed the closed-upvalue store helper from the legacy barrier name to lj_gc_pubuv while preserving its GC2 mark hook and legacy incremental color repair behavior.
- Routed x64 interpreter USETV/USETS slow paths and the x86/x64 JIT OBAR lowering through lj_gc_pubuv / IRCALL_lj_gc_pubuv.
- Follow-up: Linux/x64 JIT GC-valued `IR_USTORE` into closed upvalue cells now
  lowers through `lj_func_storeuv_forjit()`, which release-copies a complete
  `TValue` before the existing `IR_OBAR`/`lj_gc_pubuv` publication path runs.
  This avoids publishing a shared GC64 upvalue cell via split pointer/tag stores.
- Follow-up: x64 interpreter closed-cell `CSET`, closed `USETV`, closed
  `USETS`, closed `USETN`, and closed `USETP` now route through
  `lj_func_storeuv_*_pub()` helpers, release-copying the complete `TValue`
  before running the upvalue publication helper from C. Open legacy upvalues
  still write their stack slot directly.
- `lj_gc_pubuv()` now returns early for non-GC snapshots, so helper-backed
  closed upvalue stores can share the same post-store publication path for both
  GC and non-GC values without exposing the legacy white check to non-GC tags.
- Added tools/ci/m5_x64_uv_publish.sh and wired it into the M5 aggregate. The coverage is intentionally scoped to the supported x86-64 Linux path and does not touch aarch64/ARM64.
- Verification: tools/ci/m5_x64_uv_publish.sh, make -C src clean && make -C src -j, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 empty-hash table insert slice:
- Changed lj_tab_newkey so tables with hmask==0 rehash/retry before any hashkey() lookup against the shared global nilnode fallback.
- Added an assertion that the Brent/free-node path never inserts through G(L)->nilnode.
- Added tests/t-tab-emptyhash.c and tools/ci/m5_tab_emptyhash.sh to verify first string/sparse-integer hash inserts leave nilnode key/value/next untouched and to document the early empty-hash branch.
- Verification: tools/ci/m5_tab_emptyhash.sh, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 dormant local-cell opcode substrate slice:
- Added dormant BC_CNEW/BC_CGET/BC_CSET opcodes after FUNCCW and x64 interpreter handlers only. Parser emission and bytecode dump versioning remain intentionally unchanged for this slice.
- Added lj_func_newuvcell for closed nil local cells and taught lj_func_newL_gc to inherit an existing local-cell GCupval when a parent local slot is tagged LJ_TUPVAL.
- Taught debug local name tracing to follow BC_CGET like MOV, and made lua_getlocal/lua_setlocal unwrap/write cell locals through the closed-upvalue TValue with lj_gc_pubuv publication.
- Historical `tools/ci/m5_cell_ops.sh` coverage included exact implementation
  artifact checks. That script is gone; current coverage is the Lua-owned
  `m5_cell_ops` behavior case, `m5_bcdump_compat` for bytecode format
  compatibility, and code-adjacent notes for dormant parser/JIT boundaries.
- Historical verification: tools/ci/m5_cell_ops.sh passed.

M5 string table representation prep slice:
- Added StrTabHdr with mask/resize/copy_cursor/pad/bucket[] and moved StrInternState to g->str.tabh while keeping g->str.mask as a compatibility mirror for existing fast paths.
- Moved the LUAJIT_SECURITY_STRHASH secondary-chain marker from bit0 to bit1 via LJ_STRHASH_SECONDARY, reserved bit0 as LJ_STRHASH_DEAD for the future Harris unlink mark, and routed string table traversals through lj_str_hashhead() so both low bits are masked.
- Updated legacy GC, GC2 paranoia/global root scanning, shutdown, and arena mark tests to treat the whole StrTabHdr allocation as the string table memory root.
- Added tests/t-strtab-prep.c and tools/ci/m5_strtab_prep.sh; wired the coverage into tools/ci/m5_concurrent_objects.sh.
- Parser audit note: do not enable parser cell emission before the v2/v3 bytecode compatibility slice. Captured-only cellization is unsafe as a finish-time rewrite because capture discovery happens after earlier declarations/accesses have already emitted bytecode.
- Verification: tools/ci/m5_strtab_prep.sh, tools/ci/m2_arena_gcmark.sh, tools/ci/m3_gc2_paranoia.sh, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 bytecode dump compatibility slice:
- Bumped the current writer format to BCDUMP_VERSION_LOCKLESS=3 while keeping BCDUMP_VERSION_LEGACY=2 accepted by the loader.
- Added GC64 proto flags2 metadata for v2-loaded legacy-upvalue protos instead of using GCproto.flags, whose top bits are mutated as the closure-count field.
- Added load-time bytecode verification after endian normalization: opcode bounds, v2 rejection of appended cell opcodes, CNEW/CGET/CSET frame-slot bounds, cell-op plus closing-UCLO rejection, and per-proto flag bit validation.
- Made bcwrite reject legacy-loaded proto trees before writing any dump bytes, so ordinary source-compiled functions still dump as v3 and v2-loaded functions cannot be silently re-emitted as incompatible v3.
- Added tests/t-bcdump-compat.c and tools/ci/m5_bcdump_compat.sh; wired the coverage into tools/ci/m5_concurrent_objects.sh.
- Updated the local implementation plan text to use BCDUMP_VERSION_LOCKLESS, remove stale lock-behind-LJ_MT wording, and stop recommending legacy metadata in pt->flags.
- Verification: tools/ci/m5_bcdump_compat.sh, tools/ci/m5_cell_ops.sh, tools/ci/m5_concurrent_objects.sh, tools/ci/m0_matrix.sh, and src/luajit tests/t-threading-upvalue.lua passed.

M5 parser captured-local metadata slice:
- Added parser-only VSTACK_VAR_CAPTURED metadata on VarInfo.info and mark it when nested variable lookup turns an outer local into an upvalue.
- Added an assert-build invariant in fs_fixup_uv2 that every local upvalue descriptor came from a captured local marker before PROTO_UV_LOCAL encoding is finalized.
- Kept parser cell bytecode emission dormant: lj_parse.c still does not mention BC_CNEW/BC_CGET/BC_CSET, and proto/upvalue serialization remains unchanged.
- Added tests/t-parser-capture-meta.lua and tools/ci/m5_parser_capture_meta.sh; wired the coverage into tools/ci/m5_concurrent_objects.sh.
- Verification: tools/ci/m5_parser_capture_meta.sh, tools/ci/m5_bcdump_compat.sh, tools/ci/m5_cell_ops.sh, tools/ci/m5_concurrent_objects.sh, tools/ci/m0_matrix.sh, and src/luajit tests/t-parser-capture-meta.lua passed.

M5 source-v3 upvalue semantics split:
- Narrowed the post-thread-activation snapshot deviation in lj_func_newL_gc to proto_legacyuv(pt) only. Source-compiled v3 protos now keep shared Lua upvalue semantics after mt_active instead of being treated like legacy-loaded v2 chunks.
- Updated tests/t-threading-upvalue.lua to assert source-v3 post-latch sharing and to synthesize v2 legacy dumps by patching the dump version byte so legacy-loaded protos still exercise capture-at-FNEW snapshot behavior.
- This is still a transitional source path: source closures are no longer semantically degraded by the legacy latch, but parser-emitted closed cell bytecode remains the required final safety/performance direction.
- Verification: clean build, tests/t-threading-upvalue.lua, plan/auxiliary/tests/t-uv.lua (7/8 passed; remaining failure is the intentionally unsynchronized all-updates-lost visibility probe), tools/ci/m4_threading_upvalue.sh, tools/ci/m5_concurrent_objects.sh, tools/ci/m0_matrix.sh, and final src/luajit tests/t-threading-upvalue.lua passed.

M5 activation upvalue identity slice:
- Removed the first-spawn blanket lj_func_closeuv(L, stack) call from threading_gc_enter. Source-v3 closures created before activation now keep their parent-visible upvalue identity instead of detaching the parent stack slot at the latch transition.
- Kept the legacy-loaded behavior focused on closure creation: proto_legacyuv(pt) still snapshots local captures after mt_active, and tests/t-threading-upvalue.lua now checks both source-v3 sharing and synthetic v2 legacy snapshotting.
- Follow-up: the legacy-loaded snapshot predicate also includes `mt_entering`.
  A secondary VM entry can be in the lifecycle handoff before `mt_active` is
  visible, and v2-style protos lack source cell-upvalue bytecode. The focused
  `t-legacy-upvalue-entering.c` fixture marks a child proto as legacy-shaped,
  runs it before and during `mt_entering`, and verifies only the entering-window
  call snapshots at FNEW time.
- This deliberately favors Lua source semantics while the parser cell model is incomplete. The remaining final-state work is to replace source open upvalues with parser-emitted CNEW/CGET/CSET cells so cross-thread sharing is safe without relying on stack-pointing open upvalues.
- Verification: clean build, tests/t-threading-upvalue.lua, plan/auxiliary/tests/t-uv.lua, tools/ci/m4_threading_upvalue.sh, tools/ci/m5_concurrent_objects.sh, and tools/ci/m0_matrix.sh passed.

M5 source cell-upvalue parser slice:
- Bumped the current dump format to BCDUMP_VERSION_LOCKLESS=4 and kept BCDUMP_VERSION_TRANS=3 accepted only for transitional no-cell-op dumps. v2/v3 dumps now reject CNEW/CGET/CSET, while v4 verifies cell slot bounds, rejects CGET/CSET self-overwrite, and still rejects cell ops with closing UCLO A != 0.
- Added PROTO2_CELLUV for GC64 protos whose local upvalue descriptors should promote parent stack slots into closed GCupval cells. Source/v4 FNEW now promotes a raw slot on demand or inherits an existing LJ_TUPVAL cell; v2 legacy behavior remains on the proto_legacyuv path.
- Enabled parser CGET/CSET emission for owner-frame reads/writes after capture discovery, CNEW/FNEW/CSET for self-captured local functions, and captured-local materialization for indexed stores parsed before the RHS marks the table/key captured. CGET/CSET tolerate raw slots so conditional closure creation and loop-variable capture stay correct before FNEW promotion.
- Removed source closing-UCLO emission for scope/goto exits while leaving UCLO 0 as the return/jump carrier for now. At this M5 boundary, self-captured local-function CNEW/CSET protos and loaded v4 cell protos remained PROTO_NOJIT until CNEW snapshot behavior and loaded-chunk cell invariants were audited. Owner-frame CGET/CSET alone no longer marks the owner proto NOJIT. This boundary is superseded by the M6 helper-backed self-cell CNEW/FNEW recorder slice.
- Updated parser/cell/bytecode checks and focused tests for source-v4 behavior. Lua-level synthetic v2 closure tests were removed because current source dumps now contain v4 cell bytecode; v2/v3/v4 format rejection/acceptance is covered by tests/t-bcdump-compat.c.
- Verification so far: clean build, src/luajit tests/t-parser-capture-meta.lua, src/luajit tests/t-threading-upvalue.lua, plan/auxiliary/tests/t-uv.lua, stock lang/upvalue/misc/uclo/lang/goto, tools/ci/m5_parser_capture_meta.sh, tools/ci/m5_bcdump_compat.sh, and tools/ci/m5_cell_ops.sh passed.

M5 owner-cell JIT groundwork slice:
- Added bounded recorder support for BC_CGET/BC_CSET over local cell slots:
  raw-slot CGET records as the raw slot value, raw-slot CSET updates the
  recorder stack slot, and promoted-cell CSET records UREFC/USTORE plus OBAR
  for GC-value stores.
- Added x64 lowering for direct-cell UREFC where op1 is the live LJ_TUPVAL
  slot TRef, avoiding stale constant-cell writes across repeated function
  invocations. Constant-KPTR cell lowering remains supported in the x64 helper
  but source emission does not rely on it.
- Reopened source-emitted owner CGET/CSET tracing on x64. The fix needed more
  than recorder/lowering support: VM trace-exit and recording/hook redispatch
  now distinguish real function headers and fast-function pseudo opcodes from
  appended local-cell opcodes, and the static dispatch table covers all real
  bytecodes so CNEW/CGET/CSET can be redispatched while recording.
- Extended the cell-op behavior coverage with repeated-invocation runtime
  checks that assert traces are produced for numeric and GC-valued owner
  CGET/CSET loops. The x64 dispatch-boundary rationale now lives in dispatch
  helper comments and behavior coverage.
- Verification: focused owner-cell runtime checks, tests/t-parser-capture-meta.lua,
  tests/t-threading-upvalue.lua, plan/auxiliary/tests/t-uv.lua, tools/ci/m5_cell_ops.sh,
  tools/ci/m5_parser_capture_meta.sh, tools/ci/m5_bcdump_compat.sh,
  tools/ci/m5_concurrent_objects.sh, tools/ci/m0_matrix.sh, and final default
  rebuild passed.

M5 source child-cell JIT slice:
- Removed the parser-side PROTO_NOJIT mark for source child protos whose
  upvalue descriptors capture parent local cells. Runtime FNEW already promotes
  the parent raw slot to a closed GCupval cell for PROTO2_CELLUV protos, and
  child bytecode records through the existing closed-upvalue UGET/USET path.
- Extended the cell-op behavior coverage to require traces for repeated
  numeric and GC-valued child-upvalue closure loops. The `fs_has_celluv`
  recorder boundary is documented near the parser/bytecode reader code and
  proven by runtime traceability.
- Subagent CNEW audit: keep the self-captured local-function CNEW/FNEW/CSET
  guard for now. Removing it is runtime-correct in simple repros but exposes
  BC_CNEW to the recorder, which aborts as NYI, and implementing BC_CNEW alone
  immediately hits FNEW NYI in creation loops. The safe next path is a
  dedicated non-sinkable upvalue-cell allocation IR/path for BC_CNEW (not FFI
  IR_CNEW), forced snapshot correctness after the allocation, then FNEW
  recorder support before removing the guard or opening loaded v4 cell protos.

M5 loaded cell-bytecode JIT slice:
- Narrowed the loaded-v4 no-JIT policy in lj_bcread.c. The original WIP marked
  any loaded v4 cell op or parent-cell upvalue proto `PROTO_NOJIT`; after the
  source owner/child JIT work, loaded v4 CGET/CSET protos and loaded child
  protos with local-cell upvalues use the same traceable paths.
- At this M5 boundary, loaded v4 protos containing BC_CNEW stayed
  `PROTO_NOJIT`. BC_CNEW allocation and the following FNEW creation pattern
  still needed dedicated recorder, snapshot, and FNEW support before creation
  loops were safe to trace. This boundary is superseded by the M6 self-cell
  CNEW/FNEW recorder slice and the later M6 mixed raw-local sync-helper trace
  and first-promotion trace slices.
- Extended `tests/t-bcdump-compat.c` plus current cell-op behavior coverage to
  assert that loaded v4 CGET/CSET and loaded child-upvalue dumps can trace,
  while loaded CNEW creation dumps stayed no-JIT at this M5 boundary. The old
  shell script name is historical only.

M5 string table CAS publication slice:
- Added an active-user pin in StrTabHdr.resize. Interners increment the count
  before walking/publishing through the current header; resize claims the high
  bit only when the count is zero, so the old vector is not freed while a
  string lookup or bucket CAS can still touch it.
- Changed string intern publication from plain prepend stores to a release CAS
  on the bucket head with an acquire duplicate recheck after CAS failure. Losing
  duplicate allocations are still unpublished raw string objects and are freed
  immediately.
- This is not the full 06 section 6.5 helping/Harris sweep protocol yet:
  resize currently skips if active interners are present, and secondary-chain
  rehash remains a follow-up audit point. A concurrent Lua stress also exposed
  an older allocator/TG crash in lj_arena_alloc during child-thread concat
  buffer growth; keep that as a separate allocator-concurrency item rather than
  treating it as a string-table CAS failure. This records the state of the CAS
  publication slice; later active-drain and secondary-rehash slices below
  advance those two gaps without changing the original section 6.5 target.
- Added tests/t-strtab-cas.c and tools/ci/m5_strtab_cas.sh; wired the coverage
  into tools/ci/m5_concurrent_objects.sh.

M5 immutable local-cell capture slice:
- Fixed a source-v4 parser/runtime hazard where a local first captured after
  earlier owner-frame reads could be promoted to an LJ_TUPVAL cell by FNEW,
  leaving earlier MOV/CALL bytecode to read the raw cell on later loop
  iterations. Immutable child upvalues now snapshot raw parent slots into a
  closed cell without replacing the owner slot.
- This restores ordinary immutable local-function capture patterns used by
  threading.spawn smoke tests. Mutable captures with earlier owner bytecode
  still require the larger parser materialization/rewrite pass; do not treat
  this as the final general solution for all pre-capture owner accesses.
- Added focused parser/runtime and spawned-thread regressions to
  tests/t-parser-capture-meta.lua and tests/t-threading-upvalue.lua.

M5 conservative owner local-cell emission slice:
- Replaced the captured-only owner-frame parser emission rule with a
  conservative source-v4 rule: active local reads emit raw-tolerant `BC_CGET`,
  local writes emit raw-tolerant `BC_CSET`, and table/key operands backed by
  active locals are materialized through `BC_CGET`.
- This preserves the original closed-cell mutable capture semantics when a
  local is first captured after earlier bytecode in the same loop. The earlier
  captured-only plan is still the performance target, but implementing it
  safely requires a real bytecode insertion/rewrite pass; a narrow MOV-only
  rewrite misses comparisons, arithmetic, table operands, and call bases.
- Added parser and threading regressions for mutable pre-capture owner reads:
  type/comparison/arithmetic reads before the first closure creation, later
  `FNEW` promotion, and subsequent owner reads/stores through the same loop.

M5 per-TG arena allocation routing slice:
- Routed arena-backed `lj_mem_realloc()` and `lj_mem_newgco_raw()` through the
  current TG allocator for new allocations, instead of always mutating the main
  TG's `g->allocd`. Existing-pointer realloc/free now resolves the arena
  `owner_tid` back to the owning TG before touching bump/free-list state.
- Tagged freshly adopted normal and huge arenas with `TGAlloc.owner_tid`, kept
  raw `lj_arena_map()` owner-neutral, and made spawned/attached TGs inherit the
  main state's internal arena mode so they get per-TG huge tracking.
- Added `tests/t-threading-alloc.lua` and `tools/ci/m5_threading_alloc.sh` for
  spawned-thread concat/string-intern allocation pressure. The original
  8-worker concat repro that crashed in `lj_arena_alloc` now completes.
- Updated `tests/t-arena-gcmark.c` for the current source-v4 closed-cell
  upvalue model: coroutine-captured locals are closed `GCupval` cells, not
  legacy open upvalues.
- Remaining allocator-owner work at this point was dead TG arena
  reclamation/transfer and sweeping across all TG-owned arenas. Later slices
  advanced both: dead arena-internal TGs transfer allocator ownership before
  physical unlink, and the current M3 owner-sweep bridge walks registered
  arena-internal TGs instead of sweeping only the main TG. The full 04/05
  protocol still needs broader live-thread cycle ownership and global/orphan
  arena balancing.

M5 GC2 owner-aware arena mark slice:
- Added `lj_tg_find_owner()` and routed GC2 memory marking/ismarked checks for
  arena-backed memory through `GCAhdr.owner_tid`, so child-TG huge allocations
  use the owning TG's huge table instead of the current TG's table.
- GC2 mark-begin now clears arena and huge-table marks across the TG list, and
  global root scanning marks every TG tmpbuf rather than only `G2TG(g)`.
- Extended the threading allocator stress with a post-join huge string return
  and tightened `tools/ci/m5_threading_alloc.sh` to guard the owner-aware GC2
  markers.

M5 string table active-drain resize slice:
- Changed string-table resize from "skip if active interners are present" to
  claim the `LJ_STRTAB_RESIZE` bit while preserving the active-user count, then
  wait for existing active interners to drain before copying the old buckets.
  New interners already spin on the resize bit, so inserts cannot keep extending
  a table after resize has claimed it.
- Retired old string-table headers onto `g->str.retired` instead of freeing
  them immediately after publishing the replacement header. The initial
  close-time-only stopgap is now advanced to a strtab-specific epoch drain:
  each retired header records `g->gc2.hs_epoch`. Original bridge state:
  a completed later safepoint handshake called `lj_str_reclaim_retired()`
  directly to free older headers. The current M3 SMR bridge keeps the same
  grace-period rule but calls it through `lj_gc2_reclaim_retired()`. Raw
  retired blocks are still marked during GC until they drain, and state close
  remains the fallback cleanup.
- Fixed current-header string scans to snapshot one `StrTabHdr` and use its
  `mask`, avoiding a transient mix of `g->str.mask` with a different published
  header during resize.
- Kept the original 06 section 6.5 target intact: this is still an intermediate
  pin-and-drain protocol, not the final bounded helper-copy/Harris unlink/epoch
  retirement design.
- Updated `tests/t-strtab-cas.c` so it no longer relies on the old active-pin
  resize skip behavior and instead verifies the last active interner can leave
  a resize-claimed header, that same-epoch reclaim preserves the old header,
  and that the next completed handshake drains it.
- Follow-up sweep safety: legacy `GCSsweepstring` now claims the current
  `StrTabHdr` with the same resize bit before destructively relinking a bucket
  and freeing dead string bodies. Existing active interners drain first and new
  interners wait on the claim, so sweep no longer races a lockless bucket walk
  on the current bridge. `tests/t-strtab-cas.c` verifies that the sweep claim
  waits for an active reader before release.
- Tightened the safepoint scaffold used as the grace-period source: the leader
  may self-ack its own TG and may still remote-ack TGs explicitly in native
  state, but it no longer remotely acknowledges other running VM threads. That
  old deterministic test scaffold was not a valid SMR grace period for raw
  strtab header reclamation.
- Added dead-TG allocator transfer before physical unlink: `lj_tg_reclaim_dead`
  now moves a dead arena-internal TG's normal arena lists and HugeTab metadata
  to the main TG, retags arena `owner_tid`, frees/resets the dead TG tmpbuf
  while owner lookup is still valid, and only then removes the TG from
  `gc2.tg_list`. If transfer cannot complete, the dead TG remains linked as the
  conservative fallback.
- Added `lj_arena_alloc_transfer()` and `lj_arena_hugetab_transfer()` as the
  focused allocator-side helpers. This covers successful child TGs whose arena
  blocks outlive the OS thread; broader global/orphan arena balancing remains
  future allocator work.
- Follow-up owner-helper slice: arena owner tags now have explicit helper
  boundaries. `lj_arena_alloc_owner_*()` publishes/loads the owning TG id on
  `TGAlloc`, `lj_arena_owner_*()` publishes/loads the per-arena `GCAhdr`
  owner tag, and stack rehome, GC/GC2 owner lookup, fresh arena tagging, huge
  allocation, dead-TG transfer, and `lj_tg_tid_rel()` route through those
  helpers. `m5_threading_alloc` invariant: raw production owner
  tag access.

M5 secondary string-table rehash gate slice:
- Reused the string-table resize claim/drain primitive for
  `lj_str_rehash_chain()`: the rehash path now claims the current
  `StrTabHdr.resize` high bit, waits for active interners to leave, verifies
  the claimed header is still current, rechains the collision chain in place,
  releases the bit, and retries the pending insert.
- This closes the earlier secondary-chain audit point for the current
  pin-and-drain implementation. It is still not the original full 06 section
  6.5 bounded helper-copy/Harris unlink design.
- String-table resize/rehash claimers and new interners now wait on the
  `StrTabHdr.resize` claim/active-count state through the current `lua_State`
  native sleep helper instead of spinning on `la_cpu_pause()`. The
  pin-and-drain protocol is unchanged, but a preempted resize owner or active
  interner no longer forces peer threads to burn CPU. Using the active state
  keeps the contending TG native and safepoint-visible during string-table
  resize/rehash contention.
- Added `tests/t-strtab-rehash.c` and extended `tools/ci/m5_strtab_cas.sh`.
  The test uses the live string-table seed to find enough primary-hash
  collisions for one bucket, forces the secondary rehash path, verifies the
  resize bit is released, and checks duplicate interning identity afterward.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_strtab_cas.sh`, `tools/ci/m5_strtab_prep.sh`,
  `plan/auxiliary/tests/t-str.lua 8`, `tools/ci/m5_concurrent_objects.sh`, and
  `tools/ci/m3_gc2_scaffold.sh` passed. The GC2 scaffold run included a
  passing M0 matrix.

M5 string-table count atomics:
- Made `g->str.num` consistent with the current CAS intern bridge: successful
  interns already increment with `la_add32_rlx()`, and now string free
  decrements with `la_sub32_acqrel()` while GC shrink decisions read the count
  through `la_load32_acq()`.
- This avoids racing a plain sweep-side decrement or shrink-threshold read
  against concurrent string interning. It is still part of the interim
  pin-and-drain string-table bridge, not the final 06 section 6.5 helper-copy
  resize protocol.

M5 string-table GC/interner stress:
- Added `tests/t-strtab-gc-stress.lua` and the `m5_strtab_gc_stress` suite
  case. The test runs multiple Lua worker threads through shared and unique
  string interning while the main thread repeatedly drives full and incremental
  collection.
- This is runtime coverage for the current sweep bridge: legacy string sweep
  claims the active `StrTabHdr` before bucket relink/free, so active interners
  must either complete before the claim proceeds or enter after the claim is
  released. The test checks duplicate string identity, string-key table lookup,
  and post-stress collection without exposing internal table/string state.

M5 string ID allocation atomics:
- Changed `lj_str_alloc()` to assign `GCstr.sid` from
  `la_add32_rlx(&g->str.id, 1)` instead of mutating `g->str.id`,
  `g->str.idreseed`, or the global PRNG from the allocation path.
- This keeps interned string IDs unique under concurrent allocation. The
  original security reseed behavior is deferred until it can be reintroduced
  under a protocol that does not race with allocation outside the active
  string-table pin.

M5 table hash-vector SMR slice:
- Added `TabNodeRetire` records under `g->tab.retired_nodes` so
  `lj_tab_resize()` no longer immediately frees the old hash `Node[]` after
  publishing/reinserting into the replacement hash part. Resize reserves the
  raw record before mutation, keeps it unarmed until the old vector is no
  longer table-owned, then arms it with the current safepoint epoch.
- Added `lj_tab_reclaim_retired()` to drain armed records only after a later
  completed epoch, and `lj_tab_freeretired()` for close-time cleanup. Legacy GC
  and GC2 raw-root marking now keep both retired records and armed node vectors
  marked while they await reclamation.
- Original bridge state: `lj_safepoint_handshake()` called the table reclaimer
  directly after each completed epoch. The current M3 SMR bridge centralizes
  that same grace-period call through `lj_gc2_reclaim_retired()` and accounts
  reclaimed work in GC2 telemetry.
- This is an intermediate SMR step toward 06 section 6.3.5. It removes the
  immediate resize UAF hazard for future lockless table readers, but the
  runtime still uses the legacy `GCtab`/Brent insertion algorithm instead of
  the final `NHdr` helper-copy protocol from `tests/t-nbtab-model.c`.
- Added `tests/t-tab-retire.c` and `tools/ci/m5_tab_retire.sh`; wired the new
  guard into `tools/ci/m5_concurrent_objects.sh`.
- Follow-up retired-record helper slice routes table node/array retire record
  payloads, masks/capacities, epoch stamps, and armed flags through
  `lj_tab_*_retired_*()` acquire/release helpers. The table retire and array
  publication guards now document raw access to shared retired-record fields in
  `lj_tab.c`, legacy GC/GC2 scans, and the focused fixtures.
- Follow-up retired-head helper slice routes `global_State.tab.retired_nodes`
  and `global_State.tab.retired_arrays` head acquire/CAS/xchg operations
  through `lj_tab_*_retired_head_*()` helpers. Runtime retirement push, epoch
  reclaim, close-time free, legacy GC marking, GC2 paranoia scans, and focused
  fixtures now share the same head access vocabulary, and the M5 table guards
  document raw retired-head access outside `lj_tab.h`.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_tab_retire.sh`, `tools/ci/m5_concurrent_objects.sh`, and
  `tools/ci/m3_gc2_scaffold.sh` passed. The GC2 scaffold run included a
  passing M0 matrix.

M5 table hash-chain ordering slice:
- Added table-specific `lj_tab_nextnode_acq()` and `lj_tab_nextnode_rel()`
  helpers for `Node.next`. Runtime table lookup/set/keyindex walks and
  serializer dictionary walks now acquire-load chain links.
- Reworked the legacy Brent relocation and collision-append paths so moved or
  fresh free nodes are initialized before the predecessor `next` link is
  release-published. Other legacy relinks now publish through the same release
  helper.
- This is still not the final 06 section 6.3 `NHdr`/KEYLOCK/FORWARD/CAS
  insertion protocol; it gives the current legacy node chains the memory-order
  shape needed for the upcoming port and removes publish-before-init hazards
  in the current collision paths.
- Added `tests/t-tab-chain-order.c` and `tools/ci/m5_tab_chain_order.sh`; wired
  the guard into `tools/ci/m5_concurrent_objects.sh`. The test forces a
  deterministic string-key collision pattern that moves a displaced node out of
  another key's main slot, then verifies acquire walks find all three values.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_tab_chain_order.sh`, `tools/ci/m5_tab_retire.sh`,
  `tools/ci/m5_tab_emptyhash.sh`, `tools/ci/m5_concurrent_objects.sh`, and
  `tools/ci/m3_gc2_scaffold.sh` passed. The GC2 scaffold run included a
  passing M0 matrix.

M5 stable table-node insertion slice:
- Removed the legacy Brent relocation branch from `lj_tab_newkey()`. Existing
  hash nodes are no longer copied into free nodes or cleared from their old
  slots during insertion.
- Collision insertion now always initializes one free node for the new key,
  applies the table publication barrier, and release-links that node after the
  hash anchor. This matches the 06 section 6.3 direction that nodes are never
  moved within a generation, while still using the legacy `GCtab` layout and
  freetop scan until the full `NHdr`/KEYLOCK/FORWARD/CAS port lands.
- Documented the old move-specific Brent assignments/comments as obsolete.
  `tests/t-tab-chain-order.c` now verifies a
  deterministic collision where a foreign occupant remains stable in another
  key's main slot and the new key is reachable through the release-published
  chain.
- Verification: `tools/ci/m5_tab_chain_order.sh`, `tools/ci/m5_tab_retire.sh`,
  `tools/ci/m5_tab_emptyhash.sh`, a 50k string-key table smoke test,
  `tools/ci/m5_concurrent_objects.sh`, and `tools/ci/m3_gc2_scaffold.sh`
  passed. The GC2 scaffold run included a passing M0 matrix.

M5 legacy barrier documentation cleanup:
- The runtime/upvalue publication invariant now lives in the helper comments
  and behavior fixtures. The desired state remains no direct legacy barrier
  macro use outside the GC-owned implementation; this is review guidance and
  implementation documentation.

M5 invariant note refresh during M7 CLibrary cache work:
- Refreshed stale documented invariant strings in `m5_tab_value_publish.sh`,
  `m5_gcroot_publish.sh`, and `m5_threading_publish.sh` to match the current
  helper-factored code: `lj_lib.c` table setup now release-publishes directly
  through `lj_tab_set()`, FFI CLibrary cache fills now publish with
  `copyTVrel(L, tv, &tmp)`, FFI finalizer root reads use a local
  `global_State *g`, and GC2 thread root scanning delegates stack snapshots to
  `gc2_scan_thread_stack()`.
- Runtime behavior is unchanged by this note/guard refresh; it keeps the M5
  aggregate aligned with the already-landed release/acquire publication
  invariants while the M7 clib cache bridge changes nearby FFI cache code.
- Verification: `tools/ci/m5_tab_value_publish.sh`,
  `tools/ci/m5_gcroot_publish.sh`, `tools/ci/m5_threading_publish.sh`, and
  `tools/ci/m5_concurrent_objects.sh` passed.

M5 separated table-array header slice:
- Added `TabArrayHdr` immediately before separated table array slots. The
  runtime still publishes `GCtab.array` as a `TValue *` slots pointer and keeps
  `GCtab.asize`/`GCtab.acap` as compatibility mirrors, but the allocation base
  can now be recovered with `lj_tab_array_hdrw()` and sized with
  `lj_tab_array_bytes()`.
- Separated array allocation/free/SMR retirement now goes through
  `tab_array_new()` / `tab_array_free()`. Retired array records still carry the
  slots pointer for reader compatibility, but GC arena marking, GC2 marking,
  and paranoia raw-root checks mark/check the header base via
  `lj_tab_array_mem_acq()` or `lj_tab_array_hdrw(ret->array)`.
- Separated visible-size changes now publish a fresh `TabArrayHdr` generation
  instead of mutating header `asize` in place. Shrink generations keep enough
  capacity to tolerate stale C readers that still pair the old `GCtab.asize`
  mirror with the new slots pointer, but nil hidden tail slots so untraced array
  storage does not retain collectable values. Growth-within-capacity also
  retires the old generation instead of reusing it. `GCtab.asize`/`GCtab.acap`
  remain compatibility mirrors until the broader table-header reader port
  lands.
- Tightened `tests/t-tab-array-publish.c`, arena mark/sweep tests, and
  `tests/t-gc2-traverse.c` so memory-ownership assertions use array/node header
  bases instead of vector interior pointers. `tools/ci/m5_tab_array_publish.sh`
  now requires the header helpers and header-backed allocation/free path, and
  rejects reintroducing direct `TabArrayHdr.asize` stores.
- Verification: `tools/ci/m5_tab_array_publish.sh`,
  `tools/ci/m2_arena_gcmark.sh`, `tools/ci/m2_arena_gcsweep.sh`,
  `tools/ci/m3_gc2_paranoia.sh`, `tools/ci/m3_gc2_scaffold.sh`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit.sh`,
  `tools/ci/m8_weak.sh`, `tools/ci/m10_generational.sh`, and
  `tools/ci/m9_bench_smoke.sh` passed.

M5 table accessor node-header snapshot slice:
- Removed the legacy-shaped `hashkey(const GCtab *)` helper from `lj_tab.c`.
  The string, numeric, and generic get/set accessors now acquire `GCtab.node`,
  read `TabNodeHdr.hmask` from that acquired vector, and hash through the
  `_node` helpers with the explicit `(node, hmask)` pair.
- Empty-hash get paths now return before probing the shared nilnode, and
  empty-hash set paths route directly to `lj_tab_newkey()` for the resize/retry
  path. This keeps the main C table accessors on the same node-header pairing
  discipline as resize/newkey while the wider table-generation port remains
  pending.
- Documented the obsolete `hashkey/hashstr/hashnum/hashmask/hashgcref(t, ...)`
  lookup forms and the explicit node-header hash requirement.
- Verification: `tools/ci/m5_tab_node_publish.sh`,
  `tools/ci/m5_tab_nodehdr.sh`, `tools/ci/m5_tab_emptyhash.sh`,
  `tools/ci/m5_tab_chain_order.sh`, `tools/ci/m5_tab_retire.sh`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit_href_nodehdr.sh`,
  `tools/ci/m6_jit_hrefk_nodehdr.sh`, `tools/ci/m3_gc2_paranoia.sh`, and
  `git diff --check` passed.

M5 table integer array snapshot slice:
- Added `lj_tab_array_snapshot_acq()`, which acquire-loads the legacy
  `GCtab.asize` mirror before `GCtab.array`, then switches separated arrays to
  the acquired `TabArrayHdr.asize`. Colocated arrays are identified by pointer
  identity instead of the mutable `colo` flag, so colocated-to-separated
  promotion cannot misclassify an old colocated slots pointer as headered.
- Replaced the `inarray`/`arrayslot` integer get/set macros with inline
  `lj_tab_getint()` / `lj_tab_setint()` functions that use that array snapshot
  before returning an array slot or falling through to the hash lookup path.
  This keeps the broad integer table-access surface paired with separated array
  generations while preserving the legacy colocated-array mirror path.
- `lj_tab_array_mem_acq()` now uses the same pointer-identity colocated check
  before returning a separated array header base. The array publication guard
  rejects the old integer-access macros and requires the snapshot helper; the
  array publication test covers separated grow/shrink and colocated promotion
  through the new helper.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_tab_array_publish.sh`, `tools/ci/m3_gc2_paranoia.sh`,
  `tools/ci/lua_test.sh m6_jit_aref_pair_boundary`, `tools/ci/m6_jit.sh`, and
  `tools/ci/m5_concurrent_objects.sh`; `git diff --check` passed.

M5 table array scanner snapshot slice:
- Moved core C array scanners onto `lj_tab_array_snapshot_acq()`: table clear,
  duplicate, rehash array counting, traversal key indexing/next, length fast
  paths, `table.maxn`, bytecode template writing, serializer table array
  scanning, parser template fixup, and legacy/GC2 table array traversal and
  weak-value clearing.
- `lj_tab_dup()` now uses the snapshot size for allocation and reacquires the
  source array/node snapshots after allocation before copying. It also snapshots
  the newly allocated duplicate table's array and node headers before filling
  slots, preserving the old post-allocation consistency check without holding
  stale table-vector pointers across allocation.
- Tightened `tools/ci/m5_tab_array_publish.sh` with markers for the broader
  snapshot-scanner coverage.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_tab_array_publish.sh`, `tools/ci/m3_gc2_paranoia.sh`,
  `tools/ci/m2_arena_gcmark.sh`, `tools/ci/m2_arena_gcsweep.sh`,
  `tools/ci/m8_weak.sh`, `tools/ci/lua_test.sh m6_jit_aref_pair_boundary`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit.sh`,
  `tools/ci/m10_generational.sh` and `git diff --check` passed.

M5 x64 iterator array-header snapshot slice:
- Updated x64 `ipairs_aux`, `lj_vm_next`, and `BC_ITERN` array traversal to
  load the array slots pointer, keep the legacy `GCtab.asize` value only for
  empty or colocated arrays, and replace it with `TabArrayHdr.asize` when the
  slots pointer names a separated array generation. This moves the iterator
  fast paths off mixed `GCtab.array` plus `GCtab.asize` bounds for separated
  arrays while preserving current colocated table behavior.
- Follow-up hardening routes those same x64 iterator fast paths through the
  existing C forwarding helpers when a separated array header carries
  `TABARRAY_FLAG_RETIRING`, and `lj_tab_keyindex()` now retries if the table's
  array or node generation changes while mapping a traversal key.
- Documented the x64 iterator requirement: fast paths must use the
  `TAB_COLO_SLOTS`/`TABARRAY_ASIZE_OFS` checks instead of the old direct
  `ipairs_aux` array-bound compare or repeated `lj_vm_next` array reload.
  Runtime iterator and fallback coverage owns the observable behavior.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_x64_ipairs_snapshot.sh`,
  `tools/ci/m5_x64_table_next_snapshot.sh`,
  `tools/ci/m5_x64_itern_snapshot.sh`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit.sh`, and
  `git diff --check` passed.

M5 x64 TGET array-header snapshot slice:
- Updated x64 `BC_TGETV`, `BC_TGETB`, and `BC_TGETR` array fast paths to load
  the array slots pointer, use the legacy `GCtab.asize` only for empty or
  colocated arrays, and otherwise bound against `TabArrayHdr.asize` before
  reading the array slot. This removes another direct separated-array
  `GCtab.array` plus `GCtab.asize` pairing from the Linux/x64 interpreter.
- Added `tools/ci/m5_x64_tget_array_header.sh` and wired it into the M5
  aggregate. The coverage covers separated-array runtime reads and rejects the old
  direct `cmp RCd, TAB:RB->asize` / `add RC, TAB:RB->array` pattern inside
  `TGETV`/`TGETB`/`TGETR`.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_x64_tget_array_header.sh`,
  `tools/ci/m5_x64_tgets_node_order.sh`,
  `tools/ci/m5_x64_ipairs_snapshot.sh`,
  `tools/ci/m5_x64_table_next_snapshot.sh`,
  `tools/ci/m5_x64_itern_snapshot.sh`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit.sh`, and
  `git diff --check` passed.

M5 x64 TSET array-header snapshot slice:
- Updated x64 `BC_TSETV`, `BC_TSETB`, and `BC_TSETR` array fast paths to load
  the array slots pointer, keep the legacy `GCtab.asize` only for empty or
  colocated arrays, and otherwise bound against `TabArrayHdr.asize` before
  computing the slot passed to `lj_tab_storetv()`.
- Updated x64 `BC_TSETM` to perform its constructor fit check against
  `TabArrayHdr.asize` for separated array generations, then pass a destination
  pointer derived from the same array slots pointer to `lj_tab_storetvn()`.
- Tightened `tools/ci/m5_x64_tset_nil_snapshot.sh` so it exercises
  separated-array writes and rejects the old direct
  `GCtab.asize`/`GCtab.array` pairing inside `TSETV`/`TSETB`/`TSETR`/`TSETM`.
  This is still an interim step: stale-generation write forwarding/CAS remains
  part of the pending table migration protocol.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_x64_tset_nil_snapshot.sh`,
  `tools/ci/m5_x64_tget_array_header.sh`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit.sh`, and
  `git diff --check` passed.

M5 residual table array snapshot reader slice:
- Moved remaining serializer dictionary array readers onto
  `lj_tab_array_snapshot_acq()`: dictionary prep after resize now snapshots the
  visible array before scanning, and decode-time string/metatable dictionary
  lookups snapshot the slots pointer and bound from the same generation.
- Updated the JIT recorder's x64 shared-array AREF shape decision to take its
  record-time `record_array` pointer from `lj_tab_array_snapshot_acq()`, and
  moved `rec_next_types()` array scanning to the same helper.
- Documented the new snapshot readers and the obsolete serializer dictionary
  and recorder pointer/size pairings; table-array and JIT AREF behavior own the
  observable coverage.
- Verification: `tools/ci/m5_tab_array_publish.sh`,
  `tools/ci/lua_test.sh m6_jit_aref_pair_boundary`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit.sh`, and
  `git diff --check` passed.

M5 table array header flag reservation:
- Reserved the high bits of `TabArrayHdr.acap` for array-generation state while
  keeping `sizeof(TabArrayHdr) == 8`, so existing x64 `array -
  sizeof(TabArrayHdr)` displacements remain unchanged. `TABARRAY_ACAP_MASK`
  covers all legal capacities (`LJ_MAX_ASIZE` fits below bit 28), and
  `TABARRAY_FLAG_RETIRING` names the first pending protocol bit.
- Added `lj_tab_array_hdr_init()` and `lj_tab_array_hdr_pack_acap()` so new
  separated array generations initialize a packed capacity/flags word in one
  place. `lj_tab_array_hdr_acap_acq()` masks off flags for existing callers,
  and `lj_tab_array_hdr_flags_acq()` exposes the reserved state lane.
- Tightened `tests/t-tab-array-publish.c` to assert current published/retired
  arrays have zero flags. Raw `hdr->acap = acap` initialization is documented
  here.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_tab_array_publish.sh`,
  `tools/ci/lua_test.sh m6_jit_aref_pair_boundary`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit.sh`, and
  `git diff --check` passed.

M5 table array RETIRING flag publication:
- Added `lj_tab_array_hdr_flags_or_rel()` to atomically OR state bits into the
  packed `TabArrayHdr.acap` word while preserving the masked capacity seen by
  existing readers.
- `lj_tab_resize()` now sets `TABARRAY_FLAG_RETIRING` on an old separated array
  generation after replacement contents are ready and before publishing any
  replacement table array/hash state. Current published arrays still initialize
  with zero flags; retired separated arrays now retain the `RETIRING` bit until
  epoch reclamation frees the generation.
- Tightened `tests/t-tab-array-publish.c` to cover the flag transition on
  retired arrays and the resize-time publication call.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_tab_array_publish.sh`,
  `tools/ci/lua_test.sh m6_jit_aref_pair_boundary`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit.sh`, and
  `git diff --check` passed.

M5 table node RETIRING flag publication:
- Replaced the unused word in `TabNodeHdr` with a node-generation `flags` word
  and reserved `TABNODE_FLAG_RETIRING` without changing the 8-byte node prefix
  used by x64 header-mask loads.
- Added `lj_tab_node_hdr_flags_acq()` and
  `lj_tab_node_hdr_flags_or_rel()`. Fresh node vectors and the shared nilnode
  initialize with zero flags; `lj_tab_resize()` marks an old hash generation
  `RETIRING` after rebuild/routing is complete and before publishing the
  replacement hash pointer/mask.
- Tightened `tests/t-tab-nodehdr.c` and `tests/t-tab-retire.c` to cover zero
  flags on current generations and `RETIRING` on retired node vectors.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_tab_nodehdr.sh`, `tools/ci/m5_tab_retire.sh`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m6_jit.sh`, and
  `git diff --check` passed.

M5 internal table sentinel representation:
- Reserved two raw lightuserdata patterns in the public-unreachable segment
  255 for table migration sentinels: `LJ_TFORWARD_BITS` and
  `LJ_TKEYLOCK_BITS`. Public `lj_lightud_intern()` already leaves that segment
  unused for traversal-index safety, so `lua_pushlightuserdata()` cannot
  produce these raw values.
- Added `tvisforward()`, `tviskeylock()`, `tvistabinternal()`,
  `setforwardV()`, and `setkeylockV()`. The sentinels classify as
  lightuserdata, not numbers and not GC references, so they avoid the earlier
  NaN-tag regression caused by lowering `LJ_TISNUM`.
- Added `tests/t-itype-sentinel.c` and `tools/ci/m5_itype_sentinel.sh`, wired
  into the M5 aggregate after the NaN tag guard, to prove the sentinels do not
  collide with public NULL lightuserdata and do not look like collectable
  values. Full reader/writer filtering and FORWARD/KEYLOCK protocol use remain
  separate follow-up work.
- Verification: `make -C src clean && make -C src -j$(nproc)`,
  `tools/ci/m5_itype_sentinel.sh`, `tools/ci/m5_itype_nan.sh`,
  `tools/ci/m5_concurrent_objects.sh` and `git diff --check` passed.

M5 FINREG key-lock bridge:
- Routed the existing FINREG missing-key insertion helpers through the reserved
  `LJ_TKEYLOCK` key sentinel without changing their value-claim semantics.
  `lj_tab_try_newkey_anchor()` and `lj_tab_try_newkey_chain()` still claim
  `Node.val` with the FINREG finalizer sentinel first, then release-store
  `LJ_TKEYLOCK` into `Node.key` until the real cdata key is published.
- FINREG inserters now retry when they encounter a visible key lock in an
  anchor, linked collision node, or reserved free node. Legacy GC and GC2
  FINREG traversal wait out both the value claim and `LJ_TKEYLOCK` before
  marking a non-empty slot.
- Follow-up stability slice: FINREG missing-key inserters now wait out those
  visible key/value publication claims through a no-`lua_State` native sleep
  helper instead of spinning on `la_cpu_pause()`. This keeps a preempted
  publisher from turning hidden FINREG table insertion into an unbounded busy
  wait while preserving the existing claim/key publication protocol.
- This is the first concrete table-reader filtering use of the reserved
  sentinel representation. General hash lookup, FORWARD migration, and the
  final NHdr/AHdr helper-copy write protocol remain separate follow-up work.

M5 table KEYLOCK lookup filtering:
- Added `tab_key_islocked()` plus separate wait-capable and read-only retry
  helpers for the C hash lookup bridge. Write-side probes still wait/retry when
  they need a key publisher to finish, but `lj_tab_getinth()`,
  `lj_tab_getstr()`, generic `lj_tab_get()`, and `lj_tab_next()` now avoid the
  no-`lua_State` wait helper on `LJ_TKEYLOCK`: they retry/snapshot once and
  filter unpublished keys if still hidden.
- Table setter probe loops retry directly on `LJ_TKEYLOCK` so they do not
  declare a miss and insert a duplicate while a key publisher is in flight.
  `lj_tab_next()` filters a still-locked key from traversal instead of exposing
  the internal sentinel as a Lua key.
- Added and later extended `tests/t-tab-keylock-lookup.c`, now Lua-suite owned
  as `m5_tab_keylock_lookup`. The runtime test forces key-locks into collision
  chains, verifies traversal hides them, verifies direct/generic readers do not
  sleep behind them, verifies the rest of the chain remains readable, then
  restores the keys.

M5 table new-key KEYLOCK recheck:
- Hardened `lj_tab_newkey()` with `tab_findkey_or_keylock()` so direct
  new-key callers re-scan the target collision chain before insertion. If a
  racing publisher already installed the key, the helper returns the existing
  slot; if any node in the chain or candidate free-node scan is still
  `LJ_TKEYLOCK`, it pauses and retries against the current node generation.
- General `lj_tab_newkey()` now KEYLOCK-claims nil-key/nil-value anchor slots
  and claimed free nodes before release-publishing the new key. Tombstone
  anchors whose key is still non-nil are no longer overwritten by this bridge;
  the insert falls through to a claimed free node instead.
- Collision insertion now claims free nodes with a bounded linear CAS scan
  from the anchor and no longer mutates the legacy `freetop` cursor in
  `lj_tab_newkey()`.
- `TabNodeHdr.flags` now packs the free-node counter in its low bits while
  preserving the high-bit generation flags. Shared `lj_tab_newkey()` and
  FINREG anchor/chain insertion reserve that counter before claiming an
  anchor/free node and release the reservation if the publish loses a race;
  private rebuild, clear, and duplication paths keep the counter coherent for
  the next shared insertion.
- Collision insertion now also treats an anchored chain at `LJ_TAB_MAXCHAIN`
  as a resize trigger before publishing another free node. The helper runs the
  normal rehash heuristics first, then guarantees at least one hash-size growth
  if the count-based rebuild would have kept the same hash mask.
- This closes the gap between a setter's pre-insert probe and the legacy
  insertion helper for the current bridge. Cooperative resize/helping remains
  pending for the final NHdr/AHdr helper-copy implementation.
- Extended `tests/t-tab-keylock-lookup.c` and
  `tools/ci/m5_tab_keylock_lookup.sh` to assert that duplicate direct
  `lj_tab_newkey()` calls return existing anchor/chain slots instead of adding
  duplicate nodes, and that insertion after an anchor tombstone preserves the
  anchor key while installing the replacement in the collision chain without
  advancing `freetop`. The guard now also checks freecount preservation on
  duplicate new-key calls and one-count consumption on the replacement insert.
- Follow-up KEYLOCK resize proof extends the same fixture to hide an integer
  hash key behind `LJ_TKEYLOCK` while resizing to an array-only table. Resize
  must wait out the key publication, freeze the old hash value, and migrate the
  value into the array successor. The restored `m5_tab_keylock_lookup.sh`
  wrapper now documents the key-wait-before-count and
  key-wait-before-migrate ordering in `lj_tab_resize()`.
- Extended `tests/t-tab-chain-order.c` and
  `tools/ci/m5_tab_chain_order.sh` to force an eight-node same-bucket chain and
  verify that the ninth insertion grows the hash generation instead of
  extending the old chain.

M5 table RETIRING generation retry bridge:
- Added `lj_tab_array_is_retiring()` and consumed the existing
  `TABARRAY_FLAG_RETIRING` bit in `lj_tab_array_snapshot_acq()`. C paths that
  acquire array pointer/size pairs through that helper now reacquire if the
  selected separated array generation is already marked retiring, avoiding
  reads/writes through a generation that resize has begun to publish away. This
  covers the integer helpers plus the existing GC, serializer, parser,
  recorder, traversal, and length scans that share the snapshot helper.
- Routed `lj_tab_array_mem_acq()` through the same array snapshot helper, so
  GC/GC2 markmem and paranoia raw-root checks also avoid selecting an array
  generation that is already marked `TABARRAY_FLAG_RETIRING`.
- Added `lj_tab_array_separated_snapshot_acq()` /
  `lj_tab_array_separated_acap_acq()` for reader paths that need the current
  separated-array capacity. GC/GC2 table traversal, GC paranoia checks, legacy
  table-size accounting, and `lj_tab_free()` now avoid `GCtab.acap` as a
  correctness source for separated array generations.
- Serializer dictionary preparation now preserves the existing array size for
  hash-only resize from the same snapshot helper instead of reading the legacy
  `GCtab.asize` mirror directly.
- Private table array fills for `table.pack`, bytecode template-table loading,
  serializer table decode, and parser template-array growth checks now enter
  through `lj_tab_array_snapshot_acq()`, reducing the remaining direct
  `GCtab.array`/`GCtab.asize` assumptions outside ownership paths.
- Added `lj_tab_node_is_retiring()` / `lj_tab_node_snapshot_acq()` and routed
  C hash scans and write-side probe entry through the shared node/mask snapshot.
  The helper now reacquires if the selected node generation carries
  `TABNODE_FLAG_RETIRING`;
  this covers core table lookups/traversal/length, duplicate source reads,
  GC/GC2 table scans, serializer/parser/bytecode-writer scans, recorder shape
  snapshots, FFI finalizer registry scans, `hashmask()`, setters,
  `lj_tab_newkey()`, and FINREG new-key claim helpers.
- Table clear/free hash readers now use the same node snapshot discipline:
  `clearhpart()`, `lj_tab_clear()`, and `lj_tab_free()` derive the hash vector
  and mask from `lj_tab_node_snapshot_acq()` instead of pairing a raw
  `GCtab.node` load with a later node-header mask read.
- `lj_tab_resize()` now snapshots the current node/mask and array/size before
  rebuilding, and derives separated-array capacity from the selected
  `TabArrayHdr` instead of the legacy `GCtab.acap` mirror. The resize owner still
  owns the release-publish order for replacement arrays and nodes.
- Added `lj_tab_acap_acq()` / `lj_tab_acap_rel()` for the remaining
  `GCtab.acap` compatibility mirror. Table initialization, fresh array
  publication, and resize-side new-generation publication now route mirror
  stores through the helper while preserving the existing acap-before-array-
  before-asize publication order.
- Added `lj_tab_colo_acq()` / `lj_tab_colo_rel()` for the legacy colocated-array
  marker. Table initialization, colocated table construction, resize promotion
  to separated storage, table free/accounting, legacy GC propagation sizing, and
  the x64 recorder's separated-array header gate now read/write `GCtab.colo`
  through byte acquire/release helpers.
- Added `lj_tab_metatable_acq()` / `lj_tab_metatable_rel()` and
  `lj_udata_metatable_acq()` / `lj_udata_metatable_rel()` for release-published
  table/userdata metatable refs. Runtime C API/base-library setters, userdata
  constructors, GC/GC2 traversal, metamethod lookup, recorder guards,
  serializer/snapshot restore, and FINREG generation enable/disable now use the
  object-specific helpers instead of open-coded `tabref_acq()`/`setgcrefmt()`
  on `metatable` fields.
- Added `lj_func_env_acq()` / `lj_func_env_rel()`,
  `lj_funcL_env_acq()`, `lj_state_env_acq()` / `lj_state_env_rel()`,
  `lj_state_env_copy_rel()`, and `lj_udata_env_acq()` /
  `lj_udata_env_rel()` for release-published environment refs. The C API,
  base/debug/JIT/FFI/buffer/io/threading libraries, closure construction,
  loader/lib registration, recorder global accesses, and GC/GC2 traversal now
  use owner-specific helpers instead of open-coded `tabref_acq()` /
  `setgcrefrel()` on `env` fields.
- Added `lj_tab_gclist_acq()` for the legacy weak-list bridge link in
  `GCtab.gclist`. Legacy weak-list membership checks, weak clearing, and the
  GC2 weak bridge/paranoia fallback walkers now acquire-step through this
  table-specific helper while the bridge still owns the link publication.
- Added `lj_state_openupval_ref()` / `lj_state_openupval_acq()` /
  `lj_state_openupval_rel()` / `lj_state_openupval_clear_rel()` plus
  `lj_state_mt_thread_acq()` / `lj_state_mt_thread_rel()` /
  `lj_state_mt_thread_clear_rel()` for the remaining per-state GCRef roots.
  Open-upvalue insertion/closing, stack rehome/resize, snapshot liveness, GC
  and GC2 thread traversal/oracles, thread creation/free, and threading
  userdata attachment now use the state helpers instead of open-coded
  `openupval` / `mt_thread` field access in C.
- Recorder indexed-key array-shape fallback paths now use
  `lj_tab_array_snapshot_acq()` instead of direct `lj_tab_asize_acq(t)`, leaving
  the remaining direct table array-size reads confined to the
  low-level snapshot helper itself.
- This only centralizes entry filtering for visibly retiring generations; those
  write paths can still race a later retire mark after snapshot and still await
  the final forwarding/CAS mutation protocol.
- This starts consuming the RETIRING flag lane reserved by earlier slices. It
  still does not install `LJ_TFORWARD`, does not copy cooperatively, and does
  not replace legacy freetop allocation; those remain the final
  RETIRING/FORWARD/CAS helper-copy protocol.
- Extended the node-header, node-publication, and HREFK recorder guards to
  require the shared node snapshot helper.

M5 table node next-generation bridge:
- Expanded `TabNodeHdr` to an aligned 16-byte header with `next_gen`, plus
  `lj_tab_node_nextgen_acq()` / `lj_tab_node_nextgen_rel()` helpers.
- Fresh hash generations and the shared nilnode initialize `next_gen` to NULL.
  `lj_tab_resize()` now release-publishes the old generation's successor to the
  replacement hash vector, or to the shared nilnode for hash shrink-to-empty,
  before setting `TABNODE_FLAG_RETIRING`.
- This reserves the concrete hop target required by the final `LJ_TFORWARD`
  protocol; current readers still reacquire from `GCtab.node` rather than
  walking `next_gen`.
- x64 VM hash-header hmask loads now use `TABNODE_HMASK_OFS` instead of the
  previous literal `-8`, so VM table fast paths continue to read
  `TabNodeHdr.hmask` after the header expansion.

M5 table array next-generation bridge:
- Expanded `TabArrayHdr` to an aligned 16-byte header with `next_gen`, plus
  `lj_tab_array_nextgen_acq()` / `lj_tab_array_nextgen_rel()` helpers.
- Fresh separated arrays initialize `next_gen` to NULL. `lj_tab_resize()` now
  release-publishes a retired separated array's successor before setting
  `TABARRAY_FLAG_RETIRING`.
- Integer array getters now use that successor to hop a visible
  `LJ_TFORWARD` value to the replacement separated array generation, with the
  existing one-shot retry/absence fallback when no successor is visible.
  Shrink-tail reads that hop to a successor array too small for the key fall
  through to the integer hash lookup, where resize already routed the tail
  value.

M5 table FORWARD value filter bridge:
- Added `tab_val_absent()` / `tab_slot_absent_acq()` so C table resize
  counting/copying, rehash sizing, `lj_tab_next()`, `lj_tab_len()`, and
  `lj_tab_len_hint()` treat `LJ_TFORWARD` values as absent internal migration
  markers instead of live Lua values.
- Direct table getters now avoid returning a slot that already contains
  `LJ_TFORWARD`. Array integer getters hop through
  `TabArrayHdr.next_gen` for a forwarded slot when the old separated array has
  a successor, and retain the one-shot retry/absence fallback when no successor
  is visible yet.
- Hash getter paths now use `tab_node_forward_hop()` to follow a matched
  `LJ_TFORWARD` value through `TabNodeHdr.next_gen` when the retired hash
  generation has a successor. They retain the one-shot retry/absence fallback
  when no successor is visible yet. Integer hash lookups first check the
  successor array snapshot after a hash-generation hop, covering array growth
  that routes an old hash integer key into the replacement array.
- C setter lookup paths now route matched forwarded slots to successor
  generations when `next_gen` is already visible: array setters hop through
  `TabArrayHdr.next_gen`, hash setters hop through `TabNodeHdr.next_gen`, and
  integer hash setters first check whether the key migrated into the successor
  array.
- If a C setter still observes an unresolved `LJ_TFORWARD` slot after the
  bounded retry, it now repairs conservatively instead of returning the sentinel
  slot to the caller: integer array setters force a replacement array generation
  and clear any copied sentinel in the current generation, with a max-array-size
  fallback that moves the affected key to the hash part instead of raising
  overflow just to repair a sentinel. Hash setters rebuild and re-resolve the
  key. This favors language safety/stability over the shortest legacy write path
  until the final cooperative writer lands.
- `lj_tab_next()` now resolves visible `LJ_TFORWARD` slots through the array or
  hash successor for the current logical key before falling back to absent
  filtering, so synthetic iterator scans no longer drop migrated entries when a
  successor generation is already published.
- `lj_tab_len()` and `lj_tab_len_hint()` now use an array slot absence helper
  that follows `TabArrayHdr.next_gen` for visible forwarded slots before
  deciding whether a candidate boundary is absent.
- This is a non-exposure/copying bridge only. It does not install
  `LJ_TFORWARD` and does not replace the final RETIRING/FORWARD/CAS write
  protocol.
- Added `tests/t-tab-forward-filter.c` and
  `tools/ci/m5_tab_forward_filter.sh`, wired into the M5 aggregate after the
  KEYLOCK lookup guard. The test injects synthetic FORWARD values into array
  and hash slots and verifies array getter filtering/next-gen hops, hash getter
  next-gen hops, setter routing to successor slots, unresolved setter repair,
  integer hash-to-array forwarding, `next()` forwarding, and length decisions
  through array successors.

M5 table CAS store bridge:
- Added `lj_tab_trystoretv_cas()`, a narrow `TValue` publish helper that
  acquire-loads the destination slot, returns `LJ_TAB_STORE_CAS_FORWARD` if it
  sees `LJ_TFORWARD`, and otherwise loops on `lj_tv_cas()` until the new value
  is CAS-published.
- Updated `lj_meta_tsettv_pair()` to resolve the target table/slot with
  `meta_tset()`, CAS-publish the value, and run the existing parent-aware GC2
  barrier only after a successful publication. If the slot became
  `LJ_TFORWARD` after lookup, the helper pauses and re-resolves through the
  table setter path, allowing successor generation routing to handle the
  write.
- Public C API table setters now use the same CAS-publish/retry discipline:
  `lua_settable()`, `lua_setfield()`, `lua_rawset()`, and `lua_rawseti()`
  re-resolve when a destination becomes `LJ_TFORWARD` after lookup and run
  their weak/parent publication barriers only after the CAS succeeds.
- `table.insert()` now routes both shifted integer-slot copies and the final
  inserted value through local CAS-retry helpers. The shift helper keeps the
  legacy set-before-get ordering so a growth during destination resolution
  cannot leave a stale source pointer, then retries if the destination becomes
  `LJ_TFORWARD` before publication.
- `jit.profile` registry anchors and clears now re-resolve by their internal
  registry keys and CAS-publish the hidden callback coroutine/function or nil
  clear, preserving the existing weak and parent table publication barriers
  after successful stores.
- `jit.attach()` event-table detach now re-resolves by the event key and
  CAS-publishes nil clears, so a slot forwarded after lookup retries through
  the table setter instead of writing the stale destination.
- `luaL_newmetatable()` now acquire-loads the registry slot, retries if the
  slot is or becomes `LJ_TFORWARD`, and publishes a newly-created metatable
  with a nil-only CAS so a racing existing metatable is returned rather than
  overwritten.
- FFI module registration now CAS-publishes `_LOADED.ffi` through a retrying
  string-key helper before running the existing weak-value and parent table
  barriers.
- Follow-up read-side cleanup now acquire-snapshots the registry `_LOADED`
  slot before using it as the parent table for that `_LOADED.ffi` publication.
- FFI initialization now CAS-publishes the callback table into
  `cts->miscmap[strempty]` and runs the table/object publication barrier after
  the successful store.
- Generic library registration now CAS-publishes string-key function installs
  and generic `LIBINIT_SET` copies before running the existing weak-value
  bridge.
- `luaopen_base()` now CAS-publishes `_G` into the long-lived environment
  table with the same no-barrier self-reference semantics as the old direct
  store.
- `luaopen_string()` now stack-roots the freshly-created string metatable,
  CAS-publishes its `__index` link, runs the table/object publication barrier,
  and only then release-publishes the `LJ_TSTR` base-metatable root.
- This covers the x64 VM/meta slow table-store helper that already carries
  parent context, the direct public C API setter surface, and the
  `table.insert()` table-library shift/value path, plus jit.profile registry
  anchors and clears, the `luaL_newmetatable()` registry installer, and FFI
  `_LOADED` registration, and generic library registration. It is still not
  the full 06 section 6.3 flag-gated RETIRING/FORWARD/CAS writer protocol;
  remaining bulk/internal C stores and helper surfaces still need
  key/parent-aware CAS retry or generation flag gating before production
  resize can freely freeze slots.
- Added `tests/t-tab-cas-store.c` and `tools/ci/m5_tab_cas_store.sh`, wired
  into the M5 aggregate after the FORWARD filtering guard. The test covers
  direct CAS publish, concurrent ordinary-value writers, direct FORWARD
  rejection, `lj_meta_tsettv_pair()` retrying into a successor array, and the
  four public C API setters, `table.insert()`, and `luaL_newmetatable()`
  retrying across synthetic forwarded array/hash generations while preserving
  the old forwarded slot.
- Follow-up harness cleanup keeps `m5_tab_value_publish` building the shared
  `t-tab-cas-store.c` fixture with `LJ_TAB_TEST_HELPERS`, matching the fixture's
  runtime helper-counter probes and the dedicated `m5_tab_cas_store` case.

M5 x64 ipairs_aux FORWARD fallback:
- Added exported `lj_tab_getint_hop()` as a VM-facing wrapper around the C
  integer table getter, so VM assembly can delegate visible forwarded integer
  slots to the C generation-hop logic without duplicating the table protocol.
- Updated x64 `ipairs_aux` to compare a non-nil array slot against
  `LJ_TFORWARD_BITS` and call `lj_tab_getint_hop()` only on that internal
  sentinel path. The normal non-nil fast path still returns the slot snapshot
  directly, and nil still terminates `ipairs`.
- Added `tests/t-x64-ipairs-forward.c`, wired through
  `tools/ci/m5_x64_ipairs_snapshot.sh`, to synthesize an old separated array
  with `next_gen` and a forwarded slot, republish it as the table array, and
  run Lua `ipairs()` through the x64 fast function. The guard verifies the loop
  sees the successor value instead of exposing/stopping on `LJ_TFORWARD`.

M5 x64 TGET array FORWARD fallback:
- Updated x64 `BC_TGETV`, `BC_TGETB`, and `BC_TGETR` array fast paths to
  compare non-nil array slot snapshots against the full-width
  `LJ_TFORWARD_BITS` sentinel. Forwarded slots branch to the existing vmeta/C
  table lookup path, where the C integer getter follows `TabArrayHdr.next_gen`;
  normal non-nil values still return through the inline fast path.
- `BC_TGETR` now preserves the integer key before converting it into an array
  slot pointer, so a forwarded-slot fallback can still call `lj_tab_getinth()`
  through `vmeta_tgetr` with the original key.
- Added `tests/t-x64-tget-forward.c`, wired through
  `tools/ci/m5_x64_tget_array_header.sh`, to synthesize an old separated array
  with a forwarded slot and verify literal, variable, and helper-function table
  reads return the successor value instead of the internal sentinel.

M5 x64 TSET array FORWARD fallback:
- Updated x64 `BC_TSETV`, `BC_TSETB`, and `BC_TSETR` array fast paths to
  compare slot snapshots against `LJ_TFORWARD_BITS` before publishing through
  `lj_tab_storetv()`. Forwarded slots branch to the existing vmeta/C setter
  path, where the C setter bridge routes the write to `TabArrayHdr.next_gen`.
- `BC_TSETR` now preserves the integer key before converting it into an array
  slot pointer, so a forwarded-slot fallback can still call `lj_tab_setinth()`
  through `vmeta_tsetr` with the original key.
- Added `tests/t-x64-tset-forward.c`, wired through
  `tools/ci/m5_x64_tset_nil_snapshot.sh`, to synthesize an old separated array
  with forwarded slots and verify literal-key, variable-key, and numeric-for
  table writes update successor slots instead of overwriting the internal
  sentinels.
- Extended that guard with a hook-driven table-constructor case that reaches
  real x64 `BC_TSETM` after a multires call, republishes the constructor
  temporary to an old forwarded array generation before the range store, and
  verifies the successor generation receives the `TSETM` values.

M5 x64 TGETS FORWARD fallback:
- Updated x64 `BC_TGETS_Z` so a matched string-key hash value is compared
  against full-width `LJ_TFORWARD_BITS` before the normal nil/metamethod
  decision. Forwarded values branch to the existing `vmeta_tgets` path, where
  the C string-key getter follows `TabNodeHdr.next_gen`.
- Added `tests/t-x64-tgets-forward.c`, wired through
  `tools/ci/m5_x64_tgets_node_order.sh`, to synthesize an old hash generation
  with `next_gen`, republish it with a forwarded string-key slot, and verify an
  x64 string-key table read returns the successor value rather than the
  internal sentinel.

M5 x64 BC_ITERN FORWARD fallback:
- Added exported `lj_tab_itern_forward()`, a VM-facing helper that starts C
  `lj_tab_next()` from a raw traversal index (`LJ_KEYINDEX` control marker),
  writes the resolved key/value pair into the iterator frame, and updates the
  hidden control slot with the next traversal index.
- Updated x64 `BC_ITERN` so visible forwarded array or hash slot snapshots call
  this helper instead of copying `LJ_TFORWARD` into Lua-visible iterator
  results. Normal non-nil array/hash entries still use the inline snapshot
  path.
- Added `tests/t-x64-itern-forward.c`, wired through
  `tools/ci/m5_x64_itern_snapshot.sh`, to synthesize old array and hash
  generations with `next_gen`, republish forwarded slots, and verify optimized
  `pairs()` loops see successor values.

M5 x64 lj_vm_next FORWARD fallback:
- Added exported `lj_tab_vmnext_forward()`, a VM-facing helper that starts C
  `lj_tab_next()` from a raw traversal index, writes the value/key layout
  expected by `lj_vm_next`, and returns the next traversal index.
- Updated x64 `lj_vm_next` so visible forwarded array or hash slot snapshots
  call this helper instead of returning the internal sentinel to JIT-recorded
  `next()` loops. Normal non-nil array/hash entries still use the inline
  snapshot path.
- Added `tests/t-x64-vm-next-forward.c`, wired through
  `tools/ci/m5_x64_table_next_snapshot.sh`, to synthesize old array and hash
  generations with `next_gen`, republish forwarded slots, and call
  `lj_vm_next` directly to verify it returns successor values and keys.

M5 TG root and mirror publication:
- Added `mref_acq()` and `setmrefrel()` to pair MRef readers/writers with the
  existing GCRef acquire/release publication helpers.
- Added `lj_tg_load_*()` / `lj_tg_store_*()` helpers for `cur_L`, `thread_L`,
  and `jit_base`. TG thread roots now publish with release stores and GC2,
  safepoint, callback, and attach/detach readers acquire-load them.
- Converted the transitional global `g->cur_L` and `g->jit_base` mirrors in
  `lj_tg_setcur_L()`, `lj_tg_clearcur_L()`, and `lj_tg_setjit_base()` to
  `setgcrefrel()`, `setgcrefnullrel()`, `gcref_acq()`, `mref_acq()`, and
  `setmrefrel()`.
- Documented the TG publication helper layer for `cur_L`, `thread_L`, and
  `jit_base`. GC2, safepoint, callback, and attach/detach behavior fixtures
  cover the acquire/release contract, while comments explain the helper
  boundary. Updated the M3/M4 notes to the helper-based rule.
- Verification: `tools/ci/m5_threading_publish.sh`,
  `tools/ci/m4_threading_capi.sh`, `tools/ci/m5_concurrent_objects.sh`,
  `tools/ci/m7_ffi_no_cts_l.sh`, `tools/ci/m7_ffi.sh`,
  `tools/ci/m10_generational.sh` and `git diff --check` passed. A serial
  `tools/ci/m3_gc2_scaffold.sh` rerun passed the scaffold-specific tests and
  then stopped in the stock LuaJIT test block with the branch's known
  `lib/contents.lua` and `opt/sink/nosink.lua` failures.

M5 GCtrace start prototype publication:
- Added `trace_startptgco_acq()`, `trace_startpt_acq()`,
  `trace_startpt_rel()`, and `trace_startpt_clear()` so the current trace's
  prototype root is release-published before GC2 can scan `&J->cur`, and trace
  traversal/debug/flush readers acquire-snapshot the root.
- Converted legacy GC, GC2, assembler, trace save/free/flush/abort, perf, and
  GDB JIT metadata readers away from raw `gcref(T->startpt)` and converted the
  current-trace writer away from raw `setgcref(J->cur.startpt, ...)`.
- Documented the helper layer and obsolete raw `GCtrace.startpt` C-side
  reads/writes.
- Verification: `tools/ci/m5_jit_trace_publish.sh` passed.

M5 recorder table-bump rollback cache publication:
- Added `rec_rbchash_*()` helpers so the recorder's reverse bytecode cache
  publishes the cached PC/prototype before release-storing the `IRRef` key, and
  readers acquire-load the key before snapshotting the cached PC/prototype.
- Converted TNEW/TDUP table-bump cache stores and `rec_idx_bump()` readers away
  from raw `J->rbchash[...]` `setmref()` / `setgcref()` / `ref` writes.
- Documented the helper layer and obsolete direct rollback-cache stores.
- Verification: `tools/ci/m5_tab_value_publish.sh` passed.

M5 IR KGC constant publication:
- Added `ir_iskgc_acq()`, `ir_kgc_load_acq()`, `ir_kgc_store_rel()`, and
  `ir_kgc_publish()` so GC-visible IR KGC constants publish the GCRef before
  release-publishing the `IR_KGC` opcode.
- Converted `lj_ir_kgc()` and the assembler ktrace patch to use release KGC
  helpers. Legacy GC, GC2, and retired-trace marking now acquire-snapshot the
  opcode before loading the KGC pointer from trace IR.
- Documented the helper layer, raw KGC publication ownership in `lj_ir.c` /
  `lj_asm.c`, and trace GC scanner traversal ownership.
- Verification: `tools/ci/m5_jit_trace_publish.sh` passed.

M5 CType name clear publication:
- Added `ctype_clearname()` so `CType.name` clears use the same release-store
  discipline as `ctype_setname()`. Parser rollback/abandon, ctype interning,
  and CTState bootstrap now clear published CType names through this helper.
- Documented the clear helper and obsolete raw `setgcrefnull(ct->name)` stores.
- Verification: `tools/ci/m5_ctype_name_publish.sh` passed.

M5 open-upvalue list publication:
- Added explicit acquire/release helpers for `GCupval` prev/next list links and
  `GCobj.nextgc` publication. Open-upvalue insertion, close-time unlinking, and
  `uvhead` bootstrap now publish links through those helpers.
- Converted GC, GC2, stack rehome/resize, JIT snapshot, and thread migration
  open-upvalue walks to acquire-load the list head and acquire-walk `nextgc`.
- Documented the helper layer and raw C-side `openupval` walk/link-store
  ownership in the covered paths.
- Verification: `tools/ci/m5_upvalue_publish.sh`,
  `tools/ci/m5_concurrent_objects.sh`, and `tools/ci/m10_generational.sh`
  passed.

M5 string-table chain publication:
- Added string-table bucket/chain helpers that acquire-load bucket heads and
  next links, and release-store rebuilt bucket heads and string `nextgc` links.
- Converted resize, secondary rehash, successful intern publication, legacy
  string sweep, and GC/GC2 string-table scans to use those helpers.
- Documented the helper layer and obsolete raw string-chain stores/walks in
  `lj_str.c` plus raw GC bucket-head scans.
- Verification: `tools/ci/m5_strtab_cas.sh`,
  `tools/ci/m5_concurrent_objects.sh`, and `tools/ci/m10_generational.sh`
  passed.

M5 string-table head and epoch helpers:
- Added helper boundaries for `g->str.tabh`, `g->str.retired`, and
  `StrTabHdr.retire_epoch` in `lj_str.h`.
- Routed string resize publication, retired-header CAS/xchg, state bootstrap,
  close-time free, legacy GC scans, GC2 scans, and focused string/arena tests
  through those helpers.
- Documented the helper layer and raw string-table head/retire-epoch ownership
  in the covered production and test paths.
- Verification: `git diff --check`, `tools/ci/m5_strtab_cas.sh`,
  `tools/ci/m5_strtab_prep.sh`, `tools/ci/m2_arena_gcmark.sh`,
  `tools/ci/m3_gc2_paranoia.sh`, `tools/ci/m10_generational.sh`, and
  `tools/ci/m5_concurrent_objects.sh` passed.

M5 Lua closure uvptr acquire consumers:
- Added `func_uvptr_acq()` and `func_uv_acq()` so Lua closure `uvptr[]` entries
  are consumed with acquire ordering to match their existing release
  publication in closure construction/upvalue join paths.
- Converted child-closure inheritance, GC, GC2, debug/API, recorder, optimizer,
  and x64 assembler consumers away from raw `gcref(fn->l.uvptr[...])` loads.
- Documented the helper layer and raw `uvptr[]` consumer ownership in the
  x86_64-relevant C/JIT paths.
- Verification: `tools/ci/m5_upvalue_publish.sh`,
  `tools/ci/m5_concurrent_objects.sh`, `tools/ci/m10_generational.sh`, and
  `tools/ci/m6_jit_token.sh` passed.

M5 x64 setmetatable publication helper:
- Added `lj_gc_pubtabobj_vm(L, t, o)` as a VM-callable version of the existing
  table-object publication wrapper. It runs the GC2 parent/child barrier and
  then performs the legacy black-table repair only when the child is white.
- Routed the x64 `setmetatable` fast path through that helper after the release
  metatable store. This removes one direct VM-side `barrierback` use while
  preserving the previous publication ordering. The fast path now untags the
  parent table before calling the helper; the earlier direct GC2 call passed
  the tagged return copy as the parent pointer.
- Documented the helper declaration, x64 call, parent untag, and obsolete
  inline `barrierback` fast-path form.
- Verification: reduced `setmetatable` repros, `tools/ci/m5_tab_value_publish.sh`,
  `tools/ci/m5_metatable_publish.sh`, `tools/ci/m5_concurrent_objects.sh`,
  `tools/ci/m10_generational.sh`, stock `lib/base/getsetmetatable.lua`,
  `tools/ci/m0_matrix.sh`, and final normal `make -j2` passed.

M5 table allocation root publication:
- Converted `GCtab` construction to allocate unlinked raw GC storage, initialize
  a valid empty or colocated table body, nil-clear new array slots before table
  metadata exposes them, and only then CAS-publish the table through
  `lj_gc_linkobj()`.
- Added `lj_tab_new0()` and routed the x64 `BC_TNEW` `RD == 0` case through
  it. This keeps the legacy color/root/accounting work in C while isolating the
  exact empty-table shape that future inline bump allocation will replace.
- Fresh table array installation now uses the same publish order as resize:
  set the capacity mirror, release-publish the array pointer, then
  release-publish the legacy `asize` mirror. The allocation notes document why raw
  array/asize stores in `newtab()`.
- Split the array clearing helper so `lj_tab_new()` and `lj_tab_new1()` no
  longer need a second post-construction clear; `lj_tab_dup()` still receives a
  valid nil-initialized destination before it copies template slots.
- Added table-allocation publication coverage. Regressing to linked `GCtab`
  allocation before body initialization is documented here as invalid.

M5 x64 array fast-path snapshot ordering:
- Reordered x64 `ipairs_aux`, `BC_TGETV`, `BC_TGETB`, `BC_TGETR`, `BC_TSETV`,
  `BC_TSETB`, `BC_TSETR`, and `BC_TSETM` array fast paths to load the legacy
  `GCtab.asize` mirror before loading `GCtab.array`. Fresh and resized arrays
  publish the array pointer before the size mirror, so the reader side must not
  take the pointer snapshot first and then pair it with a newer size.
- Extended `tools/ci/m5_x64_ipairs_snapshot.sh`,
  `tools/ci/m5_x64_tget_array_header.sh`, and
  `tools/ci/m5_x64_tset_nil_snapshot.sh` to enforce the size-before-pointer
  ordering in addition to the existing separated-array header checks.

M5 x64 retiring hash-generation guard:
- Added `TABNODE_FLAGS_OFS` to the x64 VM and made `getmetatable`,
  `ipairs_aux`'s empty-hash fallback, `lj_vm_next`, `BC_TGETS_Z`, and
  `BC_ITERN` test `TABNODE_FLAG_RETIRING` after loading the node pointer.
  Retiring generations leave the fast path: lookup paths use their existing
  C/meta fallback and iterator paths reuse the generation-aware forward helpers.
- Documented the x64 flag-test requirement before consuming `TabNodeHdr.hmask`,
  so VM hash readers do not silently traverse a hash vector that C readers
  would retry/hop away from. Active coverage belongs in behavior and fallback
  tests, with the backend rationale beside the VM code.

M5 C hookmask atomic helpers:
- Added `hookmask_*` helpers around `global_State.hookmask` so C-side hook,
  profiler, dispatch, and trace gating paths use acquire/release loads/stores
  or CAS loops instead of direct byte access. The field remains a byte to preserve
  x86-64 VM and recorder layout assumptions.
- `hook_enter`, `hook_leave`, `hook_entergc`, `hook_vmevent`, `hook_save`, and
  `hook_restore` now route through the helper layer; `lua_sethook` preserves
  non-event bits with the same CAS helper used by the hook macros.
- Profiler timer publication stores the sampled VM state before release-setting
  `HOOK_PROFILE`, and profile callbacks claim `HOOK_VMEVENT` through an atomic
  hookmask helper while they drain samples. `lj_dispatch_update()` now
  serializes global dispatch-template rewrites with a `dispatchmode` update
  token instead of relying on the old profiler mutex.
- Added `m5_hookmask_atomic` Lua smoke coverage. Helper comments document the
  C-side `hookmask` ownership boundary; generated VM/dasc hookmask loads remain
  a separate follow-up.

M5 global hook function/count atomics:
- Added helper accessors for the other global hook fields kept in
  `global_State` by 03 section 3.6: `hookf`, `hookcount`, and `hookcstart`.
  `lua_sethook()` now release-publishes the callback/count state before the
  hook event mask, and C readers acquire through the helper layer.
- `lj_dispatch_ins()` reloads the shared count atomically and resets from the
  acquired start count when the counter is at or below zero. The x86-64 VM hook
  dispatch paths now use `lock; dec` for their direct hookcount decrements.
- Added `tools/ci/m5_hook_state_atomic.sh` plus the `m5_hook_state_atomic` Lua
  smoke, wired into the M5 aggregate, to keep raw C-side hook function/count
  access and unlocked x64 hookcount VM decrements from returning.

M5 x64 hookmask acquire loads:
- Wrapped the generated x86-64 VM hookmask byte loads in
  `x64_vm_hookmask_acq()`. The macro emits the same `movzx` TSO load, matching
  the existing x64 acquire macro convention while documenting that these
  generated-code readers pair with C-side release publication.
- Documented the macro and raw `byte GL:*->hookmask` memory operand ownership
  in `vm_x64.dasc`.

M5 GC total atomic accounting:
- Added `lj_gc_total_*` helpers around `global_State.gc.total`. Allocation,
  free, stack rehome, GC step, GC2 pacing, public stats, `gcinfo()`, and
  `collectgarbage()` readers now use atomic helper access so concurrent TG
  accounting cannot lose updates on the legacy heap-size counter.
- Later x64 cleanup moved the VM allocation-check reads for `gc.total`,
  `gc.threshold`, `gc2.alloc_since_trigger`, and `gc2.hard_bytes` behind
  `lj_gc_should_step_vm()`, leaving the shared pacing decision in C helper code.
- Added `tools/ci/m5_gc_total_atomic.sh`, a pthread C helper stress, and a Lua
  stats/threading smoke, then wired the case into the M5 aggregate.

M5 GC2 pacing counter atomic helpers:
- Added helper accessors for the GC2 pacing counters that decide allocation
  trigger and mutator-assist work: `alloc_since_trigger`,
  `cycle_alloc_bytes`, `trigger_bytes`, and `hard_bytes`. Per-TG allocation
  flushes use relaxed atomic add, cycle-start reset uses atomic exchange to
  avoid losing a concurrent flush, and stats/hard-limit checks acquire/load or
  release/store through the helper layer.
- Routed `gcpause_pct`, the `lua_gc` pause control consumed by GC2 pacing,
  through `gc2_gcpause_pct_*` helpers and extended the M5 pacing guard so raw
  C-side access is rejected outside helper definitions.
- Routed interpreter and JIT hard-limit checks through
  `lj_gc2_hard_limit_reached()`, matching the x64 VM TSO acquire-load macros
  added with the GC total accounting slice.
- Added `tools/ci/m5_gc2_pacing_atomic.sh`, a pthread C helper stress, and a
  Lua stats/threading smoke, then wired the case into the M5 aggregate.

M5 x64 VM store publication surface:
- The invariant is documented beside the x64 VM store paths and helper layer:
  interpreter stores avoid legacy inline `barrierback`/`lj_gc_barrieruv` repair,
  array/range table stores publish through the table helper layer, `TSETS` stays
  on a C fallback rather than a direct hash-slot store, and closed
  `CSET`/`USETx` stores publish through release-copy upvalue helpers.
- Observable coverage lives in `m5_x64_tset_nil_snapshot`, the M5 x64 FORWARD
  fixtures, stock `-joff` coverage, and the runtime traceability probes in
  `m6_jit_table_store_helper`.
- The plan 07 checklist documents this helper-backed path as the x64
  replacement for the older inline `wbarrier_tv` sketch, favoring a
  conservative C-auditable publication path over a faster but more fragile
  macro expansion.

M5 metadata table-store wait stability:
- The initial metadata wait slice routed parser captured-local anchor stores,
  serializer dictionary preparation, and recorder template marker stores through
  no-`lua_State` `lj_thr_sleep_ns()` helpers when they see a forwarded slot
  after lookup or lose the slot CAS, instead of spinning on `la_cpu_pause()`.
- Follow-up serializer cleanup threads the current `lua_State` through
  `serialize_dict_storeint()`. Buffer string/metatable dictionary preparation
  now waits through `lj_thr_sleep_ns(L, ...)` after a forwarded slot or slot CAS
  loss, keeping the constructor's TG native and safepoint-visible during
  dictionary publication contention.
- Follow-up parser cleanup threads the current `lua_State` through
  `parse_keep_storebool()`. Parser string/cdata keep-anchor stores now use the
  active state when waiting after table forwarding or a slot CAS loss, so source
  parsing remains visible to safepoint handshakes during keep-table contention.
- Follow-up recorder cleanup threads the current `lua_State` through optional
  `LUAJIT_ENABLE_TABLE_BUMP` template marker stores. `rec_template_mark_nil()`
  now uses `J->L` when waiting after table forwarding or a slot CAS loss, so
  table-bump recording stays native and safepoint-visible during template-table
  marker publication contention.
- Added `tools/ci/m5_metadata_store_waits.sh` and wired it through
  `m5_parser_capture_meta.sh` and `m5_concurrent_objects.sh` so these
  metadata-store retry loops keep their no-busy-spin wait policy while the
  broader table write protocol remains pending.
- Verification: `tools/ci/m5_metadata_store_waits.sh`,
  `tools/ci/m5_parser_capture_meta.sh`, and
  `tools/ci/m5_concurrent_objects.sh` passed.

M5 central table-store wait stability:
- Added `lj_tab_store_wait_no_l()` for helper-backed table-store retry loops
  that cannot safely use a `lua_State` while waiting.
- `lj_tab_trystoretv_cas()` now yields through that helper after CAS losses,
  and the VM/JIT table-store helpers for array slots, hash slots, NEWREF, and
  TSETM range stores use the same helper when a forwarded slot forces a retry.
- Added `tools/ci/m5_tab_store_waits.sh` and wired it through the table CAS,
  JIT table-store, x64 VM-store, and M5 aggregate wrappers so these central
  store paths do not regress to `la_cpu_pause()` while lower-level lookup,
  keylock, traversal, and new-key waits remain separate follow-up work.
- Verification: `tools/ci/m5_tab_store_waits.sh`,
  `tools/ci/m5_tab_cas_store.sh`, `tools/ci/m5_jit_hash_store_nyi.sh`,
  `tools/ci/m5_tab_value_publish.sh`, and
  `tools/ci/m5_concurrent_objects.sh` passed. The x64 VM store invariant is
  covered by runtime behavior.

M5 C API/library table-store wait stability:
- Routed the remaining C API/library/JIT/FFI/debug/string/threading/ctype/meta
  table-store retry loops through `lj_tab_store_wait_no_l()` instead of
  `la_cpu_pause()`.
- Follow-up C API cleanup added `lj_tab_store_wait_l()` and routes
  `luaL_newmetatable()`, `lua_settable()`, `lua_setfield()`, `lua_rawset()`,
  and `lua_rawseti()` retry waits through the active `lua_State`. Public C API
  table setters now stay native and safepoint-visible after a forwarded slot or
  slot CAS loss; at that point the library/JIT/FFI/debug/string/threading/
  ctype/meta callers in this section remained on the no-state helper until
  cleaned up separately.
- Follow-up table-library cleanup routes `table.insert()` shift/value retry
  waits and the optional Lua 5.2 `table.pack()` `"n"` store through
  `lj_tab_store_wait_l(L)`. Standard table-library writes now keep the current
  TG native and safepoint-visible after a forwarded slot or slot CAS loss; the
  JIT/FFI/debug/string/threading/ctype/meta/library-init callers remained on the
  no-state helper until cleaned up separately.
- Follow-up stateful caller cleanup routes the remaining base/library/JIT/FFI/
  debug/string/threading/ctype/meta retry waits through `lj_tab_store_wait_l(L)`.
- Follow-up low-level table cleanup routes the generic CAS retry helper, keyed
  nil CAS helper, and x64 VM/JIT table-store helpers through
  `lj_tab_store_wait_l(L)` too, then removes the now-unused
  `lj_tab_store_wait_no_l()` wrapper. Current table-store retry waits all keep
  the current TG native and safepoint-visible without changing the public stock
  LuaJIT API or behavior; `lj_tab_wait_no_l()` remains for non-store table
  retry loops that truly cannot use a `lua_State`.
- This covers `luaL_newmetatable()`, public C API setters, base/table/library
  init stores, `jit.attach`/`jit.util`/`jit.profile` tables, FFI typeinfo,
  `_LOADED`, and miscmap stores, string metatable registration, threading env
  registration, debug activelines, ctype string stores, and
  `lj_meta_tsettv_pair()`.
- Extended `tools/ci/m5_tab_store_waits.sh` to document those named
  helpers while leaving GC, channel, and non-Linux fallback waits as separate
  follow-up work.
- Verification: `tools/ci/m5_tab_store_waits.sh`,
  `tools/ci/m5_tab_cas_store.sh`, `tools/ci/m5_tab_value_publish.sh`,
  `tools/ci/m5_jit_hash_store_nyi.sh`,
  `tools/ci/m7_ffi_typeinfo_snapshot.sh`, and
  `tools/ci/m7_ffi_clib_cache.sh` passed; the full
  `tools/ci/m5_concurrent_objects.sh` aggregate passed too. The x64 VM store
  route is documented beside the VM helper and covered through behavior.

M5 low-level table retry wait stability:
- Split out generic `lj_tab_wait_no_l()` for table retry loops that cannot
  safely use a `lua_State`; the later table-store cleanup removed the unused
  store-specific no-state wrapper after all store retries moved to
  `lj_tab_store_wait_l(L)`.
- Low-level table generation snapshots, integer get/set forwarded-array
  retries, hash lookup keylock/forwarded retry-once paths, new-key insertion
  contention, FINREG new-key claim observers, and `lj_tab_next()` hidden-key
  rechecks now wait through that helper instead of `la_cpu_pause()`.
- Keep table-generation retry waits on `lj_tab_wait_no_l()` even when a caller
  has an active `lua_State`: resize, forwarded-array, keylock, and new-key
  retries can observe transient table state and must not self-ack a safepoint
  through `lj_native_leave(L)` while those invariants are in flight. The
  L-aware wait remains limited to plain store publication waits.
- Extended `tools/ci/m5_tab_store_waits.sh` to cover the generic helper, the
  store wrapper, and the low-level retry functions. The reason these paths must
  use wait helpers rather than pause loops is documented beside the table retry
  helpers.
- Verification: `tools/ci/m5_tab_store_waits.sh`,
  `tools/ci/m5_tab_keylock_lookup.sh`, `tools/ci/m5_tab_forward_filter.sh`,
  `tools/ci/m5_tab_retire.sh`, `tools/ci/m5_tab_array_publish.sh`,
  `tools/ci/m5_tab_cas_store.sh`, `tools/ci/m5_tab_value_publish.sh`,
  `tools/ci/m5_jit_hash_store_nyi.sh`,
  and `tools/ci/m5_concurrent_objects.sh` passed. The x64 VM store route is
  documented beside the VM helper and covered through behavior.

M5 GC root/retry wait stability:
- Added `gc_root_wait_no_l()` for legacy GC root-chain splice retry losers and
  routed `gc_finreg_claim_wait_no_l()` through it, so legacy/GC2 bridge root
  unlink and sweep splice contention no longer spins on `la_cpu_pause()`.
- Follow-up legacy-GC FINREG cleanup removed the now-stale
  `gc_finreg_claim_wait_no_l()` wrapper. FFI finalizer table mark waits call
  `gc_root_wait_no_l()` directly, preserving the no-state wait behavior without
  keeping a redundant FINREG helper.
- GC2 worker-control CAS fallthrough, debug/paranoia finalizer/preclaim pause
  gates, and FINREG cdata/userdata root-unlink CAS retry loops now sleep
  through the existing GC2 peer/FINREG no-`lua_State` wait helpers instead of
  raw CPU pauses.
- Follow-up GC2 wait cleanup removed the stale finalizer- and FINREG-specific
  no-L wrappers after they became aliases of `gc2_peer_wait_no_l()`. GC2
  finalizer owner waits and FINREG claim/root-unlink waits now call the shared
  peer wait helper directly, preserving behavior without redundant helper
  names.
- Follow-up cleanup removed the stale `tab_finreg_claim_wait_no_l()`
  compatibility wrapper. FINREG table insertion claim losers now call the
  shared `lj_tab_wait_no_l()` directly, keeping the same no-state wait behavior
  without preserving a redundant guard name.
- Added GC wait coverage through `m5_concurrent_objects`. The no-`la_cpu_pause()`
  rule in `lj_gc.c`/`lj_gc2.c` and the named root, peer, finalizer, and FINREG
  wait helper ownership are documented here.
- Verification: `tools/ci/m5_gc_waits.sh`,
  `tools/ci/m7_ffi_finreg.sh`, `tools/ci/m3_gc2_worker_scheduler.sh`,
  `tools/ci/m8_weak.sh`, and `tools/ci/m5_concurrent_objects.sh` passed.

M5 keyed table-store stale-generation bridge:
- Added `lj_tab_trystoretv_cas_keyed()`, which validates a destination slot
  against the parent table and logical key before and after a CAS store.
  If the slot belongs to an old generation, a retiring generation, or a
  visible `LJ_TFORWARD` successor mismatch, the helper reports a retryable
  `LJ_TAB_STORE_CAS_STALE` instead of letting the caller return after writing
  the stale slot.
- Routed public C API setters, VM/meta slow stores, table library insert/pack
  stores, generic library registration stores, JIT helper-backed table stores,
  FFI typeinfo/module/miscmap stores, string/threading/debug/ctype store
  helpers, and TSETM range stores through the keyed helper.
- Extended `tests/t-tab-cas-store.c` with direct stale array/hash generation
  checks: CAS against a retired old array/hash slot must return `STALE`
  without overwriting the old slot, and the retry must publish into the current
  generation.
- Documented that the named public/runtime store helpers must use
  `lj_tab_trystoretv_cas_keyed()`. This remains an interim
  safety bridge; cooperative resize/helping and the final flag-gated
  RETIRING/FORWARD/CAS writer are still pending.

M5 keyed nil-CAS table-store bridge:
- Added `lj_tab_read_current_keyed()` and `lj_tab_trysetnil_cas_keyed()` for
  insert-if-absent table publications. The nil-CAS helper validates the
  destination against the parent/key before observing nil, retries stale or
  forwarded destinations, returns `EXISTS` for a concurrently published value,
  and revalidates after a successful CAS.
- Routed `luaL_newmetatable()`, parser keep-string anchors, serializer
  dictionary insertion, and recorder template markers through the helper,
  removing their raw `lj_tv_cas()` nil-slot publications.
- Extended `tests/t-tab-cas-store.c` with a stale old-hash-generation nil-CAS
  check, and documented raw `lj_tv_cas()` ownership in those named
  insert-if-absent helpers.
- Corrected the M5 GC wait guard to inspect the actual
  `gc2_worker_control_lock_l()` wait loop instead of the wrapper, matching the
  implementation that waits through `gc2_peer_wait_no_l()`.

M5 table resize KEYLOCK proof:
- Extended `tests/t-tab-keylock-lookup.c` so a hash key can be hidden behind
  `LJ_TKEYLOCK` while `lj_tab_resize()` is rebuilding the hash generation. A
  pthread release-publishes the real key after a short delay, proving resize
  waits out the transient key publication sentinel and then migrates the value
  into the successor generation while leaving the old value forwarded.
- Follow-up coverage hides an integer hash key during hash-to-array resize and
  verifies the released key/value lands in the array successor with the old hash
  slot forwarded. Moving the hash-count or migration paths before the KEYLOCK
  wait is documented here as invalid.
- This is behavior coverage for the existing resize wait path; it does not
  remove the transient wait or change the table migration protocol.

M5 table resize concurrent traversal stress:
- Extended `tests/t-tab-resize-stress.lua` with traversal observers that run
  bounded `pairs()`, `ipairs()`, and `next(t, nil)` probes while writer threads
  are actively forcing array/hash growth. The observers reject visible
  userdata/cdata internal sentinels and keep GC steps interleaved with
  traversal.
- Extended the same stress with traced read workers that repeatedly load stable
  array and hash keys while peer writers force growth/migration. This exercises
  JIT read fallbacks across resize forwarding in addition to the existing traced
  store worker.
- Restored `tools/ci/m5_tab_resize_stress.sh` as a direct validation case.
  It requires the weak-clear, GC-mark, JIT-store, JIT-read, and traversal
  stress components to stay wired before running the Lua suite case.
- The traversal observer now reports sentinel exposure through worker return
  values, and `LJ_M5_TAB_RESIZE_TRAVERSAL_MODES` can narrow the traversal
  stress to comma-separated `pairs`, `ipairs`, and `next` probes for focused
  crash reduction.
- This strengthens the resize publication proof around Lua-visible iteration.
- Follow-up traversal stress now walks a bounded `next(t, k)` chain instead of
  probing only `next(t, nil)`, so the direct iterator path must keep returning
  Lua-visible keys/values across several forwarded slots while peer writers
  resize the same table.
- Follow-up length stress keeps a stable non-nil integer prefix while peer
  writers churn higher integer and string keys across resize generations.
  Observer workers repeatedly run `#t` and `rawlen(t)` and require the reported
  border to stay at or above the stable prefix, pinning the generation-aware
  `lj_tab_len()` / `lj_tab_len_hint()` forwarding path in the Lua-visible
  stress suite.

M5 table resize finalizer liveness stress:
- Extended `tests/t-tab-resize-stress.lua` with finalizer-bearing cdata values
  that are reachable only through a table while peer writers force resize and
  the main thread drives GC. The stress asserts weak observers still see the
  same cdata and that their finalizers did not run while table-held across
  forwarding/retirement windows.
- The resize stress coverage includes the finalizer resize case and its
  `LJ_M5_TAB_RESIZE_STRESS_FIN_OBJECTS` env wiring.
- This extends liveness coverage without changing the current FORWARD/KEYLOCK
  bridge protocol.

M5 table resize metatable liveness stress:
- Extended `tests/t-tab-resize-stress.lua` with a table whose metatable and
  nested `__index` probe are reachable only from the actively resized table.
  Worker threads force array/hash growth while probing the inherited field, and
  the main thread drives GC. After full collection, weak observers must still
  see both the metatable and probe and `getmetatable(t)` / `__index` lookup must
  return the same objects.
- The resize stress coverage includes the metatable resize case so the
  aggregate keeps proving table-to-metatable reachability during resize
  forwarding/retirement windows.
- The resize fixture also accepts `LJ_M5_TAB_RESIZE_STRESS_CASES` to run a
  selected comma-separated subset while keeping the default path on all stress
  cases.
- Follow-up resize liveness coverage now keeps table keys and values only
  through a strongly referenced resizing table, while weak observers watch both
  sides. This pins GC-object hash-key reachability across resize forwarding and
  hash-vector retirement windows.

M5 table resize weak-key liveness stress:
- Extended `tests/t-tab-resize-stress.lua` with a weak-key table whose odd keys
  stay rooted while even keys become collectible. Peer workers force hash growth
  and deletion churn while the main thread drives GC; after full collection,
  rooted entries must survive and weak-observed values for collected keys must
  clear.
- The resize stress coverage includes the weak-key case and checks that it uses
  both weak-key storage and weak-value observers, keeping this GC reachability
  proof wired into the aggregate resize stress.

M5 C-closure upvalue API snapshot cleanup:
- `lj_err_argtype()` now acquire-snapshots pseudo-index C upvalues before
  deriving the observed argument type name, matching the release-published
  `lua_setupvalue()`/C-closure construction path.
- `io_file_iter()` now acquire-snapshots and validates the hidden file upvalue
  before dereferencing it. Direct or racy `debug.setupvalue()` replacement of
  that upvalue now reports the ordinary closed-file error instead of touching a
  raw shared slot as a userdata.
- Follow-up coverage mutates an `io.lines(path, "*n")` read-option upvalue to
  `"*l"` and verifies the iterator observes the replacement option. The direct
  guard now also requires option upvalues to be acquire-snapshotted before
  copying them onto the read stack.
- Extended `tests/t-cclosure-upvalue-snapshot.c` to cover those direct API
  mutation surfaces.
- Extended the same C fixture to cover pseudo-index C-upvalue writes through
  `lua_replace()` and `lua_copy()`, guarding the release-published store funnel
  used before M6 trace invalidation.
- Follow-up library helper cleanup routes the remaining direct
  `lj_lib_upvalue()` reads in `pairs`/`ipairs`, `print`, `coroutine.wrap`, and
  `table.pack` through `lj_lib_upvalue_load_acq()`. Helper comments document
  the library C-upvalue acquire-read contract.
- Follow-up behavior coverage mutates the hidden thread upvalue inside a
  `coroutine.wrap()` helper and verifies the wrapper resumes the replacement
  coroutine. This pins the direct C-upvalue mutation surface used by the x64
  fast path and the C fallback.
- Follow-up behavior coverage now exercises pseudo-index C-upvalue reads through
  `lua_rawequal()`, `lua_equal()`, and `lua_lessthan()`, and the
  helper comments require those comparison APIs to keep using acquire snapshots
  before comparing.
- Follow-up type-reader coverage now exercises `lua_isnil()`, `lua_isnone()`,
  `lua_isnoneornil()`, `lua_typename()`, `luaL_typename()`,
  `luaL_checktype()`, `luaL_checkany()`, `lua_isuserdata()`, and
  `luaL_checkudata()` through C-upvalue pseudo-indexes. Helper comments
  document the direct userdata/checkany reader constraints.
- Follow-up metatable-helper cleanup acquire-snapshots live table slots
  returned from `luaL_getmetafield()` and the registry metatable slot consumed
  by `luaL_testudata()`, avoiding direct copies or tag tests from a mutable
  table slot after the lookup has returned.
- Follow-up `print()` cleanup acquire-snapshots the current environment's
  `tostring` slot before copying it or checking the native fast path, closing
  another direct live-slot read after `lj_tab_getstr()` returns.
- Follow-up table-helper coverage now exercises `luaL_ref()` and
  `luaL_unref()` with the reference table supplied through a C-upvalue
  pseudo-index, pinning their raw table mutations to the same acquired
  pseudo-index path as the lower-level C API table helpers.
- Follow-up coverage pins pseudo-index number-to-string coercion through
  `index2adr_cupvalue_store_rel()`, so `lua_tolstring()` / `luaL_checklstring()`
  style readers that coerce a C upvalue keep using the trace-flushing,
  release-published mutation funnel.
- Restored `tools/ci/m5_upvalue_publish_gc.sh` as a direct validation case
  for C-upvalue acquire reads, release-published stores, C-closure
  construction, hidden `io.lines()` file/read-option upvalue snapshots, and the
  behavior fixture.
- Follow-up aggregate parity migrates the remaining C-upvalue behavior checks
  from the direct wrapper into the aggregate-included
  `m5_upvalue_publish_gc` Lua suite case. Aggregate execution now covers
  `print()` environment snapshots, `io.lines()` read-option snapshots,
  `lj_err_argtype()` acquire reads, comparison/type-check pseudo-index reads,
  `luaL_getmetafield()`, `luaL_testudata()`, and number-to-string C-upvalue
  publication. The C fixture now also probes `luaL_checkoption()` through a
  pseudo-index C upvalue.

M5 table hash-node TValue snapshot wrapper restoration:
- Restored `tools/ci/m5_tab_slot_snapshot.sh` as a direct validation case
  for acquired hash-node key/value visibility decisions, table length slot
  snapshots, `table.maxn` hash scans, and the behavior fixture.

M5 table node-header wrapper restoration:
- Restored `tools/ci/m5_tab_nodehdr.sh` as a direct validation case for
  `TabNodeHdr` helper shape, nilnode header initialization, resize-time
  `next_gen`/`RETIRING` publication ordering, and the behavior fixture.

M5 table chain-order wrapper restoration:
- Restored `tools/ci/m5_tab_chain_order.sh` as a direct validation case
  for acquire/release `Node.next` walks, no-move collision insertion, max-chain
  resize fallback, and the behavior fixture.

M5 empty-hash table wrapper restoration:
- Restored `tools/ci/m5_tab_emptyhash.sh` as a direct validation case for
  early empty-hash lookup/insert exits, shared nilnode insertion assertions,
  acquire-read fixture links, and the behavior fixture.

M5 x64 table wrapper restoration:
- Restored direct `tools/ci` entrypoints for x64 TGET array headers,
  TGETS/TSETS node ordering, `ipairs_aux`, `BC_ITERN`, and TSET nil snapshots,
  all routing to their existing Lua-suite behavior fixtures.

M5 stock API boundary:
- Keep the public C API as close to stock LuaJIT as possible. The old-compat
  cleanup target is fork-specific or unsupported compatibility surface, not
  stock LuaJIT entry points such as `lua_equal()`, `lua_lessthan()`,
  `lua_objlen()`, or `lua_cpcall()`.
- Restored the stock LuaJIT auxlib/API compatibility surface removed by earlier
  cleanup commits: `luaL_openlib()`, `luaL_register()`, `luaL_typerror()`,
  the integer-cast auxlib macros, `luaL_putchar`, `lua_strlen`, `lua_open`,
  `lua_getregistry`, `lua_getgccount`, `lua_Chunkreader`, and
  `lua_Chunkwriter`.
- Current bytecode compatibility boundary: earlier M5 notes about loading
  legacy v2/v3 dumps are superseded by the current `plan/10_bytecode_compat.md`
  rule. The lockless loader accepts current v4 dumps and stock-compatible
  v2/v3 dumps, rejects lockless-only cell opcodes in old dump versions, and
  refuses to re-dump legacy-loaded functions.

M5 gcroot helper cleanup:
- Added `lj_gcroot_ref()` / `lj_gcroot_acq()` / `lj_gcroot_rel()` plus
  base-metatable and metamethod-name helpers for `global_State.gcroot[]`.
  Runtime C-side I/O roots, threading environment roots, metamethod-name
  publication, base-metatable loads/stores, and legacy/GC2 root scans now go
  through the helper layer instead of open-coded `g->gcroot[]` loads/stores.
- The remaining direct `gcroot[]` uses are helper bodies, generated x64 VM
  gcroot access, or recorder offset metadata that needs the fixed field offset.

M5 fixed-root helper cleanup:
- Added helper boundaries for the fixed main/vm thread roots, registry root,
  and legacy `g->gc.root` object chain. Startup publication, shutdown checks,
  legacy-GC root scans, GC2 root scans, FINREG root unlink helpers, sweep-anchor
  setup, and root-list CAS publication now use those helpers instead of direct
  C-side root loads/stores.
- The public LuaJIT API surface is unchanged. The old internal `mainthread()`
  and `vmthread()` macros remain as helper-backed aliases for stock-source
  closeness, but direct raw `mainthref`/`vmthref` dereferences are no longer
  part of the C-side access pattern.

M5 registry TValue helper cleanup:
- Added acquire-load/release-store helpers for the registry `TValue` root and
  routed ordinary C-side registry-table readers through `lj_registry_tab_acq()`.
  Registry initialization and `LUA_REGISTRYINDEX` replacement publish the root
  through `lj_registry_*_rel()` helpers instead of open-coded raw slot stores.
- `registry(L)` remains as the internal address helper for the stock Lua C API
  pseudo-index path, but read APIs snapshot `LUA_REGISTRYINDEX` with an acquire
  load before inspecting or copying the value.

M5 spawned-TG PRNG derivation:
- Added `lj_prng_derive()` and routed spawned/attached TG setup through
  `lj_tg_derive_prng()` after the TG receives its tid. Child TGs now derive
  their initial `math.random()` stream from the current parent TG seed plus the
  child tid instead of cloning the same bootstrap global PRNG snapshot.
- Explicit `math.randomseed()` remains local to the calling TG. The existing
  `m5_math_random_tg` behavior test now also checks that two unseeded spawned
  workers do not start with the same random pair.

M5 legacy TG tmpbuf root scan:
- Routed legacy/paranoia arena raw-root marking for per-TG temporary buffers
  through the registered TG list instead of only `G2TG(g)`. This matches the
  GC2 root scanner and keeps live or not-yet-reclaimed dead TG tmpbuf backing
  storage marked while arena ownership transfer is still pending.

M5 Windows os.date reentrancy:
- Routed `os.date()` broken-down time conversion through a small helper that
  keeps POSIX on `gmtime_r()`/`localtime_r()` and uses Windows
  `gmtime_s()`/`localtime_s()` into a caller-owned `struct tm`. Unsupported
  non-POSIX/non-Windows targets keep the legacy fallback outside the current
  x86_64 Linux/macOS/Windows focus.
- This extends the existing `t-os-reentrant.lua` behavior target to the Windows
  platform build instead of leaving Windows on the shared static
  `gmtime()`/`localtime()` buffer.

M5 locale mutation activation gate:
- Mutating `os.setlocale()` now serializes with secondary-thread entry through
  `mt_gc_exclusive`. The visible rule is unchanged: locale queries remain
  allowed, mutation remains stock before threading activation, and mutation is
  rejected once the VM has activated secondary Lua threads.
- Follow-up: the rejection also covers the `mt_entering` window before a
  secondary entrant publishes `mt_live`. This keeps process-global locale
  mutation from overlapping attach/spawn handoff state while preserving stock
  single-threaded mutation and query behavior.

M5 table.clear entering-window route:
- `lj_tab_clear()` now treats `mt_entering` like a shared-table boundary and
  routes through the structural-owner/keyed-CAS clear path before `mt_active`
  is latched. Single-threaded clears still use the raw path. The focused
  `t-tab-clear-entering.c` fixture pins both cases with test-helper telemetry.

M5 table.insert entering-window route:
- `table.insert()` now uses the same shared-table predicate as table clear:
  `mt_active || mt_entering`. The entering window already has a secondary VM
  entry in progress, so append/pre-grow and positional shifts acquire the
  per-table structural owner before shifting slots. `t-table-insert-entering.c`
  exercises append plus positional insert with `mt_entering` set and verifies
  both calls acquired the structural owner without latching `mt_active`.

M5 table-library shift resize stress:
- Extended `tests/t-tab-resize-stress.lua` with a `tablelibshift` case. It
  runs `table.remove()`, positional `table.insert()`, and in-place
  `table.move()` over a dense array while sibling threads churn hash keys to
  force resize, then scans the array/table to reject sparse-prefix loss or
  exposed internal migration sentinels.
- This complements the existing `tablelib` insert-marker liveness case and
  gives the table-library shift paths their own resize-forwarding proof without
  weakening the inserted-marker survival assertion.

M5 JIT iterator resize stress:
- Extended `tests/t-tab-resize-stress.lua` with a `jititer` case. It heats
  iterator-heavy JIT loops while sibling workers grow and prune array/hash
  generations, covering `pairs`, `ipairs`, direct `next`, and stable prefix/hash
  reads under ordinary racy table mutation.
- The case only asserts stock-compatible safety properties for concurrent
  mutation: no internal migration sentinel is exposed as a Lua value, a stable
  hash anchor remains reachable, and the stable array prefix survives the churn.

M5 metamethod dispatch resize stress:
- Extended `tests/t-tab-resize-stress.lua` with a `metadispatch` case. It keeps
  an `__index` table and `__newindex` rawset handler reachable only through the
  actively resized table while sibling writers churn array/hash generations.
- Observer workers repeatedly read an inherited probe, write unique keys through
  `__newindex`, traverse a bounded prefix of the table, and reject internal
  migration sentinels. After GC, the metatable, fallback table, inherited value,
  and inserted markers must still be live through the resized table.
- This pins Lua-visible metamethod lookup/write dispatch across resize
  forwarding without strengthening racy user-table ordering beyond stock
  Lua/LuaJIT semantics.

M5 JIT/metamethod resize runtime follow-up:
- Kept recorded `rawset()` out of traces for this slice. The VM path remains
  stock-visible, while traced helper stores still need generation-following side
  exits before explicit raw hash/array installation can be safely re-enabled.
- Trace recording/start/stitch is enabled again under active MT for non-table
  traces, so secondary TGs can record/re-enter numeric root and side traces.
  The remaining temporary fence now lives in the recorder's shared-table paths:
  non-trace-local `TGET*`/`TSET*` and `next()` traversal stay interpreted once
  the VM has activated secondary TGs. `TNEW`/`TDUP` allocation recording is
  enabled again under active MT, trace-local table operations still record, and
  stock single-threaded table JIT behavior is unchanged.
- Table resize publication and `table.insert()`'s compound shift now share a
  structural token under active MT. Resize still performs allocation outside the
  token, then owns the publication/retry window; table.insert pre-grows the
  destination and holds the token only across the length/shift/store compound
  operation.
- Array readers that land on an old-generation `FORWARD` slot now follow the
  next array, but retry if the next-generation slot is still nil while the table
  root still points at the old array. This closes the transient length/slot
  shrink that let table-library markers disappear during migration.
- GC2 current-trace, retired clib cache, and callback-root scans now use the
  public SSB marking path when they run as handshake roots. This avoids
  multi-mutator root scans pushing directly into the single-owner grey deque.
- Focused metadispatch stress now covers `__index` lookup, `__newindex`
  publication, bounded traversal, and GC liveness through active table resize
  with JIT enabled.
- Verification:
  - focused optimized `jititer`: 10/10 direct iterations after removing the
    blanket active-MT trace-start gate;
  - focused optimized `tablelib`: 30/30 direct iterations;
  - full direct M5 resize stress: 8/8 all-case iterations after restoring
    active-MT `TNEW`/`TDUP` recording with the shared-table fence still active;
  - `LJ_TEST_DISABLE_BUILD_CACHE=1 tools/ci/lua_test.sh m5_tab_resize_stress`.

M5 panic callback publication:
- Replaced raw target-runtime `global_State.panic` reads/writes with
  `panicf_load()`, `panicf_store()`, and `panicf_xchg()` helpers. Fatal
  error paths now acquire-load the handler, `luaL_newstate()` release-publishes
  the default handler, and `lua_atpanic()` performs one atomic exchange while
  preserving stock previous-handler semantics.
- Added `m5_panic_callback_atomic` with a C fixture for the public API exchange
  contract. Helper comments document the `global_State.panic` ownership
  boundary; runtime fixtures own the observable behavior.

M5 C wrapper callback publication:
- `luaJIT_setmode(..., LUAJIT_MODE_WRAPCFUNC|LUAJIT_MODE_ON)` now claims the
  target state while reading the stack lightuserdata, release-publishes
  `global_State.wrapf` through `wrapf_store()`, drops the claim, and then
  publishes `BC_FUNCCW`. The generated x64 VM still performs the established
  field call, but the C-side publication is no longer a raw store racing with
  external C function entry.
- Added `m5_wrapcf_func_publish` to document raw C-side `->wrapf` access and pin
  the `wrapf_store()` before `BC_FUNCCW` publication order.

M5 read-only C API owner claims:
- `lua_isyieldable()` now claims the target state before reading `L->cframe`,
  matching the already-claimed `lua_status()` and `lua_gettop()` public
  read-only APIs. `m5_api_debug_claim_cleanup` now rejects regressions in that
  claim/read/drop boundary.

M5 JIT setmode owner claims:
- `luaJIT_setmode()` FUNC/ALLFUNC/ALLSUBFUNC modes now acquire the JIT token,
  claim the target state while resolving the function/prototype slot and
  mutating prototype JIT flags, then drop the claim before publishing the
  scoped trace-flush boundary. This removes the remaining raw stack read in
  those setmode modes without holding a state claim across the safepoint
  handshake.
- `m5_wrapcf_func_publish` now also pins the FUNC-mode token/claim/mutate/drop
  ordering.
