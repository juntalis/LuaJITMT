# M9 Notes

Performance closing setup:

- Added `threading.gcstats()` as a read-only benchmark telemetry surface.
  It returns a table with existing GC2 counters for cycle requests/starts,
  allocation pacing, assist and worker progress, owner sweep progress, weak
  clearing, finalizer queueing, and live estimates. This wires the stats called
  out in `plan/13_testing_and_benchmarks.md` without changing collector pacing or
  phase behavior.
- Added `tests/t-gc-stats.lua` and `tools/ci/m9_gc_stats.sh`, covering the stats
  table under interpreter and default-JIT modes. The multi-thread benchmark now
  prints a stable subset of these fields while retaining a fallback for older
  binaries.
- The stats guard now also documents why raw production access to GC2 sweep-live
  estimate fields: `sweep_live_updates`, `sweep_live_huge_bytes`, and
  `live_estimate`. Stats and pacing read them through `gc2_*` helpers, matching
  the sweep-close publisher.
- Added safepoint poll-ack latency telemetry. Each handshake records a monotonic
  publish timestamp, and each successful TG ack contributes sample/sum/max
  nanosecond counters exposed through `threading.gcstats()`.
- Added a power-of-two poll-ack latency histogram to `threading.gcstats()`;
  `bench_mt.lua` now reports approximate P99 bucket bounds from per-benchmark
  histogram deltas.
- Exposed the remaining FINREG/finalizer bridge telemetry through
  `threading.gcstats()`: cdata/userdata FINREG registrations, queueing,
  P_WEAK/sweep fallback counters, ordered FINREG scan counters, weak-write
  marks, MPSC finalizer drains, finalizer enter/leave counts, sweep blocks, and
  finalizer-spawn deferrals. `tests/t-gc-stats.lua` now requires these fields
  and checks their monotonicity, while `bench_mt.lua` prints the most useful
  fallback counters for performance runs.
- Tightened the `threading.gcstats()` result construction so every scalar
  field and latency bucket entry is CAS-published, the nested latency bucket
  table reference is explicitly published to the parent result table, and the
  completed stats table is republished before returning it to Lua. The old
  direct stats-slot write pattern is documented as obsolete, and `m9_gc_stats`
  covers the observable telemetry surface.
- Added `threading.now()` as the monotonic wall clock used by `bench_mt.lua`.
  The multi-thread harness now honors `BENCH_SCALE`, clamps zero-duration smoke
  runs, and skips pairwise channel benchmarks for odd thread counts instead of
  hanging the documented 1-thread scaling run. The synchronized benchmark
  runner accepts `BENCH_THREADS` and `BENCH_FILTER`, and
  `tools/ci/m9_bench_smoke.sh` exercises the scaled 1/2-thread benchmark path.
- Tightened `bench_mt.lua` to require `threading.now()` directly and reject
  `os.clock`, so multi-thread scaling reports cannot silently use process CPU
  time instead of monotonic wall time.
- Added `tools/ci/m9_m10_gc.sh` as the current GC telemetry/generational
  aggregate. The M9 stats smoke, M9 benchmark smoke, and M10 generational guard
  now each reject being left out of that aggregate.
- Exposed parked-worker scheduler telemetry through `threading.gcstats()`
  and the benchmark report: worker busy retries, idle declarations, wake
  publications, park events, and async progress. `tests/t-gc-stats.lua` now
  requires those fields and checks monotonicity alongside the existing worker
  drain counters.
- Moved `threading.gcstats()` GC2 reads behind `GC2StatsSnapshot` and
  `lj_gc2_stats_snapshot()`. `lib_base.c` still owns Lua table construction and
  CAS-published result slots, but GC2 owns the acquire-load boundary for phase,
  pacing, worker, sweep, weak, FINREG, finalizer, and latency counters.
- Removed the temporary `lj_gc2_worker_drain_progress()` compatibility alias.
  `lj_gc2_worker_drain()` already returns saturated total progress for grey
  work, SSB conversion, weak draining, and sweep batches, so all internal
  callers and guards now use the single worker-drain surface.
- Removed the remaining close-time FINREG generation-table pending/discovery
  scans. Every enabled `ffi.gc` registration publishes an ordered FINREG node,
  so `lj_gc2_finreg_cdata_pending()` and close-time cdata separation now use
  the ordered list directly. A later M8 cleanup removed the P_WEAK root-list
  fallback for preclaim side-vector failure; that branch now queues the same
  ordered cdata object directly. The sweep/free cdata path was later tightened
  to a fatal FINREG invariant tripwire rather than a defensive
  missed-publication queue.
- Tightened FINREG ordered publication after removing those scans:
  `cdata_fin_store()` now CAS-publishes the ordered node while the FINREG slot
  still contains the claim sentinel, before setting the cdata FIN bit and
  release-storing the finalizer function. Ordered scans already wait out claim
  sentinels, so this avoids a candidate-visible window without restoring a
  generation-table fallback.
- Added canonical single-thread benchmark accounting for the M6/M9 performance
  gate. `bench/compare_baseline.sh` compares two `bench/run_baseline.sh` CSVs,
  fails missing or extra benchmark rows, computes the geomean over the
  `jit_ns_per_op` column by default, and enforces the documented `<= 1.10`
  threshold.
- Resynced the documented `auxiliary/bench/` harness with the active M9 benchmark
  controls from `plan/auxiliary/bench/` and retargeted benchmark CI plus
  `bench/run_baseline.sh` back to `auxiliary/bench`. `tools/ci/m9_bench_smoke.sh`
  now rejects drift between the two copies. The harness supports `BENCH_SCALE`
  for smoke runs and `BENCH_GC_MODE` for generational/incremental probes, while
  canonical full runs still use default iteration counts.
- `tools/ci/m9_bench_regression.sh` self-checks the committed pinned baseline,
  verifies a synthetic regression fails, generates a scaled current CSV, and is
  wired into the M9/M10 aggregate.
- Verification for the `auxiliary/bench` sync: clean `make -C src -j$(nproc)`,
  `tools/ci/m9_bench_smoke.sh`, `tools/ci/m9_bench_regression.sh`, and
  `tools/ci/m9_m10_gc.sh` pass.
- 2026-06-26 benchmark-evidence attempt on a 24-core/32-thread
  i9-13900F host: running `bench/run_baseline.sh src/luajit` with
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626.csv` was interrupted after
  the full current run spent more than 20 minutes in the `-joff` phase without
  producing the CSV. Isolating the scaled closure/upvalue slice exposed an
  active-thread root regression from eager thread traversal during mark
  start/atomic: a forced full GC could drop standard-library globals such as
  `collectgarbage`. Added `m3_gc_active_thread_roots` as the focused coverage.
  The larger `BENCH_SCALE=0.01 ... closures_upval` timeout also reproduces on
  `HEAD^`, so the canonical full-run geomean report remains pending as a
  separate benchmark-performance blocker.
- Resolved the scaled `closures_upval` blocker by keeping legacy open upvalues
  in the thread root traversal and preventing GC2 owner arena sweep from
  reclaiming traversable bodies before the legacy root sweep has published its
  close boundary. Closed upvalue bodies now use the accounting-only deferred
  body path, preserving stale root-chain `nextgc` links until a later legacy
  sweep can splice them. `m9_bench_smoke` now includes
  `BENCH_SCALE=0.01 ... closures_upval` as a regression guard; direct
  verification completed in about 0.03s for that scaled slice.
- Resolved the next full-baseline blocker where the interpreter
  `alloc_tables` row could run for minutes. The root cause was twofold:
  legacy `GCthreshold` was still republished from the old absolute
  `estimate*pause/100` formula, and trace-side GC checks could reach the GC2
  hard limit before publishing a GC2 cycle request. GC2 now publishes an idle
  legacy threshold from current total plus remaining GC2 allocation budget,
  uses a 256 MiB tiny-heap trigger floor to avoid repeated arena-footprint
  sweeps on short-lived churn, and shares the trigger check with interpreter
  and JIT GC-step bridges. `m9_bench_smoke` now includes a scaled
  `alloc_tables` interpreter guard.
- 2026-06-26 full single-thread CSV evidence now completes:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-gc2pacing.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against
  `bench/baseline_372b369b9afd_.csv` still fails the JIT geomean gate at
  2.006834x. Remaining high-ratio JIT rows include `fib30` 6.61x,
  `tab_array` 7.05x, `ffi_struct` 4.21x, `sbuf_format` 3.24x,
  `tab_hash_write` 2.68x, and `closures_upval` 2.55x.
- 2026-06-26 follow-up after the traced array no-GC helper split:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-array-nogc.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.873482x, but `tab_array`
  improved to 10.02 ns/op, 6.59x baseline, from the prior 10.71 ns/op, 7.05x
  run. Focused rows observed `tab_array` at 10.02 ns/op and `tab_hash_write` at
  41.54 ns/op; hash-store helper routing remains the full barrier helper.
- Stock `misc/gcstep.lua` exposed a no-upvalue interpreter `FNEW` pacing gap
  after the GC2 idle threshold was raised to the real trigger floor. GC2 now
  maintains a helper-side idle totalbytes soft limit for safe interpreter
  helpers; `lj_func_newL_gc()` uses it only for no-upvalue FNEWs, disables it
  while the forced legacy step runs, and lets idle publication rearm it. This
  fixes the stock FNEW GC-step guard without reintroducing trace-side helper GC
  or slowing upvalue-heavy closure loops.
- 2026-06-26 follow-up after root trace vmstate publish and no-upvalue FNEW
  helper pacing: `BASELINE_OUT=/tmp/lj-lockless-current-20260626-rootvmstate-softfnew.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.973892x. Remaining
  high-ratio JIT rows include `tab_array` 6.59x, `fib30` 5.82x, `ffi_struct`
  4.16x, `sbuf_format` 3.24x, `tab_hash_write` 2.64x, and `closures_upval`
  2.54x. Focused probes kept `alloc_tables` at 0.38 ns/op and observed
  `tab_hash_write` at about 44 ns/op after removing the trace-side soft check.
- 2026-06-26 table helper current-generation fast paths:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-table-fastpaths.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.961406x, but
  `tab_array` improved to 9.27 ns/op, 6.10x baseline. The remaining high-ratio
  rows are still dominated by `tab_array`, `fib30` 5.97x, `ffi_struct` 4.21x,
  `sbuf_format` 3.24x, `tab_hash_write` 2.62x, and `closures_upval` 2.56x.
- 2026-06-26 x64 inline runtime-TG tmpbuf reset:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-inline-tmpbuf.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.898071x, but the
  canonical `sbuf_format` row improved to 187.03 ns/op, 3.21x baseline, after
  traced string-format/concat tmpbuf resets stopped calling `lj_buf_tmp_reset`.
  Remaining high-ratio rows include `tab_array` 6.11x, `fib30` 6.09x,
  `ffi_struct` 4.13x, `closures_upval` 2.55x, and `tab_hash_read` 1.41x.
- 2026-06-26 x64 inline numeric array-store CAS:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-array-inline-cas.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.876161x, but
  `tab_array` improved to 8.28 ns/op, 5.45x baseline, after separated
  current-generation numeric `ASTORE` stopped calling
  `lj_tab_storetv_forjit_array_nogc()` on the hot success path and uses an
  inline `lock cmpxchg` with helper fallback. Remaining high-ratio rows include
  `fib30` 6.03x, `ffi_struct` 4.16x, `sbuf_format` 3.28x,
  `closures_upval` 2.55x, `tab_hash_write` 1.76x, and `tab_hash_read` 1.41x.
- 2026-06-26 x64 inline numeric hash-store CAS:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-hash-inline-cas.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.778314x, but
  `tab_hash_write` improved to 28.34 ns/op, 1.53x baseline, after existing-slot
  numeric `HSTORE` stopped calling `lj_tab_storetv_forjit_hash()` on the hot
  success path and uses an inline `lock cmpxchg` with helper fallback.
  Remaining high-ratio rows include `tab_array` 5.45x, `ffi_struct` 4.13x,
  `fib30` 3.74x, `sbuf_format` 3.21x, `closures_upval` 2.56x, and
  `tab_hash_read` 1.41x.
- 2026-06-26 local-cell skipped discarded pre-FNEW promotion:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-cell-promote-skip.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.768476x, but
  `closures_upval` improved to 94.12 ns/op, 2.44x baseline, after root traces
  stopped pre-promoting mutable local-cell slots that are overwritten by an
  earlier destination bytecode before `FNEW`. Remaining high-ratio rows include
  `tab_array` 5.45x, `ffi_struct` 4.13x, `fib30` 3.72x, `sbuf_format` 3.25x,
  and `tab_hash_read` 1.41x.
- 2026-06-26 same-slot table store/load forwarding:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-store-forward.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.777971x, but
  `tab_array` improved to 4.96 ns/op, 3.26x baseline, from the prior
  8.28 ns/op, 5.45x baseline, after `ALOAD`/`HLOAD` forwarding learned to
  recover same-slot stores when lockless table-header reloads force fresh
  `AREF`/`HREF` refs. Remaining high-ratio rows include `fib30` 6.35x,
  `ffi_struct` 4.13x, `sbuf_format` 3.20x, `closures_upval` 2.47x,
  `tab_hash_write` 1.52x, and `tab_hash_read` 1.41x.
- 2026-06-26 recorder same-slot store/read forwarding:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-store-read-recorder.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.771610x, but
  `tab_array` improved again to 4.85 ns/op, 3.19x baseline, after the recorder
  stopped emitting the second read-side array header/bounds guard group for an
  immediate same-slot read after a non-nil `ASTORE`/`HSTORE` in the current
  poll/loop region. Remaining high-ratio rows include `fib30` 6.47x,
  `ffi_struct` 4.08x, `sbuf_format` 3.22x, `closures_upval` 2.44x,
  `tab_hash_write` 1.52x, and `tab_hash_read` 1.40x.
- 2026-06-26 Linux/x64 TG-only trace-head vmstate publish:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-tgvmstate-only.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.754935x, but this
  removes the global vmstate absolute-address/store pair from every x64 trace
  entry while keeping the TG vmstate publish used by trace exit/unwind and GC
  trace-root marking. `tab_array` measured 4.82 ns/op, 3.17x baseline, and
  `fib30` measured 30.41 ms/op, 5.82x baseline in the full run; focused
  `fib30` probes remain noisy but observed 23.29 ms/op after the trace-head
  store removal. Remaining high-ratio rows include `ffi_struct` 4.13x,
  `sbuf_format` 3.21x, `closures_upval` 2.45x, `tab_hash_write` 1.52x, and
  `tab_hash_read` 1.40x.
- 2026-06-26 hotcall-gated regular function-entry safepoint poll:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-hotcall-funcf-poll.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.753295x. This is only a
  small benchmark move from the TG-only trace-head run, but it removes the
  per-entry non-`JFUNCF` poll from JIT builds and keeps recursive no-loop
  safepoint progress through hotcall counter underflow. Full-run rows measured
  `fib30` at 30.96 ms/op, 5.93x baseline, `tab_array` at 4.77 ns/op, 3.14x
  baseline, `ffi_struct` 4.11x, `sbuf_format` 3.19x,
  `closures_upval` 2.45x, `tab_hash_write` 1.52x, and `tab_hash_read` 1.41x.
- 2026-06-26 immutable self-upvalue recording:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-self-upvalue-rerun.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.756721x, within the
  current run-to-run noise band. This experiment removed `UREFC`/`ULOAD` from
  immutable self-recursive local-function calls; the full-run `fib30` row
  measured 29.52 ms/op, 5.65x baseline, versus 30.96 ms/op in the prior
  hotcall-gated CSV. The later M6 safety pass intentionally narrowed this:
  local-cell function-valued upvalue reads must stay on the explicit
  `UREFC`/`ULOAD` path before any immutable self shortcut is considered.
- 2026-06-26 TG tmpbuf raw helper lowering:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-tg-tmpbuf.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.761724x, within the
  current noisy band, but traced `string.format("%d:%s", i, "x")` mcode now
  calls `lj_strfmt_putint_tg`, `lj_buf_putstr_tg`, and `lj_buf_tostr_tg`
  instead of the generic shared-SBuf helpers. The full-run `sbuf_format` row
  measured 188.91 ns/op, 3.24x baseline; a focused same-build row measured
  185.48 ns/op. Remaining high-ratio rows include `fib30` 6.20x,
  `ffi_struct` 4.24x, `tab_array` 3.16x, `closures_upval` 2.43x, and
  `tab_hash_write` 1.49x.
- 2026-06-26 x64 array-store CAS array-operand gate:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-array-cas-arrayref.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.751406x, but the
  numeric `ASTORE` inline CAS gate now reuses the `AREF` array operand for its
  colocated, retiring-header, and current-generation checks instead of
  reconstructing the base from slot/key. The full-run `tab_array` row measured
  4.78 ns/op, 3.14x baseline; focused same-build samples were stable at
  4.78-4.79 ns/op.
- 2026-06-26 x64 inline constant TG `BUFPUT`:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-inline-const-bufput.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline still fails the JIT geomean gate at 1.735023x, but constant
  `BUFPUT` into a proven runtime TG tmpbuf now stores short literals inline
  after a raw slack check and keeps `lj_buf_putstr_tg()` as the cold resize
  fallback. The full-run `sbuf_format` row measured 182.63 ns/op, 3.13x
  baseline, and dynamic hash-key rows measured `tab_hash_write` 27.18 ns/op
  and `tab_hash_read` 24.15 ns/op.
- 2026-06-26 TG tmpbuf `BUFSTR` length elision:
  `BASELINE_OUT=/tmp/lj-lockless-current-20260626-bufstr-len.csv
  bench/run_baseline.sh src/luajit` wrote a current CSV. Comparing against the
  pinned baseline now passes the documented single-thread JIT geomean gate at
  1.094243x. The `BC_LEN` recorder now replaces length-only `BUFSTR` results
  rooted at `BUFHDR LREF RESET` with `lj_buf_len_tg_forjit(final_buf)`, so the
  append chain still executes but unused final strings are not interned. The
  full-run `sbuf_format` row measured 4.64 ns/op, and the concat-heavy
  `string_intern` row measured 4.93 ns/op. Remaining high-ratio rows include
  `fib30` 5.94x, `ffi_struct` 4.34x, `tab_array` 3.14x, and `closures_upval`
  2.42x.
- 2026-06-26 real-core M9 scaling and poll-latency accounting:
  `bench_mt.lua` now keeps `tab_read-shared` focused on shared table reads by
  using prebuilt string keys, while the old mixed key-construction workload is
  preserved as `tab_read-keybuild`. `intern-MT` now materializes strings through
  a small keep ring so the length-only `BUFSTR` elision does not erase the
  intended intern stress. On the x86_64 i9-13900F host (24 physical cores, 32
  CPUs online), focused `BENCH_SCALE=10` runs measured `arith-MT` at
  2.610e9 ops/s on 1 thread and 1.651e10 ops/s on 8 threads (6.33x), and
  `tab_read-shared` at 6.742e8 ops/s on 1 thread and 4.270e9 ops/s on
  8 threads (6.33x). `gc_churn-MT` on 8 threads completed at 3.540e6 ops/s
  with zero owner-side poll-ack samples after synthetic leader/native acks were
  removed from the poll-latency histogram; no mutator-observed poll pause was
  recorded in that run.
- 2026-07-02 traced FNEW duplicate GC-check removal:
  `lj_func_newL_gc_forjit()` now relies on the trace-side `CALLA` allocation
  GC check instead of repeating `lj_gc_check_fixtop()` inside the helper.
  Focused `BENCH_SCALE=0.05 src/luajit auxiliary/bench/bench.lua closures_upval`
  improved from about 834 ns/op in the same session to about 459 ns/op; the
  GC-stopped microprobe improved from about 121 ns/op to about 106 ns/op.
  Stock `computations.lua` and the local-cell JIT guards still pass.
- 2026-07-02 numeric FNEW slot-sync lowering:
  x86/x64 mcode now inlines numeric `lj_func_syncslot_forjit()` by storing
  directly to `BASE[slot]` and skips the dead numeric `TMPREF` materialization.
  The focused closure benchmark remains noisy under active GC, but repeated
  probes kept best `BENCH_SCALE=0.05 ... closures_upval` around 444 ns/op and
  the GC-stopped microprobe reached about 93 ns/op.
- 2026-07-02 trace GC2 hard-assist cadence:
  traced allocation GC checks now compare against `gc2.hard_check_bytes`, a
  batched next-assist threshold reset with GC2 pacing and cycle allocation
  accounting. Interpreter hard assists and forced `hard_bytes < 32 KiB`
  emergency checks keep the previous immediate behavior. The closure-upvalue
  counter probe dropped from 271 assists per 100k traced closure allocations
  to 17 assists for about 9.3 MiB allocated, and focused
  `BENCH_SCALE=0.05 ... closures_upval` measured about 87-88 ns/op, matching
  the GC-stopped probe in this session. Added
  `m9_trace_hard_assist_cadence` to reject the old one-assist-per-flush
  behavior.
- 2026-07-03 same-trace numeric FNEW immediate-call cleanup:
  Fresh helper-backed FNEW calls no longer reload `func.pc`, and the first
  immediate numeric `UGET` forwards the helper argument while mutable `USET`
  still stores the real heap upvalue. The focused dump shrank from 537 to 492
  bytes of mcode for the local closure loop, but a same-session
  `BENCH_SCALE=0.05 ... closures_upval` sample still measured 72.01 ns/op
  versus stock 43.86 ns/op. The remaining gap is still allocation/GC-side
  rather than this initial upvalue load.
- Follow-up: the same helper proof now emits immediate child `UREFC` without a
  closedness guard, because `lj_func_newL_gc1num_forjit()` always creates or
  receives a closed cell for this one-upvalue numeric shape. Focused mcode
  shrank again to 465 bytes.
- Correctness follow-up: the numeric helper must not actually inherit a
  previously promoted parent slot cell when the trace stores closures across
  loop iterations. It now snapshots the numeric argument into a fresh `GCupval`
  for every FNEW, restoring ordinary closure/upvalue identity. A same-session
  `BENCH_SCALE=0.05` sample measured 100.64 ns/op versus stock 44.04 ns/op
  after the fix; this is the required cost of real per-closure cells until a
  future optimization can safely batch allocation/publication or implement full
  snapshot-aware closure sinking.
- Publication follow-up: the numeric FNEW helper now publishes its freshly
  initialized closure/upvalue pair with one root-pending chain operation instead
  of two independent new-root publications. This reduces publication traffic
  without changing the benchmark's dominant cost; same-session focused
  `closures_upval` timings remain roughly 100 ns/op for the fork versus about
  40 ns/op for stock.
- Harness follow-up: `m9_bench_stock_compare` now passes an explicit
  `BENCH_SCALE` to both stock and fork subprocesses. The default is the short
  CI sanity scale (`0.001`), while release/performance runs can opt into a
  larger pass with `LJ_BENCH_STOCK_SCALE=<factor>`. This keeps the normal suite
  focused on build/run/test signal instead of turning it into a long benchmark
  run.
- Harness status follow-up: LuaJIT 5.1 `io.popen(...):close()` reports
  successful `pclose(3)`, not the child process exit status. The suite capture
  helpers now append a private shell status marker and strip it before returning
  output, so failed benchmark subprocesses and `timeout` kills surface as
  command failures instead of later CSV parse errors.
