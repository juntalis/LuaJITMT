# M0 Notes

- Source pin from plan 00: LuaJIT `b925b3e3fc6771171602323b45fbe9fb8fc90369`, ident `LuaJIT 2.1.1780076327`.
- Imported stock cleanup suite from `LuaJIT/LuaJIT-test-cleanup` commit `014708bceb70550a3ab8d539cff14d9085ca9cb8`.
- Promoted plan benchmark harness to tracked `auxiliary/bench/` and added `bench/run_baseline.sh` to write the required combined CSV.
- Adjusted three vendored stock `lib/contents.lua` expectations to match pinned LuaJIT `2.1.1780076327`: no `math.mod`, `math.log10` present, no `string.gfind`, and `table.move` present.
- M0 began as internal plumbing; later slices removed the `LJ_MT`
  compatibility wall so the lockless x86-64 Linux runtime is the default path.
- Project priority is safety, stability, and Lua language semantics before
  performance. If a lockless design cannot get close to LuaJIT throughput
  without weakening those guarantees, keep the guarantees and document the
  slower boundary explicitly.
- Strict legacy-barrier diagnostics are opt-in with
  `LJ_MT_REQUIRE_NO_LEGACY_BARRIERS=1`; M5 is the milestone where that count
  must reach zero. This is a developer diagnostic, not a CI gate.
- The x86-64 VM/JIT GC-state ownership rule is documented next to the relevant
  helper surfaces. Non-x64 DynASM ports still contain legacy global dispatch
  loads, but those are outside the current Linux/x86-64 implementation scope.
  Active tests should cover runtime behavior and counters.
- M0 now pins `io.popen(...):close()` semantics for both build profiles. The
  default LuaJIT 5.1 profile reports successful `pclose(3)` as `true` even when
  the child exits non-zero; the optional `LUAJIT_ENABLE_LUA52COMPAT` profile
  returns the decoded `nil, "exit", status` tuple. Test harnesses that need
  child-command status must parse an explicit shell status instead of changing
  runtime semantics.

Verification on 2026-06-12:

- `tools/ci/m0_invariant_audit.sh`: passed; reported 33 legacy `lj_gc_barrier*` call sites.
- `tools/ci/m0_matrix.sh`: passed under the current default lockless matrix.
  - JIT on: 508 stock tests passed.
  - JIT off: 386 stock tests passed (`-jit` tagged tests excluded).
- Rebuilt default JIT-enabled binary and ran `bench/run_baseline.sh src/luajit`; wrote `bench/baseline_372b369b9afd_.csv`.
