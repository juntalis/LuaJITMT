# M2 Notes

x64 TG dispatch-base switch:

- Moved the x64 VM `DISPATCH` register setup from `GG.dispatch` to
  embedded `GG.main_tg.dispatch` with `GG_G2TGDISP`.
- Rebased x64 VM global/JIT offsets through `TG_DISP2G`/`TG_DISP2J` and
  per-thread fields through `DISPATCH_TG(...)`.
- Rebased x64 JIT GC64 dispatch-relative code generation through
  `J2TG(as->J)->dispatch`.
- Fixed a verification failure in `lj_ir_ggfload` lowering: `FLOAD nil`
  pseudo-fields still subtracted `GG_OFS(dispatch)`, so base-metatable loads
  were emitted with old-dispatch offsets and the stock runner crashed in
  `test.lua:186`. `asm_fusefref` now subtracts `GG_OFS_TGDISP`.
- Updated `lj_asm_patchexit` GC-check recognition to match the new TG-relative
  vmstate displacement.
- This remains an embedded-main-TG bridge. Slow global/JIT paths still use
  fixed offsets from `GG.main_tg.dispatch`; later attachable TG work should
  replace those with `TG.gl` / `g->jitp` loads where TG blocks are no longer
  adjacent to `GG_State`.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `cd tests/stock/test && ../../../src/luajit test.lua --quiet`: 508 passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `src/luajit -e "jit.on(); local s=0; for i=1,1e6 do s=s+i end; assert(s==500000500000)"`: passed.

Arena bitmap scaffold:

- Added `src/lj_arena.{h,c}` with the 64 KiB arena constants, 16-byte cell
  model, 128-byte arena header, full `block`/`mark` bitmaps, sweep identities,
  and a free-run scanner.
- The plan text mixed a 64-cell metadata note with a 128-byte header plus two
  512-byte bitmaps. The implemented scaffold preserves full bitmap coverage, so
  metadata occupies 1152 bytes and the first allocatable cell is 72.
- Added `tests/t-arena-bitmap.c` and `tools/ci/m2_arena_bitmap.sh` to compare
  randomized bitmap states, major/minor sweep identities, and maximal free-run
  scanning against an independent shadow model.
- Included `lj_arena.o` in the normal core build and `lj_arena.c` in the
  amalgamation path. `Makefile.dep` was regenerated so incremental builds track
  `lj_arena.c`/`lj_arena.h` and the already-added TG dependencies.

Verification:

- `tools/ci/m2_arena_bitmap.sh`: passed; 20000 randomized cycles and 2164663
  free runs verified.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed without arena unused-function warnings.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Arena mmap source:

- Added `lj_arena_map(PRNGState *rs, uint32_t flags)` and
  `lj_arena_unmap(GCArena *a)`.
- The mapper over-maps 128 KiB, trims to one 64 KiB-aligned arena, validates
  that the full mapping stays below the GC64 47-bit pointer limit, and
  initializes only arena metadata. This keeps it as a source primitive for the
  later allocator backend; runtime allocation still uses the existing allocator.
- Probe retries follow the existing `lj_alloc.c` shape: no fixed mapping, low
  hint validation, short linear probing, then PRNG-derived low hints.
- Added `tests/t-arena-map.c` and `tools/ci/m2_arena_map.sh` to verify
  alignment, `arena_of`/`cell_of`, low-address range, metadata initialization,
  payload writeability, and unmap handling.

Verification:

- `tools/ci/m2_arena_bitmap.sh`: passed.
- `tools/ci/m2_arena_map.sh`: passed; 128 arenas mapped below 47 bits.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

TG allocator state and bump core:

- Replaced the placeholder `TGAlloc` with the planned TG-owned allocator shape:
  two bump frontiers, per-kind free-run bin slots, owned/needsweep lists, and
  the `alloc_black` color bit.
- Added a 16-byte `LJArenaFreeRun` node that fits in a free start cell, plus
  arena-kind/bin constants.
- Added `lj_arena_alloc_init`, `lj_arena_alloc_fini`, and a bump-only
  `lj_arena_alloc` core. It maps/adopts fresh owned arenas, advances the
  per-kind bump frontier, sets the block start bit, and optionally sets the
  mark bit for black allocation. It intentionally returns NULL for zero-size
  and huge allocations; free/realloc and bins are still future work.
- Wired TG lifecycle to initialize and tear down the embedded allocator state.
- Added `tests/t-arena-alloc.c` and `tools/ci/m2_arena_alloc.sh` to force
  multiple traversable/plain owned arenas, validate bitmap states and extents,
  exercise black allocation, and check teardown.

Verification:

- `tools/ci/m2_arena_bitmap.sh`: passed.
- `tools/ci/m2_arena_map.sh`: passed.
- `tools/ci/m2_arena_alloc.sh`: passed; 2400 allocations across owned arenas.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Arena focused CI wrapper:

- Added `tools/ci/m2_arena_all.sh` to run the bitmap, map/huge, and
  alloc/realloc focused tests as one stable command.

Verification:

- `tools/ci/m2_arena_all.sh`: passed.

GC header accessor invariant:

- GC header access goes through `lj_obj_*gcw*` and `lj_obj_*gcflags*`
  helpers so concurrent GC ownership, color bridging, and arena mark-bit
  transitions have one documented synchronization boundary.
- This invariant is now maintained by the helper comments, the object-layout
  offset pins below, and GC/arena behavior fixtures.

GC header accessors and offset pins:

- Added behavior-neutral `lj_obj_*gcw*` and `lj_obj_*gcflags*` accessors over
  the current `GCHeader.nextgc` and `GCHeader.marked` storage. No field rename
  or object layout change yet.
- Pinned common header offsets for `nextgc`, `marked`, and `gct`, plus the
  `GCstr`/`GCtab`/`GCupval` marked offsets and the x64 VM-sensitive
  `GCupval.marked - GCupval.tv == -8` displacement.
- Pinned current GC color/flag bit values in `lj_gc.h`, including the existing
  finalized/weak-key and cdata-finalizer/weak-val overlaps.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 object-layout work:

- Route C list/color users through the accessors in small islands before any
  physical `nextgc` -> `gcw` or `marked` -> `gcflags` rename.
- Keep x64 VM/JIT direct offset users pinned until their DASC/lowering changes
  can move in the same commit as the layout change.

First GC accessor conversion island:

- Routed the C GC color macros in `lj_gc.h` through `lj_obj_gcflags` helpers:
  `iswhite`, `isblack`, `isgray`, `isdead`, `newwhite`, `makewhite`,
  `flipwhite`, `black2gray`, `fixstring`, and `markfinalized`.
- Routed `lj_gc.c` local mark helpers through the same accessors:
  `white2gray`, `gray2black`, `isfinalized`, and `gc_mark_str`.
- Converted the main `gc_sweep`, `lj_gc_closeuv`, and `lj_mem_newgco`
  C-list touchpoints to use `lj_obj_gcwref` / `lj_obj_setgcwr` where changed.
- Left generated VM/JIT direct `marked` offsets unchanged and still pinned.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Second GC accessor conversion island:

- Cleared remaining direct `gch.marked` / `gch.nextgc` uses from `lj_gc.c`
  and `lj_gc.h` in the scanned set.
- Converted userdata finalizer separation, weak-table flag maintenance,
  string-chain sweep, finalizer list unchaining/reinsertion, and cdata
  finalizer flag clearing to `lj_obj_gcw*` / `lj_obj_gcflags*` helpers.
- Object-specific files still have direct header users (`lj_str.c`,
  `lj_cdata.c`, `lj_func.c`, `lj_udata.c`, `lj_trace.c`) and are left for
  later focused islands.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Object-specific accessor conversion island:

- Cleared direct `gch.marked` / `gch.nextgc` / `nextgc` / `marked` header
  uses from the scanned object-specific files: `lj_str.c`, `lj_cdata.c`,
  `lj_udata.c`, `lj_func.c`, and `lj_trace.c`.
- Preserved string-table low-bit tagging by writing through `GCRef *`
  accessors and keeping the tagged bucket values unchanged.
- Preserved cdata variable-size and finalizer bits by routing them through
  `lj_obj_gcflags` helpers without changing bit assignments.
- Open-upvalue list operations now use `lj_obj_gcwref` for the current
  `nextgc` storage, but the physical layout and VM offsets remain unchanged.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining easy C-side accessor cleanup:

- Converted remaining direct C-side header users outside `lj_obj.h` and
  VM/JIT offset code in `lib_ffi.c`, `lj_record.c`, `lj_snap.c`, and
  `lj_state.c`.
- `lj_snap.c` open-upvalue walk now uses `lj_obj_gcw`; main thread startup
  uses `lj_obj_setgcflags`; FFI cdata finalizer checks/setters use
  `lj_obj_gcflags` helpers.
- Remaining direct users are now expected pinned sites: `lj_obj.h` storage and
  accessors, x64 VM/JIT direct offset code, and ignored non-target DASC files.

Verification:

- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Owner-local arena free/realloc core:

- Added owner-local free-run bins to the arena allocator scaffold. `lj_arena_free`
  now encodes a freed block as a free start cell and stores a 16-byte
  `LJArenaFreeRun` node in the freed payload.
- Historical policy (superseded on 2026-07-13): `lj_arena_alloc` searched bins
  before bump allocation, supported splitting
  larger free runs, and explicitly sets allocation color so reusing a free
  start does not inherit its old mark bit.
- Added `lj_arena_realloc`: NULL pointer allocates, zero size frees, growth
  allocates/copies/frees, and shrink splits the tail into a free run so later
  frees using the smaller size remain coherent.
- Added `tests/t-arena-realloc.c`, and extended `tools/ci/m2_arena_alloc.sh`
  to run both the broad bump-allocation test and the focused reuse/realloc
  test.

Verification:

- `tools/ci/m2_arena_bitmap.sh`: passed.
- `tools/ci/m2_arena_map.sh`: passed.
- `tools/ci/m2_arena_alloc.sh`: passed.
  - `t-arena-alloc`: 2400 allocations across owned arenas.
  - `t-arena-realloc`: free-run reuse and realloc verified.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator work after free/realloc core:

- Add the concurrent huge-object side table and route backend huge
  allocations through it.
- Add a `lua_Alloc` arena backend state and select it behind
  `LJ_ALLOCF_INTERNAL`.
- Replace the temporary legacy GC object list with arena/gc2 sweep machinery
  before the physical `nextgc` -> `gcw` field rename.

Huge mapping source:

- Generalized the arena mmap source to map arbitrary 64 KiB-aligned extents
  below the GC64 47-bit limit.
- Added `lj_arena_huge_mapsize`, `lj_arena_huge_map`, and
  `lj_arena_huge_unmap`. Huge payloads start after the 128-byte arena header,
  while `lj_arena_of(payload)` still returns the aligned base carrying
  `LJ_AF_HUGE_MAGIC`.
- The helper records the rounded mapping size in header cells for now. The
  actual concurrent huge side table from 04 §4.5.1 is still pending and will
  own mark/finalizer/size metadata before runtime backend selection.
- Extended `tools/ci/m2_arena_map.sh` with `tests/t-arena-huge.c`.

Verification:

- `tools/ci/m2_arena_bitmap.sh`: passed.
- `tools/ci/m2_arena_map.sh`: passed.
  - `t-arena-map`: 128 arenas mapped below 47 bits.
  - `t-arena-huge`: 3 huge mappings verified.
- `tools/ci/m2_arena_alloc.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator work after huge source:

- Add the concurrent huge-object side table and backend integration.
- Add a `lua_Alloc` arena backend state and select it behind
  `LJ_ALLOCF_INTERNAL`.
- Replace the temporary legacy GC object list with arena/gc2 sweep machinery
  before the physical `nextgc` -> `gcw` field rename.

Arena allocator huge routing:

- Routed `lj_arena_alloc` requests larger than `LJ_HUGE_THRESHOLD` through the
  huge mmap source instead of returning NULL.
- `lj_arena_free` now recognizes `LJ_AF_HUGE_MAGIC` via `lj_arena_of(p)` and
  unmaps huge payloads using the caller-supplied size.
- `lj_arena_realloc` now moves between arena and huge storage, copies
  `min(osize, nsize)`, and keeps an existing huge pointer only when the rounded
  huge mapping size is unchanged.
- Extended focused allocation/reallocation tests to cover huge allocation,
  huge-to-huge growth, huge-to-arena shrink, flag preservation, and copied
  payload data.
- This is still a source/backend scaffold. Concurrent huge metadata, mark bits,
  finalizer bits, and size ownership remain for the HugeTab work.

Verification:

- `tools/ci/m2_arena_all.sh`: passed.
  - `t-arena-bitmap`: 20000 cycles, 2164663 free runs verified.
  - `t-arena-map`: 128 arenas mapped below 47 bits.
  - `t-arena-huge`: 3 huge mappings verified.
  - `t-arena-alloc`: 2400 allocations across owned arenas.
  - `t-arena-realloc`: free-run reuse and realloc verified.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator work after huge routing:

- Add the concurrent huge-object side table and backend integration.
- Add a `lua_Alloc` arena backend state and select it behind
  `LJ_ALLOCF_INTERNAL`.
- Replace the temporary legacy GC object list with arena/gc2 sweep machinery
  before the physical `nextgc` -> `gcw` field rename.

Huge object side-table scaffold:

- Added the public `HugeTab` / `LJHugeInfo` shape and `LJ_HUGEF_*` metadata
  bits for huge payload size, mark, traversable, and finalizer state.
- Implemented a fixed-capacity open-addressed table over huge payload
  pointers. Insert/delete publish 16-byte `{addr, meta}` entries with
  `la_cas128`; lookup uses acquire snapshots; mark sets the metadata mark bit
  atomically. No resizing yet.
- Kept `lj_arena_huge_map` / `lj_arena_huge_unmap` as the OS lifetime source;
  the side table owns only metadata in this checkpoint.
- Added `tests/t-arena-hugetab.c` and `tools/ci/m2_arena_hugetab.sh` covering
  init/fini, insert/lookup, duplicate insert, mark idempotence, delete/miss,
  tombstone reuse, and full-table failure.
- Added `-mcx16` to x64 target compile flags and to focused arena test scripts
  because `lj_arena.c` now contains `la_cas128` users.

Verification:

- `tools/ci/m2_arena_all.sh`: passed.
  - `t-arena-bitmap`: 20000 cycles, 2164663 free runs verified.
  - `t-arena-map`: 128 arenas mapped below 47 bits.
  - `t-arena-huge`: 3 huge mappings verified.
  - `t-arena-alloc`: 2400 allocations across owned arenas.
  - `t-arena-realloc`: free-run reuse and realloc verified.
  - `t-arena-hugetab`: insert lookup mark delete tombstone full.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator work after HugeTab scaffold:

- Integrate HugeTab into the runtime arena backend / future `GCHeapGlobal`
  owner and retire the temporary header-only huge-size source.
- Add RCU resize and deferred old-vector reclamation when epoch machinery is
  available.
- Add a `lua_Alloc` arena backend state and select it behind
  `LJ_ALLOCF_INTERNAL`.
- Replace the temporary legacy GC object list with arena/gc2 sweep machinery
  before the physical `nextgc` -> `gcw` field rename.

Arena lua_Alloc shim:

- Added `LJArenaAllocD`, `lj_arena_allocd_init`, and `lj_arena_allocf` as a
  `lua_Alloc`-compatible wrapper over `lj_arena_alloc/free/realloc`.
- The shim uses caller-provided `TGAlloc` and `PRNGState` storage, preserves
  the `lua_Alloc` failure contract, and relies on the lower allocator for
  small/huge transitions.
- Added `tests/t-arena-allocf.c` and extended `tools/ci/m2_arena_alloc.sh` to
  cover small allocation, growth, failed oversized realloc preserving old
  content, small-to-huge, huge-to-small, zero-size free, and repeated
  init/fini.
- Runtime selection is intentionally not in this checkpoint; the next edit
  must handle GG bootstrap and close-state teardown ordering separately.

Verification:

- `tools/ci/m2_arena_alloc.sh`: passed.
  - `t-arena-alloc`: 2400 allocations across owned arenas.
  - `t-arena-realloc`: free-run reuse and realloc verified.
  - `t-arena-allocf`: lua_Alloc shim transitions verified.
- `tools/ci/m2_arena_all.sh`: passed, including `t-arena-hugetab`.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.

Remaining M2 allocator work after lua_Alloc shim:

- Switch `LJ_ALLOCF_INTERNAL` to the arena shim with stack bootstrap allocator
  state, moved `TGAlloc` ownership, persistent `TGState` allocd storage, and
  close-state teardown that frees tmpbuf/stack/lightud before arena unmap.
- Integrate HugeTab into the runtime arena backend / future `GCHeapGlobal`
  owner.
- Replace the temporary legacy GC object list with arena/gc2 sweep machinery
  before the physical `nextgc` -> `gcw` field rename.

Internal arena allocator selection:

- Switched `lua_newstate(LJ_ALLOCF_INTERNAL, NULL)` from the bundled
  `lj_alloc_f` path to `lj_arena_allocf`.
- Bootstrapped `GG_State` with stack-local `TGAlloc` / `LJArenaAllocD`, then
  moved the live `TGAlloc` into `GG->main_tg.alloc` before initialization.
- Added persistent `TGState.allocd` and `TGF_ARENA_INTERNAL` so `g->allocd`
  points at TG-owned allocator state and close-time teardown can identify a
  GG allocated from the arena backend.
- `lj_tg_init` now preserves an already-moved allocator instead of wiping it,
  and `lj_tg_fini` frees TG scratch buffer storage before final arena teardown.
- `close_state` now frees trace/ctype/string state, TG/global buffers, main
  stack, and lightuserdata segments while the arena allocator is still live.
  It then snapshots `TGAlloc` to the C stack, verifies `g->gc.total` is back
  to `sizeof(GG_State)`, unmaps owned arenas, and only separately unmaps
  `GG_State` when it was huge.
- Added `tests/t-arena-state.c` and `tools/ci/m2_arena_state.sh` for repeated
  `luaL_newstate` / `luaL_openlibs` / churn / `lua_close` cycles. The focused
  arena wrapper now runs this lifecycle test too.
- Wrapped the legacy `lj_alloc.c` include in `ljamalg.c` with the same local
  unused-function diagnostic suppression style used for arena scaffolding,
  because the old allocator create helper remains compiled but is no longer
  the internal state-creation path.

Verification:

- `tools/ci/m2_arena_all.sh`: passed.
  - includes bitmap/map/huge/alloc/realloc/allocf/hugetab focused tests.
  - `t-arena-state`: repeated luaL_newstate churn verified.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator work after internal allocator selection:

- Integrate HugeTab into runtime huge allocation metadata so huge size/mark
  ownership moves out of temporary header-only accounting.
- Add arena/gc2 sweep ownership and stop freeing GC objects through the legacy
  object-list path.
- Replace the temporary legacy GC object list with arena/gc2 sweep machinery
  before the physical `nextgc` -> `gcw` field rename.

HugeTab-backed arena allocf:

- Extended `LJArenaAllocD` with an optional `HugeTab *` and added
  `lj_arena_allocd_sethugetab`.
- `lj_arena_allocf` now inserts huge allocations into HugeTab when attached,
  deletes table entries on free, and uses table-owned sizes for huge realloc
  copies/unmaps. Without a table it keeps the previous header/size fallback.
- Huge metadata mirrors traversable and allocation-black state into
  `LJ_HUGEF_TRAVERSABLE` / `LJ_HUGEF_MARK`. Finalizer metadata remains for
  the later object-aware allocation split.
- Internal TG state now owns a fixed `TG_HUGETAB_BITS == 16` HugeTab for
  arena-backed states. `TGF_HUGETAB` controls teardown; close-state keeps the
  table alive until TG/global buffers, stack, and lightuserdata segments have
  been freed.
- Extended `t-arena-allocf` to assert table insert/lookup/delete behavior
  through small-to-huge, huge-to-huge, and huge-to-small realloc paths.

Verification:

- `tools/ci/m2_arena_alloc.sh`: passed.
  - `t-arena-alloc`: 2400 allocations across owned arenas.
  - `t-arena-realloc`: free-run reuse and realloc verified.
  - `t-arena-allocf`: lua_Alloc shim transitions verified.
- `tools/ci/m2_arena_all.sh`: passed, including HugeTab and state lifecycle.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator work after HugeTab-backed allocf:

- Move HugeTab ownership from TG-local transitional storage to the planned
  heap-global owner when `GCHeapGlobal` lands.
- Add RCU resize and deferred old-vector/table reclamation.
- Add arena/gc2 sweep ownership and stop freeing GC objects through the legacy
  object-list path before the physical `nextgc` -> `gcw` field rename.

Owner-local arena sweep scaffold:

- Added `lj_arena_alloc_prepare_sweep` to move owner-local `owned[*]` arenas
  to `needsweep[*]`, clear bump windows and bins, and set `LJ_AF_NEEDSWEEP`.
- Added `lj_arena_sweep_one` to pop one arena from `needsweep[k]`, apply the
  existing major/minor bitmap sweep identity, rebuild free-run bins, reserve
  the largest run as the next bump window when it is at least `LJ_BUMP_MIN`,
  update `live_cells` / `sweep_epoch`, clear `LJ_AF_NEEDSWEEP`, and reattach
  the arena to `owned[k]`.
- `lj_arena_alloc_fini` now unmaps both `owned[*]` and `needsweep[*]` lists.
- This is not yet wired into legacy `lj_gc.c`; object liveness, finalizers,
  weak clearing, string sweep, and object destructors still run through the
  stock collector and root/object lists.
- Added `tests/t-arena-sweep.c` and `tools/ci/m2_arena_sweep.sh` covering
  prepare/reset behavior, major and minor sweep identities, live-start
  accounting, rebuilt bins, bump-window reuse, invalid/empty sweep handling,
  and finalization cleanup.

Verification:

- `tools/ci/m2_arena_sweep.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed, including sweep and state lifecycle.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator work after owner sweep scaffold:

- Use arena mark bits during legacy marking or a degenerate gc2 mark phase
  without changing finalization semantics.
- Add the STW gc2 bridge that can prepare/sweep TG arenas at collector phase
  boundaries while legacy lists remain as an oracle.
- Defer the physical `nextgc` -> `gcw` / `marked` -> `gcflags` rename until
  the x64 VM/JIT offset sites and invariant checks can move together.

Legacy mark mirror into arena metadata:

- Added a narrow `lj_gc.c` bridge that mirrors legacy GC marks into arena
  metadata for internal arena-backed states.
- Normal arena objects set their allocation-start mark bit with
  `la_bit_test_and_set64`; huge objects mark their HugeTab entry when the
  internal TG HugeTab is present.
- `gc_mark`, JIT trace marking, prototype chunk-name string marking, and weak
  table string preservation now pass through the mirror. Legacy header colors,
  gray lists, weak clearing, finalization, and object-list/string sweep remain
  authoritative.
- The bridge deliberately ignores non-allocation-start cells, which keeps
  embedded fixed objects inside `GG_State` from corrupting bitmap state.
- Added `tests/t-arena-gcmark.c` and `tools/ci/m2_arena_gcmark.sh`, which keep
  a table and huge string alive across a full collection and assert the table
  arena mark bit plus huge string HugeTab mark flag are set.

Verification:

- `tools/ci/m2_arena_gcmark.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed, including gcmark, sweep, and state
  lifecycle.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after mark mirror:

- Add a degenerate STW gc2 phase bridge that prepares/sweeps TG arenas around
  legacy collector phase boundaries while legacy object lists remain as the
  destructor/finalizer oracle.
- Decide how to reset arena mark bits between legacy cycles without racing
  current stock sweep semantics.
- Defer the physical `nextgc` -> `gcw` / `marked` -> `gcflags` rename until
  the x64 VM/JIT offset sites and invariant checks can move together.

Legacy GC allocation-color bridge:

- Added `gc_arena_alloc_black` and toggled `TGAlloc.alloc_black` with the
  legacy GC phase: set at `gc_mark_start`, cleared when the cycle returns to
  `GCSpause` after sweep/finalization.
- This means arena allocations born during an active legacy mark/sweep cycle
  get their arena mark bit set at allocation time. Legacy header colors and
  object-list sweep still decide liveness.
- Added `tests/t-arena-gcphase.c` and `tools/ci/m2_arena_gcphase.sh`. The test
  forces an incremental GC cycle to pause mid-cycle, verifies `alloc_black`,
  confirms a direct arena allocation is born marked, then finishes the cycle
  and verifies allocation color resets.

Verification:

- `tools/ci/m2_arena_gcphase.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed, including gcmark/gcphase, sweep, and
  state lifecycle.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after allocation color:

- Add a degenerate STW gc2 phase bridge that rebuilds arena free-run state at
  safe legacy sweep boundaries without double-freeing objects.
- Move toward object-aware traversable/plain allocation classification instead
  of the generic `lua_Alloc` flags.
- Defer the physical `nextgc` -> `gcw` / `marked` -> `gcflags` rename until
  the x64 VM/JIT offset sites and invariant checks can move together.

Typed GC-object arena allocation:

- Added `lj_arena_allocd_alloc`, a typed allocation helper over
  `LJArenaAllocD` that preserves HugeTab handling while allowing per-call arena
  flags.
- Routed `lj_mem_newgco` through that helper for internal arena-backed states,
  using `LJ_AF_TRAVERSABLE`. Generic `lj_mem_new` / vector / buffer
  allocations still go through the normal `lua_Alloc` flags and remain plain.
- This is a transitional split for `lj_mem_newgco` users. Some legacy
  constructors still allocate GC objects manually through `lj_mem_newt` and
  `newwhite`, notably strings, userdata, and variable-aligned cdata. Those
  should stay explicitly classified instead of inheriting generic allocator
  flags. The split already separates tables, functions, protos, fixed cdata,
  and other `lj_mem_newgco` users from table nodes, arrays, buffers, and
  other auxiliary storage.
- Extended `t-arena-allocf` for direct typed allocation and `t-arena-gcmark`
  to assert runtime table and huge-string GC objects carry traversable arena
  metadata.

Verification:

- `tools/ci/m2_arena_alloc.sh`: passed.
- `tools/ci/m2_arena_gcmark.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Runtime arena sweep experiment note:

- Attempting to sweep all runtime arenas at the legacy sweep boundary corrupted
  live auxiliary allocations: table nodes / arrays / buffers are not GC objects
  and are not marked by legacy object traversal. The safe bridge must sweep
  traversable GC-object arenas only, or add explicit ownership/marking for
  plain auxiliary allocations before sweeping plain arenas.
- A follow-up audit found a separate huge-header flag collision:
  `LJ_AF_HUGE_MAGIC` used bit 0, so huge plain allocations appeared
  traversable when tests checked `hdr.flags & LJ_AF_TRAVERSABLE`. The tracked
  fix moves the huge magic out of the low arena flag bits and checks HugeTab
  metadata for huge object traversal flags.

Remaining M2 allocator/GC bridge work after typed GC allocation:

- Add a degenerate STW bridge that rebuilds traversable GC-object arenas at a
  safe legacy sweep boundary while leaving plain auxiliary arenas untouched.
- Refine constructors so non-traversable GC objects such as strings can move
  back to plain/non-push arenas.
- Defer the physical `nextgc` -> `gcw` / `marked` -> `gcflags` rename until
  the x64 VM/JIT offset sites and invariant checks can move together.

Custom-linked traversable GC allocation:

- Added `lj_mem_newgco_raw`, a raw allocation helper for constructors that
  need typed arena allocation but cannot use the normal root-list linking done
  by `lj_mem_newgco`.
- Routed open upvalue allocation and JIT trace allocation through the raw
  helper with `LJ_AF_TRAVERSABLE`; their existing legacy list/linking and
  color setup remains authoritative.
- Extended `t-arena-gcmark` to keep a suspended coroutine open upvalue alive,
  generate a JIT trace, and assert both objects are in traversable arenas with
  mirrored legacy arena marks.

Verification:

- `tools/ci/m2_arena_gcmark.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after custom-linked traversable
allocation:

- Keep the runtime arena sweep bridge backed out until finalizer and
  resurrection transitions are mirrored or explicitly excluded.
- Decide whether userdata and variable-aligned cdata become traversable or
  receive inline/non-push mark handling; both currently have custom allocation
  bases or finalizer semantics that make blind arena reclaim unsafe.
- Add a verify-only legacy sweep boundary check before attempting any
  mark-based arena reclaim.

Verify-only arena sweep boundary hook:

- Added a debug-only arena verifier at the end of legacy root-list sweep. It
  asserts that block-start objects still reachable from legacy root/open-upval
  lists have mirrored arena mark bits, but performs no arena reclamation.
- Reused `TGState.mark_active` as a small mark-phase marker so the verifier
  only runs after a completed mark/atomic phase, not during LuaJIT's
  preserving sweep fast-forward used by full collection.
- The verifier intentionally skips cycles with `mmudata` and skips custom
  aligned non-block-start objects until finalizer/resurrection and allocation
  base marking are explicitly modeled.
- Added `tools/ci/m2_arena_gcverify.sh`, an assertion build wrapper that
  exercises the verifier through `t-arena-gcmark`, and included it in
  `tools/ci/m2_arena_all.sh`.

Verification:

- `tools/ci/m2_arena_gcverify.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after verify-only hook:

- Add explicit handling for custom-aligned cdata allocation-base marking before
  any mark-based arena reclaim can include those objects.
- Mirror or exclude finalizer queue transitions (`mmudata`) and resurrection
  paths before enabling a reclaiming sweep.
- The first runtime bridge should still be legacy-authoritative rebuild from
  free runs, not mark-based reclamation.

Variable cdata allocation-base arena marks:

- Updated the arena mark mirror to resolve variable/aligned cdata from the
  `GCcdata *` object pointer to `memcdatav(cd)` before marking or verifying
  arena metadata. This matches the allocation/free base used by
  `lj_cdata_newv` / `lj_cdata_free`.
- Extended `t-arena-gcmark` with a VLA cdata case and reused the same
  object-base resolution in the test helper.

Verification:

- `tools/ci/m2_arena_gcmark.sh && tools/ci/m2_arena_gcverify.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after variable cdata marks:

- Mirror or exclude finalizer queue transitions (`mmudata`) and resurrection
  paths before any mark-based arena reclaim.
- Add a legacy-authoritative rebuild bridge from allocator free runs before
  retrying mark-based reclamation.

Arena marks for legacy liveness transitions:

- Exposed the arena mark mirror as `lj_gc_arena_markobj` for non-traversal
  sites that keep an object alive.
- Mirrored arena marks on string resurrection, open-upvalue resurrection,
  finalizer cdata enqueue to `mmudata`, and cdata/userdata relinking during
  finalization.
- This keeps legacy object/list ownership authoritative; it only updates arena
  mark metadata for objects legacy has already decided are live.

Verification:

- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after liveness-transition marks:

- Add an assertion-mode stress case for finalizer queue arena marks before any
  reclaiming bridge.
- Add a legacy-authoritative rebuild bridge from allocator free runs before
  retrying mark-based reclamation.

Finalizer cdata mark coverage:

- Extended `t-arena-gcmark` with a VLA cdata finalizer that resurrects the
  object into `keep.fin`, then verifies the finalizer path leaves a mirrored
  arena mark on the allocation base.

Verification:

- `tools/ci/m2_arena_gcmark.sh && tools/ci/m2_arena_gcverify.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after finalizer coverage:

- Add a legacy-authoritative rebuild bridge from allocator free runs before
  retrying mark-based reclamation.

Legacy-authoritative free-run rebuild bridge:

- Added `lj_arena_alloc_rebuild_free`, which clears and rebuilds owner-local
  free-run bins from existing free-state bitmap runs without applying arena
  sweep identities. Legacy frees remain the only source of reclamation.
- Called the rebuild at the legacy root-list sweep boundary after the
  assertion-only arena mark verifier.
- Fixed an initial rebuild bug where a free run adjacent to the active bump
  frontier was treated as extending through the never-allocated bump tail; the
  rebuild now clamps runs at the active bump cell for that arena.
- Extended `t-arena-sweep` to cover adjacent free-run coalescing and the
  active bump frontier clamp.

Verification:

- `tools/ci/m2_arena_sweep.sh`: passed.
- `tools/ci/m2_arena_state.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after free-run rebuild:

- Mark-based arena reclamation is still disabled. A future attempt must keep
  huge mark clearing, finalizer windows, and legacy object destructors
  explicitly sequenced before reclaiming by arena mark bits.

Arena mark cycle reset:

- Added cycle-start clearing for arena mark metadata before root marking.
  Normal arena clearing preserves free-run sentinels (`block=0, mark=1`) while
  clearing marks from allocated block starts (`block=1`).
- Added hugetab mark clearing that preserves size/traversable/finalizer metadata
  and drops only `LJ_HUGEF_MARK`.
- Covered both helpers directly: `t-arena-sweep` checks free-run sentinels
  survive normal arena mark clearing, and `t-arena-hugetab` checks huge metadata
  keeps `LJ_HUGEF_TRAVERSABLE` after mark clearing.

Verification:

- `tools/ci/m2_arena_hugetab.sh`: passed.
- `tools/ci/m2_arena_sweep.sh`: passed.
- `tools/ci/m2_arena_gcmark.sh && tools/ci/m2_arena_gcverify.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after mark reset:

- Mark-based arena reclamation remains disabled. The next reclamation attempt
  should run from a clean per-cycle arena/huge mark baseline and keep legacy
  destructors authoritative until the arena sweep path has equivalent coverage.

Raw allocation arena mark coverage and bump-run canonicalization:

- Added `lj_gc_arena_markmem`, a raw allocation-base mark hook that shares the
  same normal-arena and hugetab mark path as `lj_gc_arena_markobj`.
- Mirrored marks for live raw allocations that legacy GC reaches through
  owners or global roots: table array/hash backing stores, thread stacks,
  string intern table vector, temporary string buffers, FFI ctype state/tables,
  JIT trace/IR/snapshot buffers, and owned buffer-userdata `SBuf` storage.
- Extended `t-arena-gcmark` to assert marks on separated table array/hash
  storage, coroutine stack storage, the string intern vector, and FFI ctype
  state/table storage.
- Fixed a sweep-scaffold bug found by the Ramanujan sidecar: when a coalesced
  free run became the bump window, internal free-start bits could survive and
  later appear as overlapping free runs. The selected bump run is now
  canonicalized with a single free head and extent bits before reuse.
- Extended `t-arena-sweep` with a regression that sweeps two adjacent dead
  allocations into the bump window, allocates across both, and asserts the old
  internal free head is cleared.

Verification:

- `tools/ci/m2_arena_sweep.sh`: passed.
- `tools/ci/m2_arena_gcmark.sh && tools/ci/m2_arena_gcverify.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Sidecar findings to keep in front of the next reclaim slice:

- Hubble recommended a narrow next reclaim bridge for non-huge traversable
  arenas only, with legacy GC still deciding liveness/destructors and with
  `g->gc.total` accounting preserved even if physical cell return is deferred
  to arena bitmap sweep.
- Do not sweep plain arenas by mark bits yet. They still contain raw auxiliary
  allocations and transient `lj_mem_*` blocks; this checkpoint marks several
  live classes, but it is not proof that all plain raw allocations are covered.
- Do not sweep huge objects by mark bits yet. Huge mark clearing exists, but
  huge-table enumeration/deletion and finalizer metadata are not implemented.
- Do not run mark-based sweep from `lj_gc_fullgc` fast-forward or while
  `mark_active != 2`; partial marking can make live objects look dead.
- Do not prepare `needsweep` before legacy object sweep has finished, because
  legacy frees currently insert into normal owner bins.

Remaining M2 allocator/GC bridge work after raw mark coverage:

- Add traversable-only, non-huge arena reclaim behind legacy liveness decisions:
  unlink/account/deconstruct with legacy GC, avoid double-returning whitelisted
  object bodies, then let arena bitmap sweep reclaim their cells.
- Add focused runtime reclaim tests for live/dead closure cells, skip cases
  with pending finalizers, full-GC fast-forward safety, and huge object legacy
  ownership.

Traversable runtime arena sweep bridge:

- Added kind-scoped arena maintenance helpers:
  `lj_arena_alloc_rebuild_free_kind`, `lj_arena_alloc_prepare_sweep_kind`, and
  `lj_arena_alloc_sweep_kind`.
- `prepare_sweep_kind` now materializes the active bump tail as a free run
  before moving a kind's owned arenas to `needsweep`, so bitmap sweep does not
  lose never-allocated tail capacity when the last allocated object is live.
- At the legacy root-list sweep boundary, fully marked cycles with no pending
  finalizers now run a major bitmap sweep for `LJ_ARENAK_TRAVERSABLE` only and
  rebuild plain free bins without applying the bitmap identity. If
  `mark_active != 2` or `mmudata` is non-empty, the old rebuild-only path is
  preserved.
- Added `t-arena-gcsweep` to assert that a no-finalizer full GC leaves live
  traversable objects post-sweep white with a sweep epoch, while separated
  plain table backing storage remains unswept.
- Extended `t-arena-sweep` to cover kind-scoped prepare/sweep isolation:
  traversable arenas move through `needsweep` while plain owned/bump state is
  left intact.

Verification:

- `tools/ci/m2_arena_sweep.sh && tools/ci/m2_arena_gcmark.sh &&
  tools/ci/m2_arena_gcverify.sh && tools/ci/m2_arena_gcsweep.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Raman sidecar recommendation for first body-deferment reclaim:

- Start with non-huge `LJ_TPROTO` bodies in traversable arenas. `GCproto` is
  allocated through `lj_mem_newgco`, has no finalizer/destructor path, has no
  auxiliary raw allocations, and `lj_func_freeproto` only calls
  `lj_mem_free(g, pt, pt->sizept)`.
- Next candidate is `LJ_TFUNC`; defer `LJ_TUPVAL`, `LJ_TTRACE`, tables,
  threads, and cdata until their list side effects, JIT side effects,
  auxiliary frees, and finalizer paths are split from body reclamation.

Remaining M2 allocator/GC bridge work after traversable sweep bridge:

- Add an accounting-only free helper for selected non-huge traversable object
  bodies, use it for `GCproto` first, and let the already-wired traversable
  arena sweep physically reclaim those accounted cells after legacy sweep.
- Add a runtime test that drops many loaded chunks/prototypes, forces full GC,
  verifies accounting returns to baseline, and verifies reclaimed prototype
  cells are reusable after the traversable bitmap sweep.

GCproto accounting-only body deferment:

- Added `lj_mem_freegco_defer`, which subtracts exact legacy `g->gc.total`
  accounting for a dead object body but leaves the arena block start allocated
  for the traversable bitmap sweep to reclaim later.
- The helper only accepts the internal arena allocator, non-huge pointers,
  traversable arenas, and valid block starts. Non-arena, huge, plain, or
  already-free cases fall back to the existing physical `lj_mem_free` path.
- Switched only `lj_func_freeproto` to the helper. `GCproto` has no finalizer
  path, no auxiliary raw allocations, and no free-time side effects beyond
  body accounting/release, so it is the first body-deferment target.
- Extended `t-arena-gcsweep` to:
  - allocate/account-defer a raw traversable block and assert the block bit
    stays set until a later full GC drives the traversable arena sweep;
  - drop a loaded chunk prototype, force full GC, assert total accounting falls
    by at least the prototype body size, and assert the old prototype cell is no
    longer an allocated block start after sweep.

Verification:

- `tools/ci/m2_arena_gcsweep.sh && tools/ci/m2_arena_sweep.sh &&
  tools/ci/m2_arena_gcmark.sh && tools/ci/m2_arena_gcverify.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after GCproto deferment:

- Expand accounting-only body deferment to the next safe traversable type,
  likely `LJ_TFUNC`, while preserving exact accounting and avoiding double
  physical frees.
- Keep `LJ_TUPVAL`, `LJ_TTRACE`, tables, threads, cdata, plain arenas, huge
  sweep, and finalizer-window reclaim out of this path until their side effects
  and auxiliary ownership are split and covered.
- Sartre sidecar follow-up: the current arena sweep boundary is skipped when
  `mmudata` is non-empty, so an accounted/deferred dead proto from a finalizer
  cycle can remain `block=1, mark=0` and unavailable until a later no-finalizer
  cycle. Add an explicit finalizer-window test and either tolerate/document the
  delayed reuse or add a safe post-finalize traversable sweep boundary.
- Sartre also called out focused follow-ups: bytecode-reader proto allocation,
  huge proto immediate-free behavior, JIT trace side effects for protos with
  traces, and an assertion-build `lua_close` churn test for many dead small
  protos.

Post-finalize traversable sweep for deferred bodies:

- Added a second traversable arena sweep boundary after `GCSfinalize` drains
  `mmudata`, before `alloc_black` and `mark_active` are reset. This reuses the
  existing `gc_arena_finish_sweep_boundary` gate, so it still requires
  `mark_active == 2` and `mmudata == NULL`.
- This resolves the first-order delayed reuse case from Sartre's note: a
  `GCproto` body accounted/deferred during a cycle that also has pending
  finalizers is now physically reclaimed by the same full collection once all
  finalizers have run.
- Extended `t-arena-gcsweep` with a C `__gc` userdata finalizer, a dropped
  loaded-chunk prototype in the same cycle, and assertions that the prototype
  cell is no longer an allocated block start after `lua_gc(..., collect)`.
- Updated `t-arena-gcmark` for the new canonical post-collect state: live
  traversable objects are post-sweep white (`block=1, mark=0`) with the current
  sweep epoch, while plain raw bases and huge entries still retain mark
  metadata until their later reclaim paths exist.

Verification:

- `tools/ci/m2_arena_gcmark.sh && tools/ci/m2_arena_gcverify.sh &&
  tools/ci/m2_arena_gcsweep.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -C src -j$(getconf _NPROCESSORS_ONLN)`: passed.
- `make -C src clean && make -C src amalg -j$(getconf _NPROCESSORS_ONLN)`:
  passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after post-finalize sweep:

- Add bytecode-reader and huge-proto coverage for the `GCproto` path.
- Add an assertion-build `lua_close` churn test for many dead small protos.
- Expand accounting-only body deferment to `LJ_TFUNC` once closure body reuse
  has equivalent focused coverage.

GCfunc accounting-only body deferment:

- Switched `lj_func_free` to the same `lj_mem_freegco_defer` fallback shape as
  `lj_func_freeproto`. This is intentionally narrow: closure bodies have no
  destructor side effects beyond body-size accounting/release, and both Lua and
  C closures are allocated through `lj_mem_newgco` in traversable arenas.
- Sidecar review agreed that this `LJ_TFUNC` widening is safe on x86_64 Linux
  as long as the arena sweep remains bitmap-only and does not scan stale edges
  from dead-but-not-yet-swept function bodies. Do not generalize this helper to
  upvalues, tables, traces, cdata, threads, plain arena payloads, or huge
  allocations without splitting their destructor side effects first.
- Extended `t-arena-gcsweep` with focused closure coverage:
  - a dead Lua closure body whose prototype is kept live through a sibling
    closure, proving the function body alone is accounted and reclaimed;
  - a dead loaded chunk where both the closure body and prototype body are
    accounted before traversable bitmap sweep reclaims both cells;
  - a dead C closure with a table upvalue that remains live elsewhere;
  - a finalizer-window cycle that drops both a loaded chunk and a Lua closure
    body, proving the post-finalize traversable sweep also reclaims deferred
    `GCfunc` cells.

Verification:

- `tools/ci/m2_arena_gcsweep.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -j$(nproc)`: passed.
- `make -C src clean && make amalg -j$(nproc)`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after GCfunc deferment:

- Add assertion-build `lua_close` churn for many dead small prototypes and
  closures.
- Keep the next body-deferment target conservative: only consider a type once
  its free path has no link-table, aux-storage, finalizer, JIT, or plain/huge
  allocator side effects left to preserve.

GCproto bytecode-reader and huge fallback coverage:

- Extended `t-arena-gcsweep` with a binary bytecode round-trip:
  `string.dump` creates a bytecode blob, `loadstring(blob)` drives
  `lj_bcread_proto`, and dropping the loaded function asserts both the
  bytecode-reader `GCproto` body and its closure body are accounted and
  reclaimed by the traversable bitmap sweep.
- Added a deterministic huge-proto case by generating a function whose
  prototype exceeds `LJ_HUGE_THRESHOLD`. The test asserts the live proto is in
  the huge table with `LJ_HUGEF_TRAVERSABLE|LJ_HUGEF_MARK`, then drops it and
  verifies the old pointer is no longer present in the huge table. This covers
  the intended fallback from `lj_mem_freegco_defer` to physical `lj_mem_free`
  for huge `GCproto` bodies.
- The huge-proto post-drop check only queries the huge table by address and
  does not dereference the old mapped pointer after `lua_gc(..., collect)`.

Verification:

- `tools/ci/m2_arena_gcsweep.sh`: passed.
- `tools/ci/m2_arena_gcmark.sh && tools/ci/m2_arena_gcverify.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `make -C src clean && make -j$(nproc)`: passed.
- `make -C src clean && make amalg -j$(nproc)`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after proto path coverage:

- Keep the next body-deferment target conservative: only consider a type once
  its free path has no link-table, aux-storage, finalizer, JIT, or plain/huge
  allocator side effects left to preserve.

Assertion-build lua_close proto/closure churn:

- Added `t-arena-gcclose`, a focused assertion-build lifecycle test that
  repeatedly creates a state, allocates hundreds of parser and bytecode-reader
  closures/prototypes, drops a subset of roots, and then relies on
  `lua_close`/`lj_gc_freeall` to release the remaining small protos and
  closures.
- Added `tools/ci/m2_arena_gcclose.sh`, which rebuilds LuaJIT with
  `-DLUA_USE_ASSERT`, compiles the C harness with the same assertion define,
  and runs it.
- Wired the new close-churn script into `tools/ci/m2_arena_all.sh`.

Verification:

- `tools/ci/m2_arena_gcclose.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -j$(nproc)`: passed.
- `make -C src clean && make amalg -j$(nproc)`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after close churn coverage:

- Keep the next body-deferment target conservative: only consider a type once
  its free path has no link-table, aux-storage, finalizer, JIT, or plain/huge
  allocator side effects left to preserve.

GCtab body deferment after auxiliary cleanup:

- Switched `lj_tab_free` to free table hash nodes and separated array storage
  exactly as before, then use `lj_mem_freegco_defer` only for the traversable
  table body. This keeps plain auxiliary arenas physically reclaimed by the
  legacy destructor while letting traversable table cells be reclaimed by the
  bitmap sweep.
- The body size is computed once before auxiliary cleanup. It remains
  `sizetabcolo(t->colo & 0x7f)` for both positive colocated tables and
  negative split-colocated tables; `t->colo < 0` still means the original body
  allocation included the old colocated tail even though the current array has
  been separated and is freed independently.
- Sidecar review agreed this is structurally safe because legacy sweep unlinks
  the dead `GCtab` before calling `lj_tab_free`. The retained dead body must
  not be scanned again: it may contain stale pointers to already-freed plain
  array/hash storage. Current arena sweep is bitmap-only, so this is within the
  same stale-edge boundary as `GCfunc`.
- Extended `t-arena-gcsweep` with:
  - a separated array/hash table, asserting array and node storage are
    physically freed while the table body cell is swept by traversable bitmap
    reclaim;
  - a positive colocated array table, asserting the whole colocated body is
    accounted and reclaimed as one traversable allocation;
  - a negative split-colocated table, asserting the separated current array is
    physically freed while body accounting still uses the original colocated
    allocation size.

Verification:

- `tools/ci/m2_arena_gcsweep.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -j$(nproc)`: passed.
- `make -C src clean && make amalg -j$(nproc)`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after table body deferment:

- Keep cdata, traces, threads, userdata, strings, upvalues, and plain/huge
  mark-bit reclamation out of the body-deferment path until their side effects
  or allocation classes are split and covered.

Weak/finalizer stress for retained dead table bodies:

- Strengthened `t-arena-gcclose` from proto/closure churn into
  proto/closure/table churn under `LUA_USE_ASSERT`.
- The Lua harness now:
  - creates and retains many split array tables so `lua_close` exercises
    table body deferment after auxiliary storage cleanup;
  - verifies weak-key, weak-value, and weak-key/value tables clear dead table
    entries after a full collection;
  - continues to leave many parser and bytecode-reader protos/closures for
    `lua_close`/`lj_gc_freeall`.
- Added a C `__gc` callback in the harness that allocates many tables and runs
  `collectgarbage('collect')` during finalization, stressing the post-finalize
  sweep boundary and retained dead table bodies while plain side storage is
  reused.

Verification:

- `tools/ci/m2_arena_gcclose.sh`: passed.
- `tools/ci/m2_arena_all.sh`: passed.
- `tools/ci/m2_gc_header_accessors.sh`: passed.
- `make -C src clean && make -j$(nproc)`: passed.
- `make -C src clean && make amalg -j$(nproc)`: passed.
- `tools/ci/m0_matrix.sh`: passed.
  - lockless JIT on: 508 stock tests passed.
  - lockless JIT off: 386 stock tests passed.

Remaining M2 allocator/GC bridge work after weak/finalizer table stress:

- Keep cdata, traces, threads, userdata, strings, upvalues, and plain/huge
  mark-bit reclamation out of the body-deferment path until their side effects
  or allocation classes are split and covered.

Arena gcmark test maintenance:

- Updated `t-arena-gcmark` to assert marks on the published `CTypeTab` header
  allocation via `ctype_tabh_acq(cts)`. The older `cts->tab` check pointed
  inside that allocation after the CTState table SMR wrapper landed, so it was
  no longer a valid arena block-start assertion.

Closed upvalue body deferment and sweep-ready ordering:

- Closed `GCupval` bodies now use `lj_mem_freegco_defer()` after unlinking from
  the legacy open-upvalue ring. Open upvalues still use immediate physical free
  after `unlinkuv()`, but closed upvalues keep their old `nextgc` chain until
  legacy sweep has a chance to splice any root-list edge that was skipped by
  head insertion during incremental sweep.
- `gc_traverse_thread()` now marks the per-thread legacy `openupval` list in
  addition to stack slots, frame functions, environment, and thread metatable.
  This matches the plan requirement that live thread roots include legacy open
  upvalues while source/v4 cell upvalues are still transitional.
- GC2 owner arena sweep is gated on `sweep_bridge_ready`. The bridge boundary
  preserves all currently root-chain-reachable traversable cells in the arena
  bitmap before publishing that ready flag, so deferred `gct=0` bodies reachable
  only through stale root-list links are retained until a later root sweep
  splices them.
- The arena-wide unmarked-body scan no longer runs destructors for normal
  `gct != 0` objects. Legacy sweep remains responsible for unlinking and
  destructor side effects; the arena scan reclaims only bodies whose destructor
  has already reduced accounting and left the body as deferred.

Verification:

- `BENCH_SCALE=0.01 timeout 60s src/luajit -joff auxiliary/bench/bench.lua
  closures_upval`: passed.
- `t-arena-gcsweep` now explicitly covers closed-upvalue body deferment and
  owner-sweep blocking before `lj_gc2_sweep_bridge_ready()`.
- `tools/ci/lua_test.sh m2_arena_all`: passed.
- `tools/ci/lua_test.sh m3_gc_active_thread_roots`: passed.
- `tools/ci/lua_test.sh m9_m10_gc`: passed.

Arena free-list and sweep-list hardening:

- Free-run insertion now removes duplicate entries for the same run and cuts
  invalid, self-linked, or overlong bin walks before publishing the run. Bin
  lookup applies the same pointer/alignment and walk-limit checks before
  unlinking a candidate.
- Arena prepare/restore/sweep list walks now cut self-links and malformed
  cycles instead of following them indefinitely. `lj_arena_sweep_one()` also
  drops an arena from the needsweep chain if the next link is self-referential
  or no longer marked as needsweep.
- Moved `lj_prng_condition()` out from the security-PRNG feature guard so
  standalone arena fixtures built with `-DLUAJIT_SECURITY_PRNG=0` can still
  derive deterministic low mapping hints.
- Verification: `LJ_TEST_DISABLE_BUILD_CACHE=1 tools/ci/lua_test.sh
  m2_arena_all` passed.
